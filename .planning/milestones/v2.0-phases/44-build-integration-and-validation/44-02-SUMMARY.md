# 44-02 Summary

The command reference and shipped-asset inventory now match the live cover workflow. They no longer imply Scriveno ships a generic cover template or bundles cover files as export templates, and they point writers at the canonical `.manuscript/build/` assets instead.
