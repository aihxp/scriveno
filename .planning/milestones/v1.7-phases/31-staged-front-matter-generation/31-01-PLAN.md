---
phase: 31-staged-front-matter-generation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/phase31-staged-front-matter-generation.test.js
autonomous: true
requirements:
  - FM-01
  - FM-02
  - FM-03
  - FM-04

must_haves:
  truths:
    - "Test file exists at test/phase31-staged-front-matter-generation.test.js"
    - "Tests for FM-01 through FM-04 are all written and failing (RED) before implementation begins"
    - "Test structure follows the Phase 30 node:test + node:assert/strict pattern"
    - "All 4 requirement groups have at least one describe block each"
  artifacts:
    - path: "test/phase31-staged-front-matter-generation.test.js"
      provides: "Regression assertions for FM-01 through FM-04"
      min_lines: 120
  key_links:
    - from: "test/phase31-staged-front-matter-generation.test.js"
      to: "commands/scr/front-matter.md"
      via: "readFile(FRONT_MATTER_PATH)"
      pattern: "FRONT_MATTER_PATH.*front-matter"
    - from: "test/phase31-staged-front-matter-generation.test.js"
      to: "commands/scr/export.md"
      via: "readFile(EXPORT_PATH)"
      pattern: "EXPORT_PATH.*export"
    - from: "test/phase31-staged-front-matter-generation.test.js"
      to: "commands/scr/publish.md"
      via: "readFile(PUBLISH_PATH)"
      pattern: "PUBLISH_PATH.*publish"
---

<objective>
Create the Phase 31 regression test suite in RED state. All tests target structural properties of three command files (`front-matter.md`, `export.md`, `publish.md`) that do not yet satisfy the assertions. Tests fail now; Wave 2 and Wave 3 implementations make them green.

Purpose: Nyquist compliance - every implementation task has a pre-existing automated test. RED-first prevents the executor from accidentally implementing without verifying.
Output: `test/phase31-staged-front-matter-generation.test.js` - ~150 lines, all tests failing until Wave 2 and Wave 3 complete.
</objective>

<execution_context>
@$HOME/.claude/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-staged-front-matter-generation/31-CONTEXT.md
@.planning/phases/31-staged-front-matter-generation/31-RESEARCH.md
</context>

<feature>
  <name>Phase 31 Staged Front-Matter - Regression Test Suite (RED)</name>
  <files>test/phase31-staged-front-matter-generation.test.js</files>
  <behavior>
    FM-01 tests (1 assertion):
    - front-matter.md does NOT contain `[Fill in` within the GENERATE element sections for elements 01, 03, 04, 07

    FM-02 tests (5 assertions, one per scaffold element):
    - front-matter.md text contains `scaffold: true` (at least once - overall presence check)
    - front-matter.md text contains `element: dedication` (near the 05-dedication save instruction)
    - front-matter.md text contains `element: epigraph` (near the 06-epigraph save instruction)
    - front-matter.md text contains `element: foreword` (near the 11-foreword save instruction)
    - front-matter.md text contains `element: preface` (near the 12-preface save instruction)
    - front-matter.md text contains `element: acknowledgments` (near the 13-acknowledgments save instruction)

    FM-03 tests (4 assertions):
    - export.md contains `STEP 1.6` heading
    - export.md `STEP 1.6` appears after `STEP 1.5` (indexOf ordering)
    - export.md `STEP 1.6` appears before `STEP 2` (indexOf ordering)
    - publish.md contains `STEP 1.6` heading
    - publish.md `STEP 1.6` appears after `STEP 1.5` (indexOf ordering)
    - publish.md `STEP 1.6` appears before `STEP 2` (indexOf ordering)
    - export.md STEP 3b references scaffold exclusion list from STEP 1.6

    FM-04 tests (3 assertions):
    - export.md contains `WORK.md` inside the STEP 1.6 section
    - export.md mentions the 4 GENERATE files: `01-half-title.md`, `03-title-page.md`, `04-copyright.md`, `07-toc.md`
    - export.md auto-refresh section says "Do NOT regenerate scaffold elements" or restricts to elements 1,3,4,7
  </behavior>
  <implementation>
    Write the test file following the exact pattern from `test/phase30-export-cleanup-validation-gate.test.js`:
    - `const { describe, it } = require('node:test');`
    - `const assert = require('node:assert/strict');`
    - `readFile()` helper that returns null on missing file
    - One `describe` block per requirement group (FM-01, FM-02, FM-03, FM-04)
    - Within each `describe`, one `it` per behavioral assertion
    - All `it` calls written before any implementation exists - tests will FAIL on first run (RED state)

    The tests are static text assertions - they call `content.includes(...)` or `content.indexOf(...)` on the raw file text. No execution of the command files. No mocking.

    After writing the file, run `node --test test/phase31-staged-front-matter-generation.test.js` and confirm the output shows FAIL results (RED). This is the correct and expected outcome for Wave 1.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Write Phase 31 test suite in RED state</name>
  <files>test/phase31-staged-front-matter-generation.test.js</files>

  <read_first>
    - test/phase30-export-cleanup-validation-gate.test.js (full - copy the exact structural pattern: require statements, readFile helper, describe/it block format, assert.ok style)
    - commands/scr/front-matter.md (lines 165-345 - the 5 scaffold element sections and the 4 GENERATE element sections to understand what the tests will check for)
    - commands/scr/export.md (lines 86-210 - STEP 1.5, STEP 2, STEP 3b - to understand what STEP 1.6 will be inserted between and where STEP 3b ends)
    - commands/scr/publish.md (lines 60-75 - STEP 1.5 ending, STEP 2 start - to understand the publish.md injection boundary)
  </read_first>

  <action>
    Create `test/phase31-staged-front-matter-generation.test.js` with the following structure. Write EVERY test as an assertion the files do NOT yet satisfy - all tests must fail (RED) when run before Wave 2 and Wave 3.

    File structure:

    ```javascript
    const { describe, it } = require('node:test');
    const assert = require('node:assert/strict');
    const fs = require('fs');
    const path = require('path');

    const ROOT = path.join(__dirname, '..');
    const FRONT_MATTER_PATH = path.join(ROOT, 'commands', 'scr', 'front-matter.md');
    const EXPORT_PATH = path.join(ROOT, 'commands', 'scr', 'export.md');
    const PUBLISH_PATH = path.join(ROOT, 'commands', 'scr', 'publish.md');

    function readFile(filePath) {
      try {
        return fs.readFileSync(filePath, 'utf8');
      } catch (_) {
        return null;
      }
    }
    ```

    Then write 4 describe groups:

    **Group 1 - FM-01: GENERATE elements produce no [Fill in] placeholders**

    One `it`: assert that front-matter.md does NOT contain `[Fill in` in the text between the GENERATE element section headings for elements 1, 3, 4, 7. Implementation: read file, check `!content.includes('[Fill in')` for the GENERATE sections specifically. Simplest approach: split the file on `#### Element` markers, extract the chunks for elements 1, 3, 4, 7, assert none contain `[Fill in`. Assert message: "GENERATE element sections (01, 03, 04, 07) must not contain [Fill in] placeholders - FM-01".

    **Group 2 - FM-02: front-matter.md adds scaffold: true YAML to 5 elements**

    Six `it` tests:
    1. `content.includes('scaffold: true')` - FM-02: front-matter.md must contain scaffold: true text at least once
    2. `content.includes('element: dedication')` - FM-02: dedication scaffold YAML must specify element name
    3. `content.includes('element: epigraph')` - FM-02: epigraph scaffold YAML must specify element name
    4. `content.includes('element: foreword')` - FM-02: foreword scaffold YAML must specify element name
    5. `content.includes('element: preface')` - FM-02: preface scaffold YAML must specify element name
    6. `content.includes('element: acknowledgments')` - FM-02: acknowledgments scaffold YAML must specify element name

    **Group 3 - FM-03: STEP 1.6 scaffold exclusion exists in export.md and publish.md, and STEP 3b references the exclusion list**

    Seven `it` tests:
    1. export.md contains 'STEP 1.6' - FM-03: export.md must have a STEP 1.6 scaffold gate
    2. In export.md: `indexOf('STEP 1.6') > indexOf('STEP 1.5')` - FM-03: STEP 1.6 must come after STEP 1.5
    3. In export.md: `indexOf('STEP 1.6') < indexOf('STEP 2')` - FM-03: STEP 1.6 must come before STEP 2
    4. publish.md contains 'STEP 1.6' - FM-03: publish.md must have a STEP 1.6 scaffold gate
    5. In publish.md: `indexOf('STEP 1.6') > indexOf('STEP 1.5')` - FM-03: STEP 1.6 must come after STEP 1.5 in publish.md
    6. In publish.md: `indexOf('STEP 1.6') < indexOf('STEP 2')` - FM-03: STEP 1.6 must come before STEP 2 in publish.md
    7. In export.md STEP 3b section: text contains 'scaffold exclusion' or 'exclusion list' or '1.6' after the '3b' heading - FM-03: STEP 3b must reference the STEP 1.6 scaffold exclusion list (Pitfall 1)

    **Group 4 - FM-04: export.md STEP 1.6 contains GENERATE auto-refresh logic**

    Three `it` tests:
    1. export.md contains `WORK.md` - FM-04: STEP 1.6 must reference WORK.md for timestamp comparison
    2. export.md contains all 4 GENERATE file names: `01-half-title.md`, `03-title-page.md`, `04-copyright.md`, `07-toc.md` - FM-04: auto-refresh targets must be named explicitly
    3. export.md contains text indicating regeneration is scoped to the 4 GENERATE elements only (look for: `'elements 1, 3, 4'` OR `'Do NOT regenerate scaffold'` OR `'1, 3, 4, and 7'`) - FM-04: auto-refresh must not overwrite scaffold elements (Pitfall 3)

    After writing the file, run the test and document that all tests fail (RED) as expected.
  </action>

  <acceptance_criteria>
    - `ls test/phase31-staged-front-matter-generation.test.js` returns the file path (file exists)
    - `node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | grep -c "not ok\|FAIL"` returns a count greater than 0 (tests are failing - RED state is correct)
    - `node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | grep -c "describe\|# "` returns at least 4 (4 describe blocks present)
    - `grep -c "scaffold: true" test/phase31-staged-front-matter-generation.test.js` returns at least 1 (FM-02 assertions reference scaffold: true)
    - `grep -c "STEP 1.6" test/phase31-staged-front-matter-generation.test.js` returns at least 2 (FM-03 assertions reference STEP 1.6 in both export.md and publish.md)
    - `grep "WORK.md" test/phase31-staged-front-matter-generation.test.js` returns at least 1 line (FM-04 assertion present)
    - `grep "element: dedication\|element: epigraph\|element: foreword\|element: preface\|element: acknowledgments" test/phase31-staged-front-matter-generation.test.js | wc -l` returns 5 (all 5 element name assertions present)
    - `npm test 2>&1 | grep -E "phase31|phase30"` - Phase 30 tests still pass; Phase 31 tests show failures (Phase 30 green = no regression introduced by adding the test file)
  </acceptance_criteria>

  <verify>
    <automated>node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | tail -10</automated>
  </verify>

  <done>
    - test/phase31-staged-front-matter-generation.test.js exists with 4 describe blocks covering FM-01 through FM-04
    - All Phase 31 tests FAIL (RED) - confirms assertions are testing properties not yet implemented
    - Phase 30 and earlier tests remain GREEN - adding the test file causes no regression
    - Test file follows exact node:test + node:assert/strict + readFile pattern from Phase 30
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> command files | Tests read command file content from disk; no execution of command files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | Test file assertions | accept | Static read-only assertions against file content. No execution path. Low risk: tests cannot modify production files. |
| T-31-02 | Repudiation | RED state confirmation | accept | RED state is documented in task done criteria. Executor confirms failures before proceeding to Wave 2. No persistent audit trail needed for test scaffolding step. |
</threat_model>

<verification>
1. `node --test test/phase31-staged-front-matter-generation.test.js` - all tests FAIL (RED - expected at Wave 1)
2. `npm test 2>&1 | grep "phase30"` - Phase 30 tests still GREEN
3. `grep -c "describe" test/phase31-staged-front-matter-generation.test.js` - returns at least 4
4. `grep "FM-01\|FM-02\|FM-03\|FM-04" test/phase31-staged-front-matter-generation.test.js` - all 4 requirement IDs referenced in test comments
</verification>

<success_criteria>
Test file written with all Phase 31 behavioral assertions in RED state. Prior test suite stays green. Wave 2 and Wave 3 will turn these tests GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/31-staged-front-matter-generation/31-01-SUMMARY.md`
</output>
