---
phase: 31-staged-front-matter-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - commands/scr/front-matter.md
autonomous: true
requirements:
  - FM-01
  - FM-02

must_haves:
  truths:
    - "Each of the 5 scaffold elements (05-dedication, 06-epigraph, 11-foreword, 12-preface, 13-acknowledgments) has its output markdown block prefixed with a YAML header containing scaffold: true and element: <name>"
    - "The 4 GENERATE element sections (01, 03, 04, 07) are unchanged - they still produce clean output with no [Fill in] placeholders"
    - "All other elements (02, 08-10, 14-19) are unchanged"
    - "The Phase 31 FM-01 and FM-02 tests pass GREEN after this plan"
  artifacts:
    - path: "commands/scr/front-matter.md"
      provides: "Updated element output instructions for 5 scaffold elements"
      contains: "scaffold: true"
  key_links:
    - from: "commands/scr/front-matter.md element 5 section"
      to: ".manuscript/front-matter/05-dedication.md"
      via: "Save to instruction after YAML-prefixed markdown block"
      pattern: "scaffold: true[\\s\\S]*?element: dedication"
    - from: "commands/scr/front-matter.md element 12 section"
      to: ".manuscript/front-matter/12-preface.md"
      via: "Save to instruction after YAML-prefixed markdown block"
      pattern: "scaffold: true[\\s\\S]*?element: preface"
---

<objective>
Update `commands/scr/front-matter.md` to prepend a `scaffold: true` YAML header to the output markdown blocks for the 5 personalization elements: dedication (05), epigraph (06), foreword (11), preface (12), acknowledgments (13).

Purpose: FM-02 - when the agent writes these files, the writer sees the `scaffold: true` gate in the file's frontmatter, signaling it needs personalization before export includes it. FM-01 is also verified here: the 4 GENERATE elements already have no `[Fill in]` placeholders; this plan confirms (and preserves) that fact.
Output: Surgical edits to 5 markdown code blocks in `commands/scr/front-matter.md`. No other files touched.
</objective>

<execution_context>
@$HOME/.claude/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-staged-front-matter-generation/31-CONTEXT.md
@.planning/phases/31-staged-front-matter-generation/31-RESEARCH.md
@.planning/phases/31-staged-front-matter-generation/31-01-SUMMARY.md

<interfaces>
<!-- Element save lines (from RESEARCH.md injection point table): -->
<!-- Element 5:  Dedication - Save line at 175: `Save to .../05-dedication.md` -->
<!-- Element 6:  Epigraph - Save line at 211: `Save to .../06-epigraph.md` -->
<!-- Element 11: Foreword - Save line at 283: `Save to .../11-foreword.md` -->
<!-- Element 12: Preface - Save line at 306: `Save to .../12-preface.md` -->
<!-- Element 13: Acknowledgments - Save line at 331: `Save to .../13-acknowledgments.md` -->

<!-- YAML header format (locked from CONTEXT.md): -->
<!--   ---                    -->
<!--   scaffold: true         -->
<!--   element: <name>        -->
<!--   ---                    -->
<!--                          -->
<!-- Where <name> is one of:  -->
<!--   dedication, epigraph, foreword, preface, acknowledgments -->

<!-- The element names match the 5 scaffold elements exactly. -->
<!-- The YAML header must appear INSIDE the markdown fenced code block, -->
<!-- as the FIRST content in the block, before the # Heading line. -->
<!-- The agent instruction says "write this content to the file". -->
<!-- The YAML header is part of the file content being written, not a comment. -->

<!-- Anti-pattern to avoid: do NOT add YAML frontmatter to GENERATE elements -->
<!-- (01-half-title, 03-title-page, 04-copyright, 07-toc) - they must remain -->
<!-- without frontmatter so absence = included by default in export assembly. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prepend scaffold: true YAML headers to 5 scaffold element outputs in front-matter.md</name>
  <files>commands/scr/front-matter.md</files>

  <read_first>
    - commands/scr/front-matter.md (full - must see the exact current markdown blocks for all 5 scaffold elements and the 4 GENERATE elements before making any edits; the line numbers from RESEARCH.md are guides but confirm visually before editing)
    - .planning/phases/31-staged-front-matter-generation/31-RESEARCH.md (Pattern 1: YAML Frontmatter Prepend - code examples; Critical Discrepancy sections for Acknowledgments and Epigraph - do NOT change their element type labels, only add YAML header; Anti-Patterns section - do NOT add YAML to GENERATE elements)
  </read_first>

  <behavior>
    - After edit: `content.includes('scaffold: true')` is true for front-matter.md
    - After edit: `content.includes('element: dedication')` is true
    - After edit: `content.includes('element: epigraph')` is true
    - After edit: `content.includes('element: foreword')` is true
    - After edit: `content.includes('element: preface')` is true
    - After edit: `content.includes('element: acknowledgments')` is true
    - GENERATE element sections (01, 03, 04, 07) do NOT gain any YAML frontmatter
    - Element type labels in section headers and element table remain unchanged (GENERATE DRAFT stays GENERATE DRAFT, SUGGEST stays SUGGEST)
  </behavior>

  <action>
    Read `commands/scr/front-matter.md` in full before making any changes.

    For each of the 5 scaffold elements, locate the markdown code fence (``` ```markdown ```) that contains the element's output content, and prepend the YAML header as the first content inside the fenced block.

    **Element 5: Dedication** (section around line 165-175)

    Current block starts with:
    ```markdown
    # Dedication
    ```

    Change to:
    ```markdown
    ---
    scaffold: true
    element: dedication
    ---

    # Dedication
    ```

    **Element 6: Epigraph** (section around line 188-211)

    Current block starts with:
    ```markdown
    # Epigraph
    ```

    Change to:
    ```markdown
    ---
    scaffold: true
    element: epigraph
    ---

    # Epigraph
    ```

    **Element 11: Foreword** (section around line 261-283)

    Current block starts with:
    ```markdown
    # Foreword
    ```

    Change to:
    ```markdown
    ---
    scaffold: true
    element: foreword
    ---

    # Foreword
    ```

    **Element 12: Preface** (section around line 289-306)

    Current block starts with:
    ```markdown
    # Preface
    ```

    Change to:
    ```markdown
    ---
    scaffold: true
    element: preface
    ---

    # Preface
    ```

    **Element 13: Acknowledgments** (section around line 321-331)

    Current block starts with:
    ```markdown
    # Acknowledgments
    ```

    Change to:
    ```markdown
    ---
    scaffold: true
    element: acknowledgments
    ---

    # Acknowledgments
    ```

    **Do NOT modify** the code blocks for elements 01, 03, 04, 07 (GENERATE elements) or any other element. Do NOT change any section heading text, element type labels, or the element table at the top of the file. Only insert the YAML header lines inside the 5 fenced markdown blocks.

    Write the updated file. Then run the Phase 31 test suite to confirm FM-01 and FM-02 tests go GREEN.
  </action>

  <acceptance_criteria>
    - `grep -c "scaffold: true" commands/scr/front-matter.md` returns exactly 5 (one per scaffold element)
    - `grep "element: dedication" commands/scr/front-matter.md` returns 1 line
    - `grep "element: epigraph" commands/scr/front-matter.md` returns 1 line
    - `grep "element: foreword" commands/scr/front-matter.md` returns 1 line
    - `grep "element: preface" commands/scr/front-matter.md` returns 1 line
    - `grep "element: acknowledgments" commands/scr/front-matter.md` returns 1 line
    - `grep -n "scaffold: true" commands/scr/front-matter.md` - all 5 lines appear inside fenced code blocks (lines between ``` ```markdown ``` and closing ``` ``` ```)
    - `grep "GENERATE DRAFT" commands/scr/front-matter.md` - Element 13 type label unchanged
    - `grep "SUGGEST" commands/scr/front-matter.md` - Element 6 type label unchanged
    - `grep -A5 "Element 1:" commands/scr/front-matter.md | grep "scaffold"` returns nothing (GENERATE element 01 has no scaffold YAML)
    - `node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | grep -E "FM-01|FM-02"` - FM-01 and FM-02 describe blocks show passing tests
    - `npm test` - full suite green
  </acceptance_criteria>

  <verify>
    <automated>node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | tail -10</automated>
  </verify>

  <done>
    - commands/scr/front-matter.md contains exactly 5 occurrences of `scaffold: true`
    - Each scaffold element (05, 06, 11, 12, 13) has `scaffold: true` + `element: <name>` YAML header in its output block
    - GENERATE element sections (01, 03, 04, 07) have no YAML frontmatter added
    - Element type labels (GENERATE DRAFT, SUGGEST, SCAFFOLD) unchanged in headers and table
    - FM-01 and FM-02 Phase 31 tests pass GREEN
    - npm test full suite green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent instruction -> file write | When writer runs /scr:front-matter, agent writes scaffold elements with YAML header; the YAML becomes the scaffold gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering | front-matter.md YAML header insertion | mitigate | Changes are additive (YAML prepended inside existing code blocks). Existing element content, type labels, and GENERATE sections not modified. Verified by grep checks confirming type labels and GENERATE sections unchanged after edit. |
| T-31-04 | Tampering | GENERATE elements accidentally receiving scaffold: true | mitigate | Acceptance criteria include explicit grep check: `grep -A5 "Element 1:"` must not contain "scaffold". Element 1 (half-title) is the canary - if GENERATE elements are untouched, all 4 are safe. |
</threat_model>

<verification>
1. `grep -c "scaffold: true" commands/scr/front-matter.md` - returns exactly 5
2. `grep "element: dedication\|element: epigraph\|element: foreword\|element: preface\|element: acknowledgments" commands/scr/front-matter.md | wc -l` - returns 5
3. `grep "GENERATE DRAFT" commands/scr/front-matter.md` - Element 13 type label present and unchanged
4. `node --test test/phase31-staged-front-matter-generation.test.js` - FM-01 and FM-02 groups GREEN
5. `npm test` - full suite green
</verification>

<success_criteria>
commands/scr/front-matter.md updated with scaffold: true YAML headers on all 5 personalization elements. GENERATE elements untouched. FM-01 and FM-02 tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/31-staged-front-matter-generation/31-02-SUMMARY.md`
</output>
