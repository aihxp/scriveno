---
phase: 31-staged-front-matter-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-02
files_modified:
  - commands/scr/export.md
  - commands/scr/publish.md
autonomous: true
requirements:
  - FM-03
  - FM-04

must_haves:
  truths:
    - "export.md contains STEP 1.6 positioned after STEP 1.5 and before STEP 2"
    - "export.md STEP 1.6 has two sub-steps: 1.6a (scaffold exclusion scan) and 1.6b (GENERATE auto-refresh)"
    - "export.md STEP 3b references the scaffold exclusion list built in STEP 1.6a so scaffold files are omitted from assembly"
    - "publish.md contains the same STEP 1.6 positioned after STEP 1.5 and before STEP 2"
    - "Both STEP 1.6 blocks are non-blocking: no stop instruction, always proceed to STEP 2"
    - "FM-03 and FM-04 Phase 31 tests pass GREEN; full npm test suite remains green"
  artifacts:
    - path: "commands/scr/export.md"
      provides: "STEP 1.6 scaffold gate + STEP 3b exclusion list reference"
      contains: "STEP 1.6"
    - path: "commands/scr/publish.md"
      provides: "STEP 1.6 scaffold gate"
      contains: "STEP 1.6"
  key_links:
    - from: "commands/scr/export.md STEP 1.6a"
      to: "commands/scr/export.md STEP 3b"
      via: "scaffold exclusion list referenced in 3b assembly step"
      pattern: "exclusion list.*STEP 1\\.6|1\\.6.*exclusion"
    - from: "commands/scr/export.md STEP 1.6b"
      to: "commands/scr/front-matter.md GENERATE steps"
      via: "re-run GENERATE elements 1, 3, 4, 7 instruction"
      pattern: "elements 1, 3, 4.*7|1, 3, 4, and 7"
    - from: "commands/scr/publish.md STEP 1.6"
      to: "commands/scr/export.md STEP 1.6"
      via: "publish dispatches to export which re-executes STEP 1.6 (idempotent)"
      pattern: "STEP 1\\.6"
---

<objective>
Inject STEP 1.6 (front-matter gate) into `commands/scr/export.md` and `commands/scr/publish.md`, and update STEP 3b in `export.md` to apply the scaffold exclusion list.

Purpose: FM-03 (scaffold files excluded from export assembly) and FM-04 (GENERATE elements auto-refresh when WORK.md is newer). STEP 1.6 is a single combined step with two sub-steps: 1.6a handles scaffold exclusion scan and 1.6b handles timestamp-based auto-refresh.
Output: Surgical edits to two existing command files. All Phase 31 tests go GREEN.
</objective>

<execution_context>
@$HOME/.claude/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-staged-front-matter-generation/31-CONTEXT.md
@.planning/phases/31-staged-front-matter-generation/31-RESEARCH.md
@.planning/phases/31-staged-front-matter-generation/31-02-SUMMARY.md

<interfaces>
<!-- Injection points (from RESEARCH.md, confirmed by direct file inspection): -->

<!-- export.md injection: -->
<!--   Line 116: `If no markers found: proceed to STEP 2.`  ← STEP 1.5 ends here -->
<!--   Line 117: (blank)                                                           -->
<!--   Line 118: `---`                                                             -->
<!--   Line 119: (blank)                                                           -->
<!--   Line 120: `### STEP 2: CHECK PREREQUISITES`          ← before this         -->
<!--   INSERT STEP 1.6 block AFTER line 116, shifting the existing --- + STEP 2 down -->

<!-- export.md STEP 3b (from RESEARCH.md Pattern 3 + direct inspection): -->
<!--   Lines 186-201: STEP 3b "Scan front matter" section -->
<!--   Current end of 3b: "Proceed with body content only." -->
<!--   ADD after the sort instruction: "Scaffold exclusion: Omit any files whose path -->
<!--     appears in the scaffold exclusion list from STEP 1.6a." -->

<!-- publish.md injection: -->
<!--   Line 66: `If no markers found: proceed to STEP 2.`  ← STEP 1.5 ends here -->
<!--   Line 67: (blank)                                                            -->
<!--   Line 68: `---`                                                              -->
<!--   Line 69: (blank)                                                            -->
<!--   Line 70: `### STEP 2: ROUTE`                        ← before this          -->
<!--   INSERT STEP 1.6 block AFTER line 66, shifting the existing --- + STEP 2 down -->

<!-- STEP 1.6 block (combined 1.6a + 1.6b, per RESEARCH.md Code Examples section): -->
<!--   Non-blocking: NO "stop" instruction. Always "Proceed to STEP 2." at end. -->
<!--   1.6a: scaffold scan + exclusion list + summary note if any excluded       -->
<!--   1.6b: WORK.md timestamp compare + conditional regenerate of 4 GENERATE elements only -->

<!-- STEP separator pattern (from Phase 30 plan pattern): -->
<!--   Between steps: blank line + `---` + blank line + `### STEP N: HEADING`   -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject STEP 1.6 into export.md and update STEP 3b</name>
  <files>commands/scr/export.md</files>

  <read_first>
    - commands/scr/export.md (full - must see exact current text at lines 86-125 for the injection boundary, and lines 180-210 for STEP 3b, before making any edits)
    - .planning/phases/31-staged-front-matter-generation/31-RESEARCH.md (Code Examples section: exact STEP 1.6 block text; Pattern 3: STEP 3b addition; Anti-Patterns: do NOT add stop language, do NOT regenerate all elements; Pitfall 1: STEP 3b must reference exclusion list; Pitfall 3: auto-refresh must name the 4 specific files only)
  </read_first>

  <behavior>
    - `indexOf('STEP 1.6')` in export.md is greater than `indexOf('STEP 1.5')`
    - `indexOf('STEP 1.6')` in export.md is less than `indexOf('STEP 2')`
    - export.md contains `WORK.md` in the STEP 1.6 section
    - export.md contains `01-half-title.md` in the STEP 1.6 section
    - export.md contains `03-title-page.md` in the STEP 1.6 section
    - export.md contains `04-copyright.md` in the STEP 1.6 section
    - export.md contains `07-toc.md` in the STEP 1.6 section
    - export.md STEP 3b section references the scaffold exclusion list from STEP 1.6a
    - export.md STEP 1.6 has NO "stop" or "Then **stop**" instruction (non-blocking)
    - export.md STEP 1.6 ends with "Proceed to STEP 2."
    - Existing STEP 1.5, STEP 2, and all content outside the two injection points are unchanged
  </behavior>

  <action>
    Read `commands/scr/export.md` in full before making any edits.

    **Edit 1: Inject STEP 1.6 after line 116**

    Locate the line reading `If no markers found: proceed to STEP 2.` (the last line of STEP 1.5). This is followed by a blank line, a `---` separator, a blank line, and `### STEP 2: CHECK PREREQUISITES`.

    Insert the following block between `If no markers found: proceed to STEP 2.` and the `---` separator that precedes STEP 2. The insertion is ADDITIVE - do not remove any existing lines.

    **Block to insert:**

    ```
    ---

    ### STEP 1.6: FRONT-MATTER GATE

    **1.6a - Scaffold exclusion**

    Check if `.manuscript/front-matter/` exists.

    If the directory does not exist:
    > **Note:** No front matter found - run `/scr:front-matter` first if you want publication front matter included.

    Proceed to 1.6b.

    If the directory exists, scan all `.md` files in `.manuscript/front-matter/`. For each file, check the first 10 lines for a YAML block containing `scaffold: true`. Build a scaffold exclusion list of the paths of all files where `scaffold: true` is found.

    If any files were added to the scaffold exclusion list, note them for the assembly step (STEP 3b) and show:
    > **Note:** [N] scaffold front-matter element(s) will be excluded from this export:
    >   - `.manuscript/front-matter/12-preface.md` (scaffold: true - edit and set scaffold: false to include)
    >
    > To include a scaffold element, open the file and change `scaffold: true` to `scaffold: false`.

    If no scaffold files were found, show no note.

    **1.6b - GENERATE element auto-refresh**

    If `.manuscript/WORK.md` does not exist, skip auto-refresh and proceed to STEP 2.

    Compare the modification timestamp of `.manuscript/WORK.md` against each of the following GENERATE front-matter files:
    - `.manuscript/front-matter/01-half-title.md`
    - `.manuscript/front-matter/03-title-page.md`
    - `.manuscript/front-matter/04-copyright.md`
    - `.manuscript/front-matter/07-toc.md`

    To compare timestamps, use the appropriate command for the platform:
    - macOS: `stat -f %m <file>`
    - Linux: `stat -c %Y <file>`
    - Windows: `(Get-Item '<file>').LastWriteTimeUtc.Ticks`
    - If timestamp comparison is not possible, assume WORK.md is newer and regenerate.

    If WORK.md is newer than ANY of those 4 files, or if ANY of those 4 files do not exist:
    Re-run the GENERATE step from `/scr:front-matter` for elements 1, 3, 4, and 7 only (half-title, title page, copyright page, TOC) using current WORK.md metadata. Do NOT regenerate scaffold elements (5, 6, 11, 12, 13) or any other elements.

    If WORK.md is not newer than all 4 files and all 4 files exist: skip regeneration silently.

    Proceed to STEP 2.
    ```

    After this insertion, the sequence in export.md should be:
    1. `If no markers found: proceed to STEP 2.` (end of STEP 1.5)
    2. (blank line)
    3. `---`
    4. (blank line)
    5. `### STEP 1.6: FRONT-MATTER GATE`
    6. ... (STEP 1.6 content) ...
    7. `Proceed to STEP 2.`
    8. (blank line)
    9. `---`
    10. (blank line)
    11. `### STEP 2: CHECK PREREQUISITES`

    **Edit 2: Update STEP 3b to reference the exclusion list**

    Locate the STEP 3b section (around line 186-201 in the original file, now shifted down by the STEP 1.6 insertion). The section begins with `**3b. Scan front matter:**` and contains:

    ```
    Read all files in `.manuscript/front-matter/` directory. Sort by numeric prefix to maintain Chicago Manual of Style order:
    ```

    After the sort instruction (the code block showing the sorted filenames), add the following sentence as a new bold paragraph:

    ```
    **Scaffold exclusion:** Omit any files whose path appears in the scaffold exclusion list from STEP 1.6a. These files have `scaffold: true` in their frontmatter and are not yet ready for publication.
    ```

    This sentence must appear within the 3b block, after the sort order code block and before the "If no front matter files exist" note.

    Write the updated file.
  </action>

  <acceptance_criteria>
    - `grep -n "STEP 1.6" commands/scr/export.md` returns at least 1 line with "STEP 1.6: FRONT-MATTER GATE"
    - `grep -n "STEP 1.5\|STEP 1.6\|STEP 2" commands/scr/export.md | head -5` shows STEP 1.5 line number < STEP 1.6 line number < STEP 2 line number
    - `grep "WORK.md" commands/scr/export.md` returns at least 1 line in the STEP 1.6 block
    - `grep "01-half-title.md\|03-title-page.md\|04-copyright.md\|07-toc.md" commands/scr/export.md | wc -l` returns 4 (all 4 GENERATE files named)
    - `grep "Do NOT regenerate scaffold\|elements 1, 3, 4" commands/scr/export.md` returns at least 1 line (scope restriction present)
    - `grep "exclusion list\|scaffold exclusion" commands/scr/export.md | wc -l` returns at least 2 (one in STEP 1.6a, one in STEP 3b)
    - `grep "1\.6a" commands/scr/export.md` returns at least 1 line
    - `grep "1\.6b" commands/scr/export.md` returns at least 1 line
    - `grep -A2 "STEP 1.6" commands/scr/export.md | grep "stop"` returns nothing (STEP 1.6 is non-blocking)
    - `grep "Proceed to STEP 2" commands/scr/export.md | wc -l` returns at least 3 (one from STEP 1.5, one from 1.6a, one from 1.6b end)
    - `grep "CHECK PREREQUISITES" commands/scr/export.md` returns 1 line (STEP 2 unchanged)
    - `node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | grep "FM-03\|FM-04"` - FM-03 and FM-04 export.md assertions show passing
    - `npm test` - full suite green
  </acceptance_criteria>

  <verify>
    <automated>node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | tail -10</automated>
  </verify>

  <done>
    - commands/scr/export.md contains STEP 1.6 positioned between STEP 1.5 and STEP 2
    - STEP 1.6 has sub-steps 1.6a (scaffold scan + exclusion list) and 1.6b (WORK.md timestamp + auto-refresh)
    - STEP 1.6 is non-blocking - ends with "Proceed to STEP 2.", no stop instruction
    - STEP 1.6b names only the 4 GENERATE files and explicitly says not to regenerate scaffold elements
    - STEP 3b contains scaffold exclusion sentence referencing the STEP 1.6a list
    - FM-03 and FM-04 tests for export.md pass GREEN
    - Full npm test suite green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject STEP 1.6 into publish.md</name>
  <files>commands/scr/publish.md</files>

  <read_first>
    - commands/scr/publish.md (full - must see exact current text at lines 60-75 for the injection boundary; confirm line 66 reads "If no markers found: proceed to STEP 2." and line 70 reads "### STEP 2: ROUTE" before making any edits)
    - .planning/phases/31-staged-front-matter-generation/31-RESEARCH.md (Pitfall 5: double-execution of STEP 1.6 is idempotent and acceptable; Open Question 3: publish.md has no own STEP 3b so no 3b edit needed here; Pattern 2: STEP 1.5 as injection template)
  </read_first>

  <behavior>
    - `indexOf('STEP 1.6')` in publish.md is greater than `indexOf('STEP 1.5')`
    - `indexOf('STEP 1.6')` in publish.md is less than `indexOf('STEP 2')`
    - publish.md STEP 1.6 is non-blocking - no "stop" instruction, ends with "Proceed to STEP 2."
    - publish.md STEP 1.6 contains `WORK.md` reference for auto-refresh
    - publish.md STEP 1 (LOAD CONTEXT) and STEP 2 (ROUTE) content unchanged
  </behavior>

  <action>
    Read `commands/scr/publish.md` in full before making any edits.

    Locate the line reading `If no markers found: proceed to STEP 2.` (last line of STEP 1.5, at line 66). This is followed by a blank line, a `---` separator, a blank line, and `### STEP 2: ROUTE`.

    Insert the following STEP 1.6 block between `If no markers found: proceed to STEP 2.` and the existing `---` separator. The insertion is ADDITIVE - do not remove any existing lines.

    **Block to insert:**

    ```
    ---

    ### STEP 1.6: FRONT-MATTER GATE

    **1.6a - Scaffold exclusion**

    Check if `.manuscript/front-matter/` exists.

    If the directory does not exist:
    > **Note:** No front matter found - run `/scr:front-matter` first if you want publication front matter included.

    Proceed to 1.6b.

    If the directory exists, scan all `.md` files in `.manuscript/front-matter/`. For each file, check the first 10 lines for a YAML block containing `scaffold: true`. Build a scaffold exclusion list of the paths of all files where `scaffold: true` is found.

    If any files were added to the scaffold exclusion list, note them for the assembly step and show:
    > **Note:** [N] scaffold front-matter element(s) will be excluded from this export:
    >   - `.manuscript/front-matter/12-preface.md` (scaffold: true - edit and set scaffold: false to include)
    >
    > To include a scaffold element, open the file and change `scaffold: true` to `scaffold: false`.

    If no scaffold files were found, show no note.

    **1.6b - GENERATE element auto-refresh**

    If `.manuscript/WORK.md` does not exist, skip auto-refresh and proceed to STEP 2.

    Compare the modification timestamp of `.manuscript/WORK.md` against each of the following GENERATE front-matter files:
    - `.manuscript/front-matter/01-half-title.md`
    - `.manuscript/front-matter/03-title-page.md`
    - `.manuscript/front-matter/04-copyright.md`
    - `.manuscript/front-matter/07-toc.md`

    To compare timestamps, use the appropriate command for the platform:
    - macOS: `stat -f %m <file>`
    - Linux: `stat -c %Y <file>`
    - Windows: `(Get-Item '<file>').LastWriteTimeUtc.Ticks`
    - If timestamp comparison is not possible, assume WORK.md is newer and regenerate.

    If WORK.md is newer than ANY of those 4 files, or if ANY of those 4 files do not exist:
    Re-run the GENERATE step from `/scr:front-matter` for elements 1, 3, 4, and 7 only (half-title, title page, copyright page, TOC) using current WORK.md metadata. Do NOT regenerate scaffold elements (5, 6, 11, 12, 13) or any other elements.

    If WORK.md is not newer than all 4 files and all 4 files exist: skip regeneration silently.

    Proceed to STEP 2.
    ```

    After this insertion, the sequence in publish.md should be:
    1. `If no markers found: proceed to STEP 2.` (end of STEP 1.5)
    2. (blank line)
    3. `---`
    4. (blank line)
    5. `### STEP 1.6: FRONT-MATTER GATE`
    6. ... (STEP 1.6 content) ...
    7. `Proceed to STEP 2.`
    8. (blank line)
    9. `---`
    10. (blank line)
    11. `### STEP 2: ROUTE`

    Write the updated file.
  </action>

  <acceptance_criteria>
    - `grep -n "STEP 1.6" commands/scr/publish.md` returns at least 1 line with "STEP 1.6: FRONT-MATTER GATE"
    - `grep -n "STEP 1.5\|STEP 1.6\|STEP 2" commands/scr/publish.md | head -5` shows STEP 1.5 line < STEP 1.6 line < STEP 2 line
    - `grep "WORK.md" commands/scr/publish.md` returns at least 1 line (1.6b present)
    - `grep "01-half-title.md" commands/scr/publish.md` returns at least 1 line (GENERATE files named)
    - `grep -A2 "STEP 1.6" commands/scr/publish.md | grep "stop"` returns nothing (non-blocking)
    - `grep "Proceed to STEP 2" commands/scr/publish.md | wc -l` returns at least 3 (one from STEP 1.5, one from 1.6a, one from 1.6b end)
    - `grep "ROUTE" commands/scr/publish.md` returns 1 line with "STEP 2: ROUTE" (unchanged)
    - `grep "LOAD CONTEXT" commands/scr/publish.md` returns 1 line (STEP 1 unchanged)
    - `node --test test/phase31-staged-front-matter-generation.test.js` - all Phase 31 tests GREEN (FM-01 through FM-04)
    - `npm test` - full suite green
  </acceptance_criteria>

  <verify>
    <automated>node --test test/phase31-staged-front-matter-generation.test.js 2>&1 | tail -10</automated>
  </verify>

  <done>
    - commands/scr/publish.md contains STEP 1.6 positioned between STEP 1.5 and STEP 2 (ROUTE)
    - STEP 1.6 has sub-steps 1.6a and 1.6b - same structure as export.md
    - STEP 1.6 is non-blocking
    - STEP 2 (ROUTE) and STEP 1 (LOAD CONTEXT) content unchanged
    - All Phase 31 regression tests GREEN (FM-01 through FM-04)
    - Full npm test suite green - Phase 31 complete
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer -> /scr:export STEP 1.6a | Agent scans .manuscript/front-matter/ for scaffold: true; exclusion is non-blocking |
| writer -> /scr:export STEP 1.6b | Agent compares WORK.md mtime to GENERATE files; may regenerate 4 files |
| writer -> /scr:publish STEP 1.6 | Same as export; publish chains to export so STEP 1.6 executes twice (idempotent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | export.md STEP 1.6 injection | mitigate | Additive insertion only. Existing STEP 1.5 and STEP 2 content verified unchanged by grep checks for "CHECK PREREQUISITES" and "STEP 1.5" after edit. |
| T-31-06 | Tampering | publish.md STEP 1.6 injection | mitigate | Same additive-only approach. STEP 1 (LOAD CONTEXT) and STEP 2 (ROUTE) verified present after edit. |
| T-31-07 | Tampering | Auto-refresh overwrites scaffold files | mitigate | STEP 1.6b instruction explicitly names only the 4 GENERATE files and says "Do NOT regenerate scaffold elements (5, 6, 11, 12, 13)". Acceptance criteria grep-verifies this restriction text is present. |
| T-31-08 | Denial of Service | Scaffold exclusion blocks export | accept | Scaffold exclusion is explicitly non-blocking per CONTEXT.md locked decision. No "stop" language allowed in STEP 1.6. Accepted: writer can always export; scaffold files are silently excluded with a summary note. |
| T-31-09 | Repudiation | Scaffold files excluded without writer awareness | mitigate | STEP 1.6a produces a named summary note listing each excluded file with opt-in instructions. Writer sees which files were excluded and why in every export session where exclusions occur. |
</threat_model>

<verification>
1. `grep -n "STEP 1.5\|STEP 1.6\|STEP 2" commands/scr/export.md | head -6` - STEP 1.5 line < STEP 1.6 line < STEP 2 line
2. `grep -n "STEP 1.5\|STEP 1.6\|STEP 2" commands/scr/publish.md | head -6` - STEP 1.5 line < STEP 1.6 line < STEP 2 line
3. `grep "scaffold exclusion\|exclusion list" commands/scr/export.md | wc -l` - returns at least 2 (STEP 1.6a + STEP 3b)
4. `grep "Do NOT regenerate scaffold\|elements 1, 3, 4" commands/scr/export.md` - restriction text present
5. `grep "Do NOT regenerate scaffold\|elements 1, 3, 4" commands/scr/publish.md` - restriction text present
6. `node --test test/phase31-staged-front-matter-generation.test.js` - all FM-01 through FM-04 tests GREEN
7. `npm test` - full suite green (1132+ prior tests + Phase 31 new tests all passing)
</verification>

<success_criteria>
STEP 1.6 injected into both export.md and publish.md. STEP 3b updated in export.md to apply scaffold exclusion. All Phase 31 regression tests (FM-01 through FM-04) passing. Full npm test suite green. Phase 31 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/31-staged-front-matter-generation/31-03-SUMMARY.md`
</output>
