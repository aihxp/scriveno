---
phase: 33-sacred-tradition-profiles
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - commands/scr/build-ebook.md
  - commands/scr/build-print.md
autonomous: true
requirements:
  - TRAD-05

must_haves:
  truths:
    - "build-ebook.md contains a STEP 1.7: TRADITION LOADING section"
    - "build-print.md contains a STEP 1.7: TRADITION LOADING section"
    - "STEP 1.7 appears after STEP 1.6 and before ### STEP 2: in both files"
    - "STEP 1.7 silently skips when tradition: is absent or null"
    - "STEP 1.7 applies font-family and lang to metadata.yaml when tradition is present"
    - "STEP 1.7 adds --metadata dir=rtl to STEP 4 Pandoc invocation when rtl: true"
    - "STEP 1.7 notes approval block requirement when approval_block.required: true"
    - "Tradition slug is validated via inline list (per Phase 32 CR-01 fix - no JS function call)"
  artifacts:
    - path: "commands/scr/build-ebook.md"
      provides: "EPUB build pipeline with tradition loading at STEP 1.7"
      contains: "STEP 1.7"
    - path: "commands/scr/build-print.md"
      provides: "Print PDF build pipeline with tradition loading at STEP 1.7"
      contains: "STEP 1.7"
  key_links:
    - from: "commands/scr/build-ebook.md STEP 1.7"
      to: "templates/sacred/{tradition}/manifest.yaml"
      via: "reads manifest by slug from config.json tradition: key"
      pattern: "templates/sacred/\\{tradition\\}/manifest\\.yaml"
    - from: "commands/scr/build-print.md STEP 1.7"
      to: "templates/sacred/{tradition}/manifest.yaml"
      via: "identical to build-ebook.md"
      pattern: "templates/sacred/\\{tradition\\}/manifest\\.yaml"
    - from: "STEP 1.7"
      to: ".manuscript/output/metadata.yaml"
      via: "sets font-family and lang keys before STEP 3f writes metadata.yaml"
      pattern: "metadata\\.yaml"
---

<objective>
Add STEP 1.7 (TRADITION LOADING) between STEP 1.6 and STEP 2 in both `commands/scr/build-ebook.md` and `commands/scr/build-print.md`.

Purpose: When a writer's project has `tradition:` in config.json, the build pipeline loads the tradition manifest and applies it to the output - font stack, RTL direction, lang tag, and approval block note. When absent, the step is silent and transparent to existing workflows.

Output: Both build command files updated with identical STEP 1.7 wording, positioned correctly in the step sequence.
</objective>

<execution_context>
@/Users/hprincivil/.claude/workflows/execute-plan.md
@/Users/hprincivil/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-sacred-tradition-profiles/33-CONTEXT.md
@.planning/phases/33-sacred-tradition-profiles/33-PATTERNS.md

<interfaces>
<!-- Current STEP 1.6 end in build-ebook.md (lines 120-124) -->
<!-- Insertion point is between "Proceed to STEP 2." and "### STEP 2:" -->
```markdown
If WORK.md is not newer than all 4 files and all 4 files exist: skip regeneration silently.

Proceed to STEP 2.

---

### STEP 2: CHECK PREREQUISITES
```

<!-- Current STEP 1.6 end in build-print.md (lines 127-132) -->
<!-- Identical close pattern -->
```markdown
If WORK.md is not newer than all 4 files and all 4 files exist: skip regeneration silently.

Proceed to STEP 2.

---

### STEP 2: CHECK PREREQUISITES
```

<!-- STEP heading format (from 33-PATTERNS.md - must match exactly for TRAD-05 test) -->
<!-- Test checks: content.indexOf('STEP 1.7') and content.includes('TRADITION LOADING') -->
```markdown
### STEP 1.7: TRADITION LOADING
```

<!-- Valid tradition slugs (inline list per Phase 32 CR-01 fix - NOT a JS function call) -->
<!-- Source: data/CONSTRAINTS.json architectural_profiles.traditions -->
<!-- catholic, orthodox, tewahedo, protestant, jewish, islamic-hafs, islamic-warsh, pali, tibetan, sanskrit -->

<!-- Termination pattern for intermediate steps (from 33-PATTERNS.md) -->
```markdown
Proceed to STEP 2.

---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add STEP 1.7 to build-ebook.md</name>
  <files>commands/scr/build-ebook.md</files>
  <read_first>
    - commands/scr/build-ebook.md (full file - read before editing to find exact insertion point)
    - .planning/phases/33-sacred-tradition-profiles/33-PATTERNS.md (STEP 1.7 block content, lines 134-156)
  </read_first>
  <action>
Read `commands/scr/build-ebook.md` in full. Locate the exact text:

```
Proceed to STEP 2.

---

### STEP 2: CHECK PREREQUISITES
```

This block appears at the end of STEP 1.6. Insert STEP 1.7 between the closing `---` of STEP 1.6 and `### STEP 2:`.

After the edit, the sequence must be:

```
Proceed to STEP 2.

---

### STEP 1.7: TRADITION LOADING

Read `tradition:` from `.manuscript/config.json`.

If absent or null: skip this step silently and proceed to STEP 2.

Validate the tradition slug against the accepted list:
`catholic`, `orthodox`, `tewahedo`, `protestant`, `jewish`, `islamic-hafs`, `islamic-warsh`, `pali`, `tibetan`, `sanskrit`

If the value is not in this list:
> **Unknown tradition "{tradition}". Valid values: catholic, orthodox, tewahedo, protestant, jewish, islamic-hafs, islamic-warsh, pali, tibetan, sanskrit**

Then **stop**.

If present and valid, load `templates/sacred/{tradition}/manifest.yaml`.

Apply tradition data to `.manuscript/output/metadata.yaml` (before STEP 3f writes the file):
- Set `lang:` to the tradition's primary language tag:
  - `arabic` script -> `ar`
  - `hebrew` script -> `he`
  - `ethiopic` script -> `am` (Amharic, primary Ge'ez liturgical language)
  - `tibetan` script -> `bo`
  - `devanagari` script -> `sa` (Sanskrit)
  - `latin` script -> use the project language from config.json (default `en`)
- Set `font-family:` to the first entry in the manifest's `font_stack`.

If `rtl: true` in the manifest, add `--metadata dir=rtl` to the Pandoc invocation in STEP 4.

If `approval_block.required: true` in the manifest, show after the build completes:
> **Note:** This tradition requires an approval block ("{{approval_block.label}}") before publication. Scope: {{approval_block.scope}}.

Proceed to STEP 2.

---

### STEP 2: CHECK PREREQUISITES
```

Do NOT modify any other part of the file.
  </action>
  <verify>
    <automated>cd /Users/hprincivil/Projects/scriveno && node -e "const c=require('fs').readFileSync('commands/scr/build-ebook.md','utf8'); const s16=c.indexOf('STEP 1.6'); const s17=c.indexOf('STEP 1.7'); const s2=c.indexOf('### STEP 2:'); console.log('s16:', s16, 's17:', s17, 's2:', s2, 'order_ok:', s16 < s17 && s17 < s2);"</automated>
  </verify>
  <acceptance_criteria>
    - grep "STEP 1.7" commands/scr/build-ebook.md returns a match
    - grep "TRADITION LOADING" commands/scr/build-ebook.md returns a match
    - grep "tradition:" commands/scr/build-ebook.md returns at least 2 matches (config.json read + slug validation)
    - grep "rtl: true" commands/scr/build-ebook.md returns a match (RTL conditional)
    - grep "approval_block.required" commands/scr/build-ebook.md returns a match
    - grep "skip this step silently" commands/scr/build-ebook.md returns a match
    - node -e above prints order_ok: true (s16 < s17 < s2)
    - grep "STEP 1.5" commands/scr/build-ebook.md returns a match (existing step not removed)
    - grep "STEP 1.6" commands/scr/build-ebook.md returns a match (existing step not removed)
  </acceptance_criteria>
  <done>build-ebook.md has STEP 1.7: TRADITION LOADING inserted between STEP 1.6 and STEP 2, with inline slug validation, lang/font application, RTL flag, and approval block note.</done>
</task>

<task type="auto">
  <name>Task 2: Add STEP 1.7 to build-print.md and run TRAD-05 GREEN check</name>
  <files>commands/scr/build-print.md</files>
  <read_first>
    - commands/scr/build-print.md (full file - read before editing to find exact insertion point)
    - commands/scr/build-ebook.md (verify STEP 1.7 wording just added - use as reference for identical wording)
  </read_first>
  <action>
Read `commands/scr/build-print.md` in full. Locate the exact text:

```
Proceed to STEP 2.

---

### STEP 2: CHECK PREREQUISITES
```

This block appears at the end of STEP 1.6 (around line 129-132 before this edit). Insert STEP 1.7 between the closing `---` of STEP 1.6 and `### STEP 2:`.

The STEP 1.7 wording MUST be word-for-word identical to build-ebook.md (copy the block as inserted in Task 1). The TRAD-05 test uses the same `content.includes('TRADITION LOADING')` check on both files - any wording difference will cause one test to fail.

After the edit, the sequence must be:

```
Proceed to STEP 2.

---

### STEP 1.7: TRADITION LOADING

Read `tradition:` from `.manuscript/config.json`.

If absent or null: skip this step silently and proceed to STEP 2.

Validate the tradition slug against the accepted list:
`catholic`, `orthodox`, `tewahedo`, `protestant`, `jewish`, `islamic-hafs`, `islamic-warsh`, `pali`, `tibetan`, `sanskrit`

If the value is not in this list:
> **Unknown tradition "{tradition}". Valid values: catholic, orthodox, tewahedo, protestant, jewish, islamic-hafs, islamic-warsh, pali, tibetan, sanskrit**

Then **stop**.

If present and valid, load `templates/sacred/{tradition}/manifest.yaml`.

Apply tradition data to `.manuscript/output/metadata.yaml` (before STEP 3f writes the file):
- Set `lang:` to the tradition's primary language tag:
  - `arabic` script -> `ar`
  - `hebrew` script -> `he`
  - `ethiopic` script -> `am` (Amharic, primary Ge'ez liturgical language)
  - `tibetan` script -> `bo`
  - `devanagari` script -> `sa` (Sanskrit)
  - `latin` script -> use the project language from config.json (default `en`)
- Set `font-family:` to the first entry in the manifest's `font_stack`.

If `rtl: true` in the manifest, add `--metadata dir=rtl` to the Pandoc invocation in STEP 4.

If `approval_block.required: true` in the manifest, show after the build completes:
> **Note:** This tradition requires an approval block ("{{approval_block.label}}") before publication. Scope: {{approval_block.scope}}.

Proceed to STEP 2.

---

### STEP 2: CHECK PREREQUISITES
```

Do NOT modify any other part of the file.

After writing the file, run the full Phase 33 test suite to confirm GREEN:

```bash
cd /Users/hprincivil/Projects/scriveno && node --test test/phase33-sacred-tradition-profiles.test.js
```

All tests should pass (GREEN state). If TRAD-05 still fails, re-read the file and verify the insertion was placed correctly.
  </action>
  <verify>
    <automated>cd /Users/hprincivil/Projects/scriveno && node --test test/phase33-sacred-tradition-profiles.test.js 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "STEP 1.7" commands/scr/build-print.md returns a match
    - grep "TRADITION LOADING" commands/scr/build-print.md returns a match
    - grep "rtl: true" commands/scr/build-print.md returns a match
    - grep "approval_block.required" commands/scr/build-print.md returns a match
    - grep "skip this step silently" commands/scr/build-print.md returns a match
    - node -e "const c=require('fs').readFileSync('commands/scr/build-print.md','utf8'); const s16=c.indexOf('STEP 1.6'); const s17=c.indexOf('STEP 1.7'); const s2=c.indexOf('### STEP 2:'); console.log(s16 < s17 && s17 < s2);" prints true
    - node --test test/phase33-sacred-tradition-profiles.test.js exits zero (ALL tests GREEN including TRAD-05)
    - grep "STEP 1.5" commands/scr/build-print.md returns a match (existing steps not removed)
    - grep "STEP 1.6" commands/scr/build-print.md returns a match (existing steps not removed)
  </acceptance_criteria>
  <done>build-print.md has STEP 1.7: TRADITION LOADING with identical wording to build-ebook.md. Full Phase 33 test suite exits zero - all TRAD-01 through TRAD-05 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json tradition: -> manifest path | Agent constructs path from `{tradition}` slug; slug validated against inline list before any filesystem read |
| manifest.yaml -> metadata.yaml | Agent reads static YAML file, extracts safe string/bool fields |
| metadata.yaml -> Pandoc invocation | `--metadata dir=rtl` flag is a static string added conditionally; no user-controlled interpolation into shell command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Tampering | STEP 1.7 tradition slug -> path construction | mitigate | Validate slug against explicit inline list (`catholic`, `orthodox`, ...) before constructing `templates/sacred/{tradition}/manifest.yaml` path - per Phase 32 CR-01 inline-list pattern |
| T-33-07 | Elevation of Privilege | `--metadata dir=rtl` injection | accept | Flag value is a static string from manifest boolean field `rtl:`, not from user input. Manifest is source-controlled. |
| T-33-08 | Repudiation | Approval block note shown post-build | accept | Note is informational only; writer takes action externally (getting approval). No audit log required at this stage. |
| T-33-09 | Denial of Service | Missing manifest file after slug validation | accept | Validator uses directory-listing intersection (Phase 29 Plan 03 pattern) - if manifest exists in directory, it was committed. Slug validation + manifest read are closely coupled. |
</threat_model>

<verification>
Full Phase 33 test suite - all 5 requirements GREEN:

```bash
cd /Users/hprincivil/Projects/scriveno && node --test test/phase33-sacred-tradition-profiles.test.js
```

Expected: zero exit code, all TRAD-01 through TRAD-05 tests pass.

Verify full regression suite still passes (no regressions):

```bash
cd /Users/hprincivil/Projects/scriveno && npm test 2>&1 | tail -20
```

Spot-check STEP ordering in both files:

```bash
node -e "
  const fs = require('fs');
  for (const f of ['commands/scr/build-ebook.md', 'commands/scr/build-print.md']) {
    const c = fs.readFileSync(f, 'utf8');
    const s16 = c.indexOf('STEP 1.6');
    const s17 = c.indexOf('STEP 1.7');
    const s2  = c.indexOf('### STEP 2:');
    console.log(f, s16 < s17 && s17 < s2 ? 'ORDER OK' : 'ORDER FAIL');
  }
"
```
</verification>

<success_criteria>
- Both `commands/scr/build-ebook.md` and `commands/scr/build-print.md` contain `### STEP 1.7: TRADITION LOADING`
- STEP 1.7 wording is identical in both files
- STEP ordering: 1.5 -> 1.6 -> 1.7 -> 2 in both files
- STEP 1.7 handles absent/null tradition silently (skip)
- STEP 1.7 validates slug via inline list before path construction
- STEP 1.7 applies lang, font-family, RTL flag, and approval block note when tradition is present
- `node --test test/phase33-sacred-tradition-profiles.test.js` exits zero
- `npm test` exits zero (no regressions in prior phases)
</success_criteria>

<output>
After completion, create `.planning/phases/33-sacred-tradition-profiles/33-03-SUMMARY.md` following the summary template.
</output>
