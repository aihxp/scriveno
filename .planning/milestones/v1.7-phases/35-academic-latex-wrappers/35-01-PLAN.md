---
phase: 35-academic-latex-wrappers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/phase35-academic-latex-wrappers.test.js
autonomous: true
requirements:
  - TPL-07

must_haves:
  truths:
    - "Running node --test test/phase35-academic-latex-wrappers.test.js produces failing tests (RED state) covering all TPL-07 behaviors"
    - "Each of the 5 wrapper templates has file-existence and documentclass tests"
    - "Each wrapper has tightlist, $body$, $if(abstract)$, and CSL reference environment tests"
    - "build-print.md is tested for kpsewhich, academic platform slugs, paper-{platform}.tex output naming, and tlmgr install guidance strings"
    - "CONSTRAINTS.json is tested for exports.build_print.available containing 'academic'"
  artifacts:
    - path: "test/phase35-academic-latex-wrappers.test.js"
      provides: "Phase 35 regression suite - all TPL-07 assertions"
      min_lines: 180
  key_links:
    - from: "test/phase35-academic-latex-wrappers.test.js"
      to: "data/export-templates/scriveno-{ieee,acm,lncs,elsevier,apa7}.latex"
      via: "fs.readFileSync + assert.ok content checks"
      pattern: "readFile.*scriveno-ieee"
    - from: "test/phase35-academic-latex-wrappers.test.js"
      to: "data/CONSTRAINTS.json"
      via: "JSON.parse + nested path access"
      pattern: "constraints\\.exports\\.build_print\\.available"
---

<objective>
Create the Wave 0 regression test file for Phase 35. All tests begin in RED (failing) state because the template files and build-print.md extensions don't exist yet. Plans 02 and 03 make them go GREEN.

Purpose: Establish machine-executable contracts for TPL-07 before implementation starts. Wave 1 runs first so executors in Wave 2 get immediate feedback on every file they create.

Output: test/phase35-academic-latex-wrappers.test.js - 180+ lines covering all TPL-07 assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-academic-latex-wrappers/35-CONTEXT.md
@.planning/phases/35-academic-latex-wrappers/35-RESEARCH.md
@.planning/phases/35-academic-latex-wrappers/35-PATTERNS.md
@.planning/phases/35-academic-latex-wrappers/35-VALIDATION.md

<interfaces>
<!-- Analog test file - copy header and helper VERBATIM, then write Phase 35 describe/it blocks -->

From test/phase34-cross-domain-templates.test.js (lines 1-21):
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const ROOT              = path.join(__dirname, '..');
const TEMPLATES_DIR     = path.join(ROOT, 'data', 'export-templates');
const COMMANDS_DIR      = path.join(ROOT, 'commands', 'scr');
const BUILD_PRINT_PATH  = path.join(ROOT, 'commands', 'scr', 'build-print.md');
const CONSTRAINTS_PATH  = path.join(ROOT, 'data', 'CONSTRAINTS.json');

function readFile(filePath) {
  try { return fs.readFileSync(filePath, 'utf8'); }
  catch (_) { return null; }
}
```

CONSTRAINTS.json target key to test (exports section, verified from PATTERNS.md):
```json
constraints.exports.build_print.available  // must include "academic"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write phase35-academic-latex-wrappers.test.js</name>
  <files>test/phase35-academic-latex-wrappers.test.js</files>

  <read_first>
    - test/phase34-cross-domain-templates.test.js - copy header, path constants, readFile helper VERBATIM; study describe/it structure and assertion message format
    - .planning/phases/35-academic-latex-wrappers/35-RESEARCH.md - section "Validation Architecture -> Phase Requirements -> Test Map" lists all 14 assertions required
    - .planning/phases/35-academic-latex-wrappers/35-PATTERNS.md - section "test/phase35-academic-latex-wrappers.test.js" contains the exact JavaScript patterns to use
  </read_first>

  <action>
Create test/phase35-academic-latex-wrappers.test.js following the exact pattern of test/phase34-cross-domain-templates.test.js.

**File header - copy VERBATIM from phase34 analog (lines 1-21), update COMMANDS_DIR to COMMANDS_DIR (keep same variable name but unused is fine):**
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const ROOT              = path.join(__dirname, '..');
const TEMPLATES_DIR     = path.join(ROOT, 'data', 'export-templates');
const BUILD_PRINT_PATH  = path.join(ROOT, 'commands', 'scr', 'build-print.md');
const CONSTRAINTS_PATH  = path.join(ROOT, 'data', 'CONSTRAINTS.json');

function readFile(filePath) {
  try { return fs.readFileSync(filePath, 'utf8'); }
  catch (_) { return null; }
}
```

**Write one describe block per template file (5 blocks), each with these it-blocks:**

For EACH of the 5 platforms (ieee, acm, lncs, elsevier, apa7), using class names (IEEEtran, acmart, llncs, elsarticle, apa7):

```javascript
describe('Phase 35: TPL-07 scriveno-{platform}.latex exists with {CLASS} class', () => {
  const TPLFILE = path.join(TEMPLATES_DIR, 'scriveno-{platform}.latex');

  it('scriveno-{platform}.latex exists - TPL-07', () => {
    const content = readFile(TPLFILE);
    assert.ok(content !== null, 'data/export-templates/scriveno-{platform}.latex must exist - TPL-07');
  });

  it('scriveno-{platform}.latex contains \\documentclass{{CLASS}} - TPL-07', () => {
    const content = readFile(TPLFILE);
    assert.ok(content !== null, 'scriveno-{platform}.latex must exist - TPL-07');
    assert.ok(
      content.includes('\\documentclass') && content.includes('{CLASS}'),
      'scriveno-{platform}.latex must contain \\documentclass with {CLASS} - TPL-07'
    );
  });

  it('scriveno-{platform}.latex contains \\providecommand{\\tightlist} - TPL-07', () => {
    const content = readFile(TPLFILE);
    assert.ok(content !== null, 'scriveno-{platform}.latex must exist - TPL-07');
    assert.ok(
      content.includes('\\providecommand{\\tightlist}'),
      'scriveno-{platform}.latex must contain \\providecommand{\\tightlist} - TPL-07'
    );
  });

  it('scriveno-{platform}.latex contains $body$ variable - TPL-07', () => {
    const content = readFile(TPLFILE);
    assert.ok(content !== null, 'scriveno-{platform}.latex must exist - TPL-07');
    assert.ok(
      content.includes('$body$'),
      'scriveno-{platform}.latex must contain $body$ Pandoc variable - TPL-07'
    );
  });

  it('scriveno-{platform}.latex contains $if(abstract)$ block - TPL-07', () => {
    const content = readFile(TPLFILE);
    assert.ok(content !== null, 'scriveno-{platform}.latex must exist - TPL-07');
    assert.ok(
      content.includes('$if(abstract)$'),
      'scriveno-{platform}.latex must contain $if(abstract)$ block - TPL-07'
    );
  });

  it('scriveno-{platform}.latex contains CSL reference environment - TPL-07', () => {
    const content = readFile(TPLFILE);
    assert.ok(content !== null, 'scriveno-{platform}.latex must exist - TPL-07');
    assert.ok(
      content.includes('CSLReferences'),
      'scriveno-{platform}.latex must contain CSLReferences environment - TPL-07'
    );
  });
});
```

**CRITICAL - exact CLASS names for each platform:**
- ieee -> `IEEEtran`
- acm -> `acmart`
- lncs -> `llncs`
- elsevier -> `elsarticle`
- apa7 -> `apa7`

**After the 5 template describe blocks, write one describe block for build-print.md:**

```javascript
// ─────────────────────────────────────────────────────────────────────────────
// TPL-07: build-print.md academic route checks
// ─────────────────────────────────────────────────────────────────────────────

describe('Phase 35: TPL-07 build-print.md contains academic LaTeX route', () => {
  it('build-print.md references kpsewhich - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(content.includes('kpsewhich'), 'build-print.md must reference kpsewhich - TPL-07');
  });

  it('build-print.md contains ieee as valid platform value - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(content.includes('ieee'), 'build-print.md must contain ieee as a platform value - TPL-07');
  });

  it('build-print.md contains acm as valid platform value - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(content.includes('acm'), 'build-print.md must contain acm as a platform value - TPL-07');
  });

  it('build-print.md contains lncs as valid platform value - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(content.includes('lncs'), 'build-print.md must contain lncs as a platform value - TPL-07');
  });

  it('build-print.md contains elsevier as valid platform value - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(content.includes('elsevier'), 'build-print.md must contain elsevier as a platform value - TPL-07');
  });

  it('build-print.md contains apa7 as valid platform value - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(content.includes('apa7'), 'build-print.md must contain apa7 as a platform value - TPL-07');
  });

  it('build-print.md contains paper- output naming pattern - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(content.includes('paper-'), 'build-print.md must contain paper-{platform} output naming - TPL-07');
  });

  it('build-print.md contains tlmgr install guidance - TPL-07', () => {
    const content = readFile(BUILD_PRINT_PATH);
    assert.ok(content !== null, 'commands/scr/build-print.md must be readable - TPL-07');
    assert.ok(
      content.includes('tlmgr install'),
      'build-print.md must contain tlmgr install guidance for missing classes - TPL-07'
    );
  });
});
```

**Write one describe block for CONSTRAINTS.json:**

```javascript
// ─────────────────────────────────────────────────────────────────────────────
// TPL-07: CONSTRAINTS.json exports.build_print.available includes "academic"
// ─────────────────────────────────────────────────────────────────────────────

describe('Phase 35: TPL-07 CONSTRAINTS.json exports.build_print.available includes academic', () => {
  it('data/CONSTRAINTS.json exports.build_print.available includes "academic" - TPL-07', () => {
    const raw = readFile(CONSTRAINTS_PATH);
    assert.ok(raw !== null, 'data/CONSTRAINTS.json must be readable - TPL-07');
    const constraints = JSON.parse(raw);
    assert.ok(
      Array.isArray(constraints.exports.build_print.available) &&
      constraints.exports.build_print.available.includes('academic'),
      'CONSTRAINTS.json exports.build_print.available must include "academic" - TPL-07'
    );
  });
});
```

**Verification: all tests must fail (RED) when this file is first created** - the template files and build-print.md extensions don't exist yet. Confirm with node --test output showing failures.
  </action>

  <verify>
    <automated>node --test test/phase35-academic-latex-wrappers.test.js 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - test/phase35-academic-latex-wrappers.test.js exists and is parseable by Node.js (no syntax errors)
    - `node --test test/phase35-academic-latex-wrappers.test.js` runs without crashing the process (test failures are expected and acceptable at this stage)
    - The output shows tests for all 5 platforms: grep confirms `IEEEtran`, `acmart`, `llncs`, `elsarticle`, `apa7` all appear in the test file
    - The output shows build-print.md tests: `grep -c 'kpsewhich\|tlmgr install\|paper-' test/phase35-academic-latex-wrappers.test.js` returns at least 3 matches
    - The output shows CONSTRAINTS.json test: `grep 'exports.build_print.available' test/phase35-academic-latex-wrappers.test.js` returns at least 1 match
    - `grep -c "TPL-07" test/phase35-academic-latex-wrappers.test.js` returns at least 40 (each assert message ends with " -  TPL-07")
    - `node --test test/phase35-academic-latex-wrappers.test.js 2>&1 | grep -c "not ok"` returns > 0 (tests are failing = RED state, which is correct for Wave 1)
  </acceptance_criteria>

  <done>test/phase35-academic-latex-wrappers.test.js exists, is syntactically valid, covers all TPL-07 behaviors, and all tests are in RED state (failing because implementation files do not yet exist).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file -> filesystem | Test reads local files only; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | test/phase35 | accept | Test file is read-only file system access; no secrets or sensitive data involved |
</threat_model>

<verification>
```bash
# Syntax check
node --check test/phase35-academic-latex-wrappers.test.js

# Run suite (RED state expected)
node --test test/phase35-academic-latex-wrappers.test.js 2>&1 | tail -30

# Confirm all 5 class names appear
grep -E "IEEEtran|acmart|llncs|elsarticle|apa7" test/phase35-academic-latex-wrappers.test.js | wc -l

# Confirm TPL-07 tag on every assertion
grep -c "TPL-07" test/phase35-academic-latex-wrappers.test.js
```
</verification>

<success_criteria>
- test/phase35-academic-latex-wrappers.test.js exists with no syntax errors
- All 5 publisher classes (IEEEtran, acmart, llncs, elsarticle, apa7) have describe blocks covering: file existence, documentclass, tightlist, $body$, $if(abstract)$, CSLReferences
- build-print.md describe block covers: kpsewhich, ieee/acm/lncs/elsevier/apa7 platform values, paper- output naming, tlmgr install guidance
- CONSTRAINTS.json describe block covers: exports.build_print.available includes "academic"
- All tests fail in RED state (expected - implementation is in Wave 2)
- npm test still passes all pre-existing suites
</success_criteria>

<output>
After completion, create `.planning/phases/35-academic-latex-wrappers/35-01-SUMMARY.md`
</output>
