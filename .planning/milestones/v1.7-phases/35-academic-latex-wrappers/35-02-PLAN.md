---
phase: 35-academic-latex-wrappers
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01-PLAN.md
files_modified:
  - data/export-templates/scriveno-ieee.latex
  - data/export-templates/scriveno-acm.latex
  - data/export-templates/scriveno-lncs.latex
  - data/export-templates/scriveno-elsevier.latex
  - data/export-templates/scriveno-apa7.latex
autonomous: true
requirements:
  - TPL-07

must_haves:
  truths:
    - "Five .latex files exist in data/export-templates/ - one per publisher class"
    - "Each wrapper uses \documentclass{<CLASS>} with classoption pass-through, NOT the generic article class"
    - "Each wrapper contains the Pandoc boilerplate: tightlist, $body$, $if(abstract)$, CSL reference environment, highlighting-macros block"
    - "Each wrapper omits geometry, fancyhdr, setspace, lmodern, hyperref, biblatex - the packages that conflict with publisher classes"
    - "scriveno-lncs.latex contains a comment about splncs04 bib style and Springer download page URL"
    - "Running node --test test/phase35-academic-latex-wrappers.test.js turns all 30 template tests GREEN"
  artifacts:
    - path: "data/export-templates/scriveno-ieee.latex"
      provides: "IEEEtran Pandoc wrapper template"
      contains: "\\documentclass"
      min_lines: 60
    - path: "data/export-templates/scriveno-acm.latex"
      provides: "acmart Pandoc wrapper template"
      contains: "\\documentclass"
      min_lines: 60
    - path: "data/export-templates/scriveno-lncs.latex"
      provides: "llncs Pandoc wrapper template"
      contains: "\\documentclass"
      min_lines: 60
    - path: "data/export-templates/scriveno-elsevier.latex"
      provides: "elsarticle Pandoc wrapper template"
      contains: "\\documentclass"
      min_lines: 60
    - path: "data/export-templates/scriveno-apa7.latex"
      provides: "apa7 Pandoc wrapper template"
      contains: "\\documentclass"
      min_lines: 60
  key_links:
    - from: "data/export-templates/scriveno-ieee.latex"
      to: "IEEEtran.cls (user-installed)"
      via: "\\documentclass{IEEEtran}"
      pattern: "\\\\documentclass.*IEEEtran"
    - from: "data/export-templates/scriveno-lncs.latex"
      to: "Springer author download"
      via: "comment in bibliography section"
      pattern: "springer.com.*lncs"
---

<objective>
Create five minimal Pandoc LaTeX template wrapper files - one per academic publisher class (IEEEtran, acmart, llncs, elsarticle, apa7). Each wrapper routes Scriveno's title/author/abstract/keywords metadata through a hardcoded \documentclass{<CLASS>} invocation without shipping the publisher class itself.

Purpose: These are the template files the build-print.md academic route invokes via pandoc --template=. A writer with IEEEtran.cls installed on their TeX Live gets a .tex file they can compile directly.

Output: 5 .latex files in data/export-templates/, each ~60-80 lines, each turning its test suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-academic-latex-wrappers/35-CONTEXT.md
@.planning/phases/35-academic-latex-wrappers/35-RESEARCH.md
@.planning/phases/35-academic-latex-wrappers/35-PATTERNS.md

<interfaces>
<!-- Analog template - read this file to understand the Pandoc variable syntax and which sections to KEEP vs OMIT -->

From data/export-templates/scriveno-academic.latex - FULL FILE IS THE REFERENCE.

KEEP from analog (verbatim copy with adaptation):
1. Header comment block (lines 1-5) - update for publisher
2. Encoding: \usepackage[T1]{fontenc} + \usepackage[utf8]{inputenc} - YES, keep (drop lmodern)
3. Math: \usepackage{amsmath,amssymb} - YES, keep
4. Graphics block (lines 55-60): graphicx + maxwidth/maxheight macros + setkeys - YES, keep
5. $if(highlighting-macros)$ block (lines 86-88) - YES, keep
6. \providecommand{\tightlist} (lines 91-92) - YES, keep
7. CSL references block (lines 95-108) - YES, copy VERBATIM
8. Metadata bridge: \title, \author, \date (lines 127-131) - YES, keep (drop subtitle)
9. \begin{document} / \maketitle / abstract block / keywords block / $body$ / \end{document} - YES, keep (drop TOC, LOF, LOT, appendix)

OMIT from analog (will conflict with publisher classes):
- \usepackage{lmodern} - line 15
- \usepackage[...]{geometry} - line 22
- \usepackage[...]{babel} - lines 25-29
- \usepackage{setspace} + \setstretch - lines 32-37
- \usepackage{fancyhdr} + pagestyle block - lines 40-45
- \setcounter{secnumdepth} - lines 48-52
- \usepackage{longtable,booktabs,array} - line 63
- \usepackage{enumitem} - line 66
- \usepackage[usenames,dvipsnames]{xcolor} - line 69
- \usepackage[...]{hyperref} block - lines 72-80
- \usepackage[backend=biber,...]{biblatex} block - lines 112-123
- \printbibliography - line 175
- TOC/LOF/LOT conditionals - lines 153-167
- \appendix conditional - lines 179-182

\documentclass line in wrapper - NOT the analog's variable form but hardcoded with classoption passthrough:
```latex
\documentclass[$if(classoption)$$for(classoption)$$classoption$$sep$,$endfor$$endif$]{<PUBLISHERCLASS>}
```

Per-class details:
| File              | PUBLISHERCLASS | Comment default options  | Bib style comment |
|-------------------|---------------|--------------------------|-------------------|
| scriveno-ieee.latex    | IEEEtran      | [conference] or [journal]| IEEEtran          |
| scriveno-acm.latex     | acmart        | [acmconf,review=false]   | biblatex-acm      |
| scriveno-lncs.latex    | llncs         | (none standard)          | splncs04          |
| scriveno-elsevier.latex| elsarticle    | [preprint,12pt]          | elsarticle-num    |
| scriveno-apa7.latex    | apa7          | [jou,longtable] or [man] | biblatex-apa      |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scriveno-ieee.latex and scriveno-acm.latex</name>
  <files>data/export-templates/scriveno-ieee.latex, data/export-templates/scriveno-acm.latex</files>

  <read_first>
    - data/export-templates/scriveno-academic.latex - the analog; read every line before writing to understand what to keep and what to omit
    - .planning/phases/35-academic-latex-wrappers/35-PATTERNS.md - sections "What to KEEP" and "What to OMIT" list exact analog line numbers
  </read_first>

  <action>
Create data/export-templates/scriveno-ieee.latex with this exact structure:

```latex
% Scriveno IEEE LaTeX Template for Pandoc --template flag
% Requires IEEEtran.cls (install: tlmgr install ieeetran)
% Usage: pandoc ... --template=data/export-templates/scriveno-ieee.latex
%
% Default class options: [conference] for IEEE conference papers, [journal] for IEEE journal articles.
% Pass via Pandoc: --metadata classoption=conference

\documentclass[$if(classoption)$$for(classoption)$$classoption$$sep$,$endfor$$endif$]{IEEEtran}

% Encoding - safe with IEEEtran when using pdflatex
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% Math support
\usepackage{amsmath,amssymb}

% Graphics
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}

% Pandoc required: code highlighting
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% Pandoc required: tight lists
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Pandoc required: CSL references (citeproc compatibility)
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newenvironment{CSLReferences}[2]%
  {\setlength{\parindent}{0pt}%
   \everypar{\setlength{\hangindent}{\cslhangindent}}\ignorespaces}%
  {\par}
\newcommand{\CSLBlock}[1]{\hfill\break#1\hfill\break}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{\strut#1\strut}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\dimexpr\linewidth-\csllabelwidth\relax}{\strut#1\strut}}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}
$endif$

% Document metadata
\title{$if(title)$$title$$else$Untitled$endif$}
\author{$if(author)$$for(author)$$if(it.name)$$it.name$$else$$it$$endif$$sep$ \and $endfor$$else$~$endif$}
\date{$if(date)$$date$$else$\today$endif$}

\begin{document}

\maketitle

$if(abstract)$
\begin{abstract}
\noindent $abstract$
\end{abstract}
$endif$

$if(keywords)$
\noindent\textbf{Keywords:} $keywords$
$endif$

$body$

% Bibliography: use IEEEtran bib style or BibLaTeX with IEEE style
% \bibliographystyle{IEEEtran}
% \bibliography{references}

\end{document}
```

Create data/export-templates/scriveno-acm.latex with the same structure, replacing:
- Comment header: "ACM" and "acmart.cls" and "tlmgr install acmart"
- Comment default options: "[acmconf,review=false] for ACM conference papers"
- \documentclass line: `{acmart}` instead of `{IEEEtran}`
- Bibliography comment: "% Bibliography: use biblatex-acm or natbib per ACM template guidance"
- Comment note: "% Note: acmart loads hyperref internally - do not add \usepackage{hyperref}"

DO NOT include geometry, fancyhdr, setspace, lmodern, hyperref, biblatex, longtable, enumitem, xcolor, or any other packages beyond what is shown above. Publisher classes handle their own layout.
  </action>

  <verify>
    <automated>node --test test/phase35-academic-latex-wrappers.test.js 2>&1 | grep -E "ok|not ok" | grep -E "ieee|acm" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `test -f data/export-templates/scriveno-ieee.latex` exits 0
    - `test -f data/export-templates/scriveno-acm.latex` exits 0
    - `grep -c "\\\\documentclass" data/export-templates/scriveno-ieee.latex` returns 1
    - `grep "IEEEtran" data/export-templates/scriveno-ieee.latex` matches the \documentclass line
    - `grep "acmart" data/export-templates/scriveno-acm.latex` matches the \documentclass line
    - `grep "\\\\providecommand{\\\\tightlist}" data/export-templates/scriveno-ieee.latex` returns a match
    - `grep "CSLReferences" data/export-templates/scriveno-ieee.latex` returns a match
    - `grep "\\$body\\$" data/export-templates/scriveno-ieee.latex` returns a match
    - `grep "\\$if(abstract)\\$" data/export-templates/scriveno-ieee.latex` returns a match
    - `grep "geometry\|fancyhdr\|setspace\|lmodern\|hyperref\|biblatex" data/export-templates/scriveno-ieee.latex` returns NO match (these packages must be absent)
    - `grep "geometry\|fancyhdr\|setspace\|lmodern\|biblatex" data/export-templates/scriveno-acm.latex` returns NO match
    - node --test for ieee and acm it-blocks shows all "ok" (passing)
  </acceptance_criteria>

  <done>scriveno-ieee.latex and scriveno-acm.latex exist in data/export-templates/, contain correct \documentclass, all required Pandoc boilerplate, and no conflicting packages. Their test suite blocks are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Create scriveno-lncs.latex, scriveno-elsevier.latex, scriveno-apa7.latex</name>
  <files>data/export-templates/scriveno-lncs.latex, data/export-templates/scriveno-elsevier.latex, data/export-templates/scriveno-apa7.latex</files>

  <read_first>
    - data/export-templates/scriveno-ieee.latex - read the file just created in Task 1; use it as the structural template for the remaining three wrappers (same structure, different class names)
    - .planning/phases/35-academic-latex-wrappers/35-PATTERNS.md - section "Per-class specifics" table and "llncs-specific bibliography note" block
    - .planning/phases/35-academic-latex-wrappers/35-RESEARCH.md - section "Common Pitfalls -> Pitfall 5" for the splncs04 bib style requirement
  </read_first>

  <action>
Use the scriveno-ieee.latex structure as the pattern. Create three more wrappers with these class-specific differences:

**scriveno-lncs.latex** - use `{llncs}` as the document class:
- Header comment: "Scriveno Springer LNCS LaTeX Template for Pandoc --template flag"
- Requires comment: "Requires llncs.cls (install: tlmgr install llncs)"
- Comment default options: "(no standard class options for llncs)"
- \documentclass line: `{llncs}`
- Add llncs-specific bibliography note at the end of the file (before \end{document}):
```latex
% Bibliography: use splncs04 bib style (not llncs - that style is deprecated)
% \bibliographystyle{splncs04}
% \bibliography{references}
% Note: llncs.cls may also be obtained from Springer's author resources page
% if tlmgr install llncs is unavailable on your TeX distribution:
% https://www.springer.com/gp/computer-science/lncs/conference-proceedings-guidelines
```

**scriveno-elsevier.latex** - use `{elsarticle}` as the document class:
- Header comment: "Scriveno Elsevier LaTeX Template for Pandoc --template flag"
- Requires comment: "Requires elsarticle.cls (install: tlmgr install elsarticle)"
- Comment default options: "[preprint,12pt] for preprint; [final] for final submission"
- \documentclass line: `{elsarticle}`
- Bibliography comment: "% Bibliography: use elsarticle-num (numeric) or elsarticle-harv (author-year) bib style"

**scriveno-apa7.latex** - use `{apa7}` as the document class:
- Header comment: "Scriveno APA7 LaTeX Template for Pandoc --template flag"
- Requires comment: "Requires apa7.cls (install: tlmgr install apa7)"
- Comment default options: "[jou,longtable] for journal format; [man] for manuscript format"
- \documentclass line: `{apa7}`
- Bibliography comment: "% Bibliography: apa7 requires biblatex-apa; use \printbibliography manually after compiling"
- Note comment: "% Note: apa7.cls requires biblatex - load with \\usepackage[style=apa]{biblatex} in your local preamble additions"

All three files use the IDENTICAL structure as scriveno-ieee.latex for every other section. The CSL references block must be copied VERBATIM from scriveno-ieee.latex.

DO NOT include geometry, fancyhdr, setspace, lmodern, hyperref, biblatex, longtable, enumitem, xcolor.
  </action>

  <verify>
    <automated>node --test test/phase35-academic-latex-wrappers.test.js 2>&1 | grep -c "^ok"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f data/export-templates/scriveno-lncs.latex` exits 0
    - `test -f data/export-templates/scriveno-elsevier.latex` exits 0
    - `test -f data/export-templates/scriveno-apa7.latex` exits 0
    - `grep "llncs" data/export-templates/scriveno-lncs.latex` matches the \documentclass line
    - `grep "elsarticle" data/export-templates/scriveno-elsevier.latex` matches the \documentclass line
    - `grep "apa7" data/export-templates/scriveno-apa7.latex` matches the \documentclass line (the class, not just file reference)
    - `grep "CSLReferences" data/export-templates/scriveno-lncs.latex` returns a match
    - `grep "\\$body\\$" data/export-templates/scriveno-elsevier.latex` returns a match
    - `grep "\\$if(abstract)\\$" data/export-templates/scriveno-apa7.latex` returns a match
    - `grep "splncs04" data/export-templates/scriveno-lncs.latex` returns a match (correct LNCS bib style)
    - `grep "springer.com" data/export-templates/scriveno-lncs.latex` returns a match (Springer download URL in comment)
    - `grep "geometry\|fancyhdr\|setspace\|lmodern\|hyperref\|biblatex" data/export-templates/scriveno-lncs.latex` returns NO match
    - `node --test test/phase35-academic-latex-wrappers.test.js 2>&1 | grep -c "^ok"` returns 30 (all 5 × 6 template tests pass)
  </acceptance_criteria>

  <done>All 5 publisher wrapper templates exist in data/export-templates/. All 30 template-related tests in phase35 test suite are GREEN. scriveno-lncs.latex includes splncs04 comment and Springer download URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pandoc template engine -> LaTeX output | Pandoc substitutes $variable$ tokens; no user-supplied content reaches shell |
| Writer's TeX distribution -> compiled PDF | Writer-controlled; Scriveno produces .tex source only, no subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02 | Tampering | .latex template files | accept | Static text files; no executable code; writer reviews .tex output before compiling |
| T-35-03 | Information Disclosure | $title$/$author$ substitution | accept | Pandoc template substitution is local-only; no network transmission in this plan |
</threat_model>

<verification>
```bash
# All 5 files exist
ls data/export-templates/scriveno-{ieee,acm,lncs,elsevier,apa7}.latex

# Each has correct documentclass
grep "\\documentclass" data/export-templates/scriveno-ieee.latex
grep "\\documentclass" data/export-templates/scriveno-acm.latex
grep "\\documentclass" data/export-templates/scriveno-lncs.latex
grep "\\documentclass" data/export-templates/scriveno-elsevier.latex
grep "\\documentclass" data/export-templates/scriveno-apa7.latex

# No conflicting packages in any wrapper
grep -l "geometry\|fancyhdr\|setspace\|lmodern" data/export-templates/scriveno-{ieee,acm,lncs,elsevier,apa7}.latex

# splncs04 and Springer URL in lncs wrapper
grep "splncs04\|springer.com" data/export-templates/scriveno-lncs.latex

# 30 template tests GREEN
node --test test/phase35-academic-latex-wrappers.test.js 2>&1 | grep -c "^ok"
```
</verification>

<success_criteria>
- All 5 wrapper files exist in data/export-templates/ with correct \documentclass{<CLASS>} using classoption pass-through
- No conflicting packages (geometry, fancyhdr, setspace, lmodern, hyperref, biblatex) in any wrapper
- Every wrapper contains: tightlist, $body$, $if(abstract)$, CSLReferences, highlighting-macros guard
- scriveno-lncs.latex references splncs04 bib style and Springer download URL in comments
- All 30 template-related tests in phase35 suite are GREEN
- npm test passes all suites including the existing phase34 regression
</success_criteria>

<output>
After completion, create `.planning/phases/35-academic-latex-wrappers/35-02-SUMMARY.md`
</output>
