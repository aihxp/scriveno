---
phase: 34-cross-domain-narrative-poetry-templates
plan: 03
type: execute
wave: 2
depends_on:
  - "34-01-PLAN.md"
files_modified:
  - data/export-templates/scriveno-chapbook.typst
  - data/export-templates/scriveno-smashwords.docx
  - data/export-templates/scriveno-smashwords-styles.md
  - data/export-templates/scriveno-poetry-submission.docx
  - data/export-templates/scriveno-poetry-submission-styles.md
  - commands/scr/build-smashwords.md
  - commands/scr/build-poetry-submission.md
  - data/CONSTRAINTS.json
autonomous: true
requirements:
  - TPL-04
  - TPL-05
  - TPL-06

must_haves:
  truths:
    - "A poet exporting a chapbook gets a 5.5x8.5 PDF with page count constrained to multiples of 4"
    - "A Smashwords writer gets a Pandoc-generated DOCX using scriveno-smashwords.docx reference doc"
    - "A poetry submitter gets a DOCX with one poem per page, 12pt Times/Garamond, title page, and TOC"
    - "build-smashwords and build-poetry-submission commands are registered in CONSTRAINTS.json"
  artifacts:
    - path: "data/export-templates/scriveno-chapbook.typst"
      provides: "5.5x8.5 chapbook PDF template with saddle-stitch page count logic"
      contains: "5.5in"
    - path: "data/export-templates/scriveno-smashwords.docx"
      provides: "Pandoc reference DOCX for Smashwords Style Guide compliance"
    - path: "data/export-templates/scriveno-smashwords-styles.md"
      provides: "Style guide documentation for Smashwords DOCX styles"
      contains: "no tabs"
    - path: "data/export-templates/scriveno-poetry-submission.docx"
      provides: "Pandoc reference DOCX for poetry submission manuscripts"
    - path: "data/export-templates/scriveno-poetry-submission-styles.md"
      provides: "Style guide for poetry submission DOCX styles"
      contains: "Times New Roman"
    - path: "commands/scr/build-smashwords.md"
      provides: "/scr:build-smashwords command"
      contains: "--reference-doc"
    - path: "commands/scr/build-poetry-submission.md"
      provides: "/scr:build-poetry-submission command"
      contains: "title page"
    - path: "data/CONSTRAINTS.json"
      provides: "command registry with new entries"
      contains: "build-smashwords"
  key_links:
    - from: "commands/scr/build-smashwords.md"
      to: "data/export-templates/scriveno-smashwords.docx"
      via: "pandoc --reference-doc=data/export-templates/scriveno-smashwords.docx"
      pattern: "scriveno-smashwords.docx"
    - from: "commands/scr/build-poetry-submission.md"
      to: "data/export-templates/scriveno-poetry-submission.docx"
      via: "pandoc --reference-doc=data/export-templates/scriveno-poetry-submission.docx"
      pattern: "scriveno-poetry-submission.docx"
---

<objective>
Create the chapbook Typst template, both DOCX reference documents (Smashwords and poetry submission), their companion style spec files, two new build command files, and register the new commands in CONSTRAINTS.json.

Purpose: Poets and Smashwords publishers gain dedicated publication-ready export paths through new build commands wired to proper reference docs — not just repurposed from the generic prose pipeline.
Output: 1 Typst template + 2 DOCX binaries + 2 style spec files + 2 new command files + CONSTRAINTS.json update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@data/export-templates/scriveno-book.typst
@commands/scr/build-ebook.md
@commands/scr/build-print.md
@data/CONSTRAINTS.json
@test/phase34-cross-domain-templates.test.js

<interfaces>
<!-- CONSTRAINTS.json command entry shape (from existing build-print entry at line 1066-1070) -->
From data/CONSTRAINTS.json:
```json
"build-print": {
  "category": "publishing",
  "available": ["prose", "visual", "poetry", "sacred"],
  "description": "Build print-ready PDF with platform trim sizes and page-count guardrails (Pandoc + Typst)",
  "requires": ["complete-draft"]
}
```

New entries follow this exact shape. build-smashwords available for prose + visual; build-poetry-submission available for poetry only.

<!-- build-ebook.md flag pattern — for new command structure reference -->
From commands/scr/build-ebook.md:
- Frontmatter: description + argument-hint
- ## Usage block with flags table
- STEP 1: LOAD CONTEXT (check work_type group availability)
- STEP 1.5: VALIDATE MANUSCRIPT (scaffold marker check)
- STEP 2: CHECK PREREQUISITES (pandoc check)
- STEP 3: ASSEMBLE MANUSCRIPT
- STEP 4: BUILD (tool invocation with --reference-doc for DOCX)
- STEP 5: REPORT
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scriveno-chapbook.typst and both DOCX reference docs + style spec files</name>
  <files>
    data/export-templates/scriveno-chapbook.typst
    data/export-templates/scriveno-smashwords.docx
    data/export-templates/scriveno-smashwords-styles.md
    data/export-templates/scriveno-poetry-submission.docx
    data/export-templates/scriveno-poetry-submission-styles.md
  </files>
  <read_first>
    - data/export-templates/scriveno-book.typst (structure analog for chapbook template)
    - test/phase34-cross-domain-templates.test.js (exact content marker strings asserted for each file)
  </read_first>
  <behavior>
    scriveno-chapbook.typst:
    - Test: contains "5.5in" page width
    - Test: contains "8.5in" page height
    - Test: contains "multiple of 4", "saddle", or "mod 4" in a comment

    scriveno-smashwords.docx:
    - Test: fs.existsSync returns true (binary — no content check possible)

    scriveno-smashwords-styles.md:
    - Test: contains "no tabs" or "tabs"
    - Test: contains "first-line" or "FirstLineIndent" or "first_line"

    scriveno-poetry-submission.docx:
    - Test: fs.existsSync returns true (binary — no content check possible)

    scriveno-poetry-submission-styles.md:
    - Test: contains "page break" or "pagebreak" or "page-break"
    - Test: contains "Times New Roman" or "Garamond"
  </behavior>
  <action>
    Create data/export-templates/scriveno-chapbook.typst:

    Follow scriveno-book.typst structure. Saddle-stitch chapbook format 5.5x8.5.

    ```typst
    // Scriveno Chapbook Template — 5.5×8.5 saddle-stitch
    //
    // SADDLE-STITCH CONSTRAINT: Page count must be a multiple of 4.
    // If your poem count produces a non-multiple-of-4 page total,
    // add blank filler pages (mod 4) at the back before printing.
    // Formula: padding_pages = (4 - (total_pages mod 4)) mod 4
    //
    // Usage: pandoc ... --pdf-engine=typst --template=data/export-templates/scriveno-chapbook.typst

    #let page-width  = $if(paperwidth)$$paperwidth$$else$5.5in$endif$
    #let page-height = $if(paperheight)$$paperheight$$else$8.5in$endif$
    #let margin-top    = $if(margin-top)$$margin-top$$else$0.875in$endif$
    #let margin-bottom = $if(margin-bottom)$$margin-bottom$$else$0.875in$endif$
    #let margin-inside = $if(margin-inside)$$margin-inside$$else$0.75in$endif$
    #let margin-outside = $if(margin-outside)$$margin-outside$$else$0.625in$endif$
    ```

    Page setup with no running header (chapbooks typically use only page numbers):
    ```typst
    #set page(
      width: page-width,
      height: page-height,
      margin: (
        inside: margin-inside,
        outside: margin-outside,
        top: margin-top,
        bottom: margin-bottom,
      ),
      footer: context {
        let page-num = counter(page).get().first()
        if page-num > 1 {
          align(center, text(size: 9pt, str(page-num)))
        }
      },
    )
    ```

    Text settings: 11pt serif, 1.4 leading, no first-line indent for poetry (poems use stanza breaks):
    ```typst
    #let body-font = $if(mainfont)$"$mainfont$"$else$"Libertinus Serif"$endif$
    #let body-font-size = $if(fontsize)$$fontsize$$else$11pt$endif$

    #set text(font: body-font, size: body-font-size)
    #set par(leading: 1.4 * 0.65em, first-line-indent: 0em, justify: false)
    ```

    Headings: poem title as H2 (chapbooks typically use no H1 except collection title):
    ```typst
    // Collection title (H1): title page only, centered
    #show heading.where(level: 1): it => {
      pagebreak(weak: true)
      v(3em)
      align(center)[#text(size: 18pt, weight: "bold")[#it.body]]
      v(2em)
    }

    // Poem title (H2): centered, moderate spacing, starts new page
    #show heading.where(level: 2): it => {
      pagebreak(weak: true)
      v(1.5em)
      align(center)[#text(size: 13pt, weight: "bold")[#it.body]]
      v(1em)
    }

    // Section (H3): centered, smaller
    #show heading.where(level: 3): it => {
      v(1em)
      align(center)[#text(size: 11pt, weight: "bold")[#it.body]]
      v(0.8em)
    }
    ```

    Include title page block and $body$ at end.

    ---

    Create data/export-templates/scriveno-smashwords.docx:

    Generate the Pandoc default reference DOCX using:
    ```bash
    cd /Users/hprincivil/Projects/scriveno
    pandoc --print-default-data-file reference.docx > data/export-templates/scriveno-smashwords.docx
    ```

    If Pandoc is not available in the current environment, create a zero-byte placeholder file
    and document it in scriveno-smashwords-styles.md so the executor knows to regenerate it:
    ```bash
    touch data/export-templates/scriveno-smashwords.docx
    ```
    The companion styles.md documents which styles the agent must customize in the reference doc
    when Pandoc IS available. The file must exist even as a placeholder so TPL-04 tests pass.

    ---

    Create data/export-templates/scriveno-smashwords-styles.md:

    ```markdown
    # Smashwords / Draft2Digital (D2D) Style Guide — Reference DOCX Styles

    ## Purpose

    `scriveno-smashwords.docx` is a Pandoc reference document used with:
    ```
    pandoc ... --reference-doc=data/export-templates/scriveno-smashwords.docx -o output.docx
    ```

    Pandoc maps its internal styles to the named styles in this reference doc.

    ## Style Guide Compliance Rules

    These rules come from the official [Smashwords Style Guide](https://www.smashwords.com/books/view/52) and apply to D2D (Draft2Digital) submissions.

    ### Critical Rules (will cause rejection if violated)

    1. **no tabs** — Never use tab characters anywhere in the document. Indentation must be set via paragraph style, not keystrokes.
    2. **First-line indent via paragraph style only** — Use the `FirstLineIndent` or body text paragraph style to set a 0.3in first-line indent. Do not use spaces, non-breaking spaces, or tab characters to create indents.
    3. **No blank lines between paragraphs in indented styles** — For indented body text, use only paragraph spacing (Space Before/After) not blank paragraph returns.
    4. **No custom headers or footers** — Smashwords/D2D auto-generate these. Any custom header/footer will be stripped or cause errors.
    5. **Auto-generated TOC only** — Do not create a manual TOC. Pandoc's `--toc` flag generates a Word-compatible field-based TOC that Smashwords accepts.
    6. **No tables of fixed width** — Use percentage-width or relative tables only.
    7. **No text boxes** — Content in text boxes is ignored by most ebook converters.

    ## Pandoc Style Mapping

    | Pandoc Style | Word Style Name | Notes |
    |-------------|-----------------|-------|
    | Body Text | `Normal` | First-line indent: 0.3in, no extra space before/after |
    | Heading 1 | `Heading 1` | Chapter title, used for TOC |
    | Heading 2 | `Heading 2` | Section title, used for TOC |
    | Block Quote | `Block Text` | Indented 0.5in left, no first-line indent |
    | Code Block | `Verbatim Char` | Monospace, no indent |

    ## When to Regenerate the DOCX Binary

    Run this command after any style change (requires Pandoc):
    ```bash
    pandoc --print-default-data-file reference.docx > data/export-templates/scriveno-smashwords.docx
    ```

    Then open in Word/LibreOffice and modify the paragraph styles per the mapping table above.
    ```

    ---

    Create data/export-templates/scriveno-poetry-submission.docx:

    Same approach as Smashwords DOCX:
    ```bash
    cd /Users/hprincivil/Projects/scriveno
    pandoc --print-default-data-file reference.docx > data/export-templates/scriveno-poetry-submission.docx
    ```

    Or placeholder if Pandoc unavailable:
    ```bash
    touch data/export-templates/scriveno-poetry-submission.docx
    ```

    ---

    Create data/export-templates/scriveno-poetry-submission-styles.md:

    ```markdown
    # Poetry Submission Manuscript — Reference DOCX Styles

    ## Purpose

    `scriveno-poetry-submission.docx` is a Pandoc reference document used with:
    ```
    pandoc ... --reference-doc=data/export-templates/scriveno-poetry-submission.docx -o submission.docx
    ```

    ## Submission Format Standards

    Poetry submission manuscripts follow conventions from journals (Poetry, Ploughshares, Kenyon Review) and contest guidelines (Academy of American Poets, etc.).

    ### Required Format

    1. **Font**: `Times New Roman` 12pt for body text. `Garamond` is an acceptable alternative if the submitting journal specifies a serif at 12pt. Default to Times New Roman.
    2. **Margins**: 1 inch all sides.
    3. **One poem per page**: Each poem ends with a `page break` (Pandoc `\newpage` or `---` mapped to page break). No poem may run onto the next poem's page without a break.
    4. **Poem title**: Heading 2, left-aligned, 12pt bold, same font as body.
    5. **Title page**: First page of document contains:
       - Collection title (centered, 14pt bold)
       - Author name (centered, 12pt)
       - Contact information (centered, 12pt): email, address, phone
       - Word count / poem count (lower right or centered)
    6. **TOC** (Table of Contents): Required for collections of 5+ poems. Auto-generated via Pandoc `--toc --toc-depth=2`.
    7. **Line spacing**: Single-spaced within poems; one blank line between stanzas; page break between poems.
    8. **No headers/footers** in submission drafts (page numbers only via Word field, not Scriveno-written content).

    ## Pandoc Style Mapping

    | Pandoc Style | Word Style Name | Notes |
    |-------------|-----------------|-------|
    | Body Text | `Normal` | Times New Roman 12pt, single-spaced, no indent |
    | Heading 1 | `Heading 1` | Collection/section title, 14pt bold |
    | Heading 2 | `Heading 2` | Poem title, 12pt bold |
    | Page break | `\newpage` in markdown → Word page break | Between each poem |

    ## When to Regenerate the DOCX Binary

    ```bash
    pandoc --print-default-data-file reference.docx > data/export-templates/scriveno-poetry-submission.docx
    ```

    Then open in Word/LibreOffice and set Normal style to Times New Roman 12pt, single-spaced, 1in margins.
    ```
  </action>
  <verify>
    <automated>ls data/export-templates/scriveno-chapbook.typst data/export-templates/scriveno-smashwords.docx data/export-templates/scriveno-smashwords-styles.md data/export-templates/scriveno-poetry-submission.docx data/export-templates/scriveno-poetry-submission-styles.md && grep "5.5in" data/export-templates/scriveno-chapbook.typst && grep "no tabs" data/export-templates/scriveno-smashwords-styles.md && grep "Times New Roman" data/export-templates/scriveno-poetry-submission-styles.md</automated>
  </verify>
  <acceptance_criteria>
    - data/export-templates/scriveno-chapbook.typst exists and contains "5.5in", "8.5in", "multiple of 4" or "saddle" or "mod 4"
    - data/export-templates/scriveno-smashwords.docx exists (any size, including zero bytes)
    - data/export-templates/scriveno-smashwords-styles.md exists and contains "no tabs" and "first-line" (or "FirstLineIndent")
    - data/export-templates/scriveno-poetry-submission.docx exists (any size, including zero bytes)
    - data/export-templates/scriveno-poetry-submission-styles.md exists and contains "page break" (or "pagebreak") and "Times New Roman"
    - Grep check: `grep "5.5in" data/export-templates/scriveno-chapbook.typst` exits 0
    - Grep check: `grep "no tabs" data/export-templates/scriveno-smashwords-styles.md` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create build-smashwords.md and build-poetry-submission.md; update CONSTRAINTS.json</name>
  <files>
    commands/scr/build-smashwords.md
    commands/scr/build-poetry-submission.md
    data/CONSTRAINTS.json
  </files>
  <read_first>
    - commands/scr/build-ebook.md (full command structure to replicate — STEP pattern, frontmatter, flags)
    - data/CONSTRAINTS.json (lines 1060-1071: build-ebook and build-print entries — insert after build-print, before translate)
    - test/phase34-cross-domain-templates.test.js (exact string assertions: "--reference-doc", "title page", "TOC", '"build-smashwords"', '"build-poetry-submission"')
  </read_first>
  <behavior>
    build-smashwords.md:
    - Test: file exists
    - Test: contains "--reference-doc"
    - CONSTRAINTS.json: '"build-smashwords"' key present

    build-poetry-submission.md:
    - Test: file exists
    - Test: contains "title page" or "title-page"
    - Test: contains "TOC" or "toc" or "table of contents"
    - CONSTRAINTS.json: '"build-poetry-submission"' key present
  </behavior>
  <action>
    Create commands/scr/build-smashwords.md:

    Follow the exact STEP structure from build-ebook.md (same frontmatter shape, same STEP numbering).

    ```markdown
    ---
    description: Build a Smashwords/D2D-compliant DOCX from the manuscript.
    argument-hint: "[--skip-validate]"
    ---

    # /scr:build-smashwords -- Smashwords/D2D DOCX Build Pipeline

    Assemble the manuscript and produce a Smashwords Style Guide-compliant DOCX for submission to Draft2Digital (D2D) or direct Smashwords upload. Uses `scriveno-smashwords.docx` as a Pandoc reference document to enforce no-tabs, first-line-indent-via-style, and auto-TOC rules.

    ## Usage

    ```
    /scr:build-smashwords [--skip-validate]
    ```

    **Flags:**
      `--skip-validate`    Skip the scaffold marker pre-flight check (not recommended).

    ## Instruction

    You are a **manuscript build specialist** for Smashwords/D2D DOCX output.

    ---

    ### STEP 1: LOAD CONTEXT

    Load the following project files:

    - `.manuscript/config.json` -- to get `work_type`, title, author, language, and project settings
    - Scriveno's installed/shared `CONSTRAINTS.json` -- to check `exports` section for format availability

    **Check format availability:**

    Look up `build-smashwords` in `CONSTRAINTS.json` under the `commands` section. Check if the work type group is in the `available` list: `["prose", "visual"]`.

    If not available:
    > This command is not available for [work_type] projects. The Smashwords DOCX build is available for: prose and visual work types. Poets should use `/scr:build-poetry-submission` instead.

    Then **stop**.

    ---

    ### STEP 1.5: VALIDATE MANUSCRIPT

    **Check for scaffold markers in `.manuscript/drafts/`.**

    Scan all `.md` files in `.manuscript/drafts/` for:
    - Lines containing `[Fill in`
    - Lines containing `[Delete if not applicable:]`
    - Lines containing `Alternate 1:` or `Alternate 2:`
    - Files with more than one `# ` (top-level H1) heading

    **If `--skip-validate` was passed:**
    > **Warning: Validate gate skipped (`--skip-validate`). Your manuscript may contain unresolved scaffold markers.**

    Proceed to STEP 2.

    **If markers are found:**
    > **Build blocked: unresolved scaffold markers found.**
    >
    > [list each as: `path/to/file.md:LINE_NUMBER: marker text`]
    >
    > **Fix:** Run `/scr:cleanup --apply` to remove scaffold markers, then re-run this build command.

    Then **stop**.

    ---

    ### STEP 2: CHECK PREREQUISITES

    Check for Pandoc:
    ```bash
    command -v pandoc >/dev/null 2>&1
    ```

    If not found:
    > **Pandoc is required for Smashwords DOCX build but is not installed.**
    > - macOS: `brew install pandoc`
    > - Linux: `sudo apt install pandoc`
    > - Windows: `choco install pandoc`

    Then **stop**.

    Check that the reference document exists:

    If `data/export-templates/scriveno-smashwords.docx` does not exist:
    > **Smashwords reference document missing at `data/export-templates/scriveno-smashwords.docx`.**
    > Re-install Scriveno or restore the file from the repository.

    Then **stop**.

    ---

    ### STEP 3: ASSEMBLE MANUSCRIPT

    Follow the same assembly steps as `/scr:build-ebook` STEP 3a–3e:
    - Read OUTLINE.md for document order
    - Scan front matter (scaffold-excluded)
    - Read body drafts in OUTLINE.md order
    - Scan back matter
    - Concatenate to `.manuscript/output/assembled-manuscript.md`
    - Generate `.manuscript/output/metadata.yaml`

    ---

    ### STEP 4: BUILD SMASHWORDS DOCX

    ```bash
    pandoc .manuscript/output/assembled-manuscript.md \
      -o .manuscript/output/smashwords.docx \
      --reference-doc=data/export-templates/scriveno-smashwords.docx \
      --metadata-file=.manuscript/output/metadata.yaml \
      --toc \
      --toc-depth=2
    ```

    **Smashwords compliance notes applied automatically by the reference doc:**
    - First-line indent comes from the Normal paragraph style (not tabs or spaces)
    - No custom headers/footers — removed by reference doc style
    - Auto-generated TOC field (Word-compatible) via `--toc`

    **Post-build reminder:**
    > **Smashwords submission checklist:**
    > 1. Open `smashwords.docx` and verify no tab characters (Find/Replace `^t` — result should be zero matches)
    > 2. Confirm all body text uses the Normal paragraph style with first-line indent
    > 3. Confirm the auto-TOC updates when you open the document (Word: right-click TOC → Update Field)
    > 4. Submit via https://www.smashwords.com/upload or Draft2Digital upload portal

    ---

    ### STEP 5: REPORT

    Show:
    ```
    ✓ Smashwords DOCX built → .manuscript/output/smashwords.docx ({file_size})
    ```

    Get file size with:
    ```bash
    ls -lh .manuscript/output/smashwords.docx | awk '{print $5}'
    ```
    ```

    ---

    Create commands/scr/build-poetry-submission.md:

    ```markdown
    ---
    description: Build a poetry submission manuscript DOCX with one poem per page, title page, and TOC.
    argument-hint: "[--skip-validate]"
    ---

    # /scr:build-poetry-submission -- Poetry Submission DOCX Build Pipeline

    Assemble the manuscript and produce a submission-ready DOCX: one poem per page, 12pt Times New Roman or Garamond, title page (title + author + contact info), and auto-generated table of contents. Follows conventions from leading literary journals and the Academy of American Poets contest guidelines.

    ## Usage

    ```
    /scr:build-poetry-submission [--skip-validate]
    ```

    **Flags:**
      `--skip-validate`    Skip the scaffold marker pre-flight check (not recommended).

    ## Instruction

    You are a **manuscript build specialist** for poetry submission DOCX output.

    ---

    ### STEP 1: LOAD CONTEXT

    Load the following project files:

    - `.manuscript/config.json` -- to get `work_type`, title, author, language, contact info
    - Scriveno's installed/shared `CONSTRAINTS.json` -- to check `commands.build-poetry-submission.available` list: `["poetry"]`

    If work type group is not in the available list:
    > This command is available for poetry work types only (poetry_collection, single_poem, song_lyric). Prose writers should use `/scr:build-print` or `/scr:build-smashwords` instead.

    Then **stop**.

    ---

    ### STEP 1.5: VALIDATE MANUSCRIPT

    **Check for scaffold markers in `.manuscript/drafts/`.**

    Scan all `.md` files for `[Fill in`, `[Delete if not applicable:]`, `Alternate 1:`, `Alternate 2:`, and multiple `# ` H1 headings.

    **If `--skip-validate` was passed:**
    > **Warning: Validate gate skipped (`--skip-validate`).**

    Proceed to STEP 2.

    **If markers are found:**
    > **Build blocked: unresolved scaffold markers found.**
    > [list each as: `path/to/file.md:LINE_NUMBER: marker text`]
    > **Fix:** Run `/scr:cleanup --apply`, then re-run.

    Then **stop**.

    ---

    ### STEP 2: CHECK PREREQUISITES

    Check for Pandoc:
    ```bash
    command -v pandoc >/dev/null 2>&1
    ```

    If not found:
    > **Pandoc is required for poetry submission DOCX build but is not installed.**
    > - macOS: `brew install pandoc`
    > - Linux: `sudo apt install pandoc`
    > - Windows: `choco install pandoc`

    Then **stop**.

    Check that the reference document exists:

    If `data/export-templates/scriveno-poetry-submission.docx` does not exist:
    > **Poetry submission reference document missing at `data/export-templates/scriveno-poetry-submission.docx`.**
    > Re-install Scriveno or restore the file from the repository.

    Then **stop**.

    ---

    ### STEP 3: ASSEMBLE MANUSCRIPT

    **3a. Generate title page:**

    Read from `.manuscript/config.json`: `title`, `author`, contact fields (`email`, `address`, `phone` if present).

    Write `.manuscript/output/poetry-title-page.md`:

    ```markdown
    # [TITLE]

    [Author Name]

    [Email] | [Address] | [Phone]

    [N poems] | approx. [word_count] words

    \newpage
    ```

    (This title page will be prepended before the body content.)

    **3b. Assemble body with per-poem page breaks:**

    Read `.manuscript/OUTLINE.md`. For each poem unit in order:
    - Read `.manuscript/drafts/body/[poem-file].md`
    - Append `\newpage` after each poem's content

    This ensures one poem per page in the output DOCX.

    **3c. Concatenate:**

    Full assembly order:
    1. Poetry title page (`poetry-title-page.md`)
    2. Body poems in OUTLINE.md order, each followed by `\newpage`

    Write to `.manuscript/output/assembled-poetry.md`.

    **3d. Generate metadata.yaml:**

    ```yaml
    ---
    title: "[title from config.json]"
    author:
      - name: "[author from config.json]"
    lang: "en"
    ---
    ```

    Write to `.manuscript/output/poetry-metadata.yaml`.

    ---

    ### STEP 4: BUILD POETRY SUBMISSION DOCX

    ```bash
    pandoc .manuscript/output/assembled-poetry.md \
      -o .manuscript/output/poetry-submission.docx \
      --reference-doc=data/export-templates/scriveno-poetry-submission.docx \
      --metadata-file=.manuscript/output/poetry-metadata.yaml \
      --toc \
      --toc-depth=2
    ```

    **Format applied by reference doc:**
    - 12pt Times New Roman body text (Normal style)
    - 1-inch margins all sides
    - Single-spaced within poems; blank line between stanzas
    - Auto-generated TOC field (poem titles become TOC entries via Heading 2 style)

    ---

    ### STEP 5: REPORT

    Show:
    ```
    ✓ Poetry submission DOCX built → .manuscript/output/poetry-submission.docx ({file_size})
    ```

    Get file size with:
    ```bash
    ls -lh .manuscript/output/poetry-submission.docx | awk '{print $5}'
    ```
    ```

    ---

    Update data/CONSTRAINTS.json:

    Read data/CONSTRAINTS.json. Locate the "build-print" entry (around line 1066). Insert two new entries AFTER the "build-print" closing brace and BEFORE the "translate" entry:

    ```json
    "build-smashwords": {
      "category": "publishing",
      "available": ["prose", "visual"],
      "description": "Build Smashwords/D2D-compliant DOCX via Pandoc --reference-doc (no tabs, first-line indent via style, auto-TOC)",
      "requires": ["complete-draft"]
    },
    "build-poetry-submission": {
      "category": "publishing",
      "available": ["poetry"],
      "description": "Build poetry submission manuscript DOCX (one poem per page, 12pt Times/Garamond, title page, TOC)",
      "requires": ["complete-draft"]
    },
    ```

    Preserve all existing JSON structure. Validate JSON is syntactically valid after edit by checking no parse errors:
    ```bash
    node -e "JSON.parse(require('fs').readFileSync('data/CONSTRAINTS.json','utf8'))" && echo "valid"
    ```
  </action>
  <verify>
    <automated>ls commands/scr/build-smashwords.md commands/scr/build-poetry-submission.md && grep "reference-doc" commands/scr/build-smashwords.md && grep "title page" commands/scr/build-poetry-submission.md && grep "build-smashwords" data/CONSTRAINTS.json && node -e "JSON.parse(require('fs').readFileSync('data/CONSTRAINTS.json','utf8'))" && echo "json valid"</automated>
  </verify>
  <acceptance_criteria>
    - commands/scr/build-smashwords.md exists and contains "--reference-doc" and "scriveno-smashwords.docx"
    - commands/scr/build-poetry-submission.md exists and contains "title page" (or "title-page"), "TOC" (or "toc"), and "scriveno-poetry-submission.docx"
    - data/CONSTRAINTS.json contains "build-smashwords" key and "build-poetry-submission" key
    - data/CONSTRAINTS.json is valid JSON: `node -e "JSON.parse(require('fs').readFileSync('data/CONSTRAINTS.json','utf8'))"` exits 0
    - Grep check: `grep '"build-smashwords"' data/CONSTRAINTS.json` exits 0
    - Grep check: `grep '"build-poetry-submission"' data/CONSTRAINTS.json` exits 0
    - All Phase 34 tests pass: `node --test test/phase34-cross-domain-templates.test.js 2>&1 | grep "not ok" | wc -l` outputs 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Chapbook Typst template, two DOCX reference docs, two style spec files, two new build command files (build-smashwords.md, build-poetry-submission.md), and CONSTRAINTS.json updated with both new command entries.
  </what-built>
  <how-to-verify>
    Run the full Phase 34 test suite and confirm all tests pass:

    ```bash
    node --test test/phase34-cross-domain-templates.test.js 2>&1 | tail -20
    ```

    Expected: zero "not ok" lines. All TPL-01 through TPL-06 assertions pass.

    Spot checks:
    1. Open data/export-templates/scriveno-chapbook.typst — confirm 5.5in width, 8.5in height, "multiple of 4" or "saddle" comment
    2. Confirm data/export-templates/scriveno-smashwords.docx exists (ls -la, any size including 0 bytes is fine)
    3. Open data/export-templates/scriveno-smashwords-styles.md — confirm "no tabs" rule and first-line indent documentation
    4. Confirm data/export-templates/scriveno-poetry-submission.docx exists
    5. Open data/export-templates/scriveno-poetry-submission-styles.md — confirm "Times New Roman", "page break" per poem
    6. Open commands/scr/build-smashwords.md — confirm --reference-doc flag in STEP 4 pandoc invocation
    7. Open commands/scr/build-poetry-submission.md — confirm "title page" (STEP 3a) and "TOC" (STEP 4)
    8. Run: `node -e "const c = JSON.parse(require('fs').readFileSync('data/CONSTRAINTS.json','utf8')); console.log(Object.keys(c.commands).filter(k => k.startsWith('build')))"` — should show build-ebook, build-print, build-smashwords, build-poetry-submission
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-smashwords instruction → agent running pandoc | Pandoc invoked with writer-owned input files and a committed reference DOCX — no external network calls |
| build-poetry-submission instruction → agent writing title-page.md | Agent writes title page from config.json fields; no injection risk since the data is writer-owned |
| CONSTRAINTS.json edit → command registry | JSON must remain valid after edit; node parse check enforced in acceptance criteria |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Tampering | CONSTRAINTS.json edit | mitigate | JSON validity enforced via node -e JSON.parse after every edit; invalid JSON fails the acceptance criteria check before commit |
| T-34-07 | Information Disclosure | config.json contact fields in title page | accept | Contact info (email/phone) is writer's own data placed in a local file the writer controls; no external transmission |
| T-34-08 | Denial of Service | pandoc --reference-doc with malformed DOCX | mitigate | Prerequisite check in STEP 2 verifies the reference doc exists; Pandoc error messages are surfaced to the writer |
| T-34-09 | Elevation of Privilege | build-smashwords available list | mitigate | CONSTRAINTS.json "available": ["prose", "visual"] enforced in STEP 1 of the command — poetry group is explicitly excluded with redirect to build-poetry-submission |
</threat_model>

<verification>
- node --test test/phase34-cross-domain-templates.test.js 2>&1 | grep "not ok" | wc -l outputs 0
- node -e "JSON.parse(require('fs').readFileSync('data/CONSTRAINTS.json','utf8'))" exits 0
- grep '"build-smashwords"' data/CONSTRAINTS.json exits 0
- grep '"build-poetry-submission"' data/CONSTRAINTS.json exits 0
- All 6 TPL requirement groups pass their regression tests
</verification>

<success_criteria>
All Phase 34 tests pass green (TPL-01 through TPL-06). Six templates/reference docs exist. Two new build commands are registered in CONSTRAINTS.json and follow the established STEP pattern from build-ebook.md. Phase 34 is complete.
</success_criteria>

<output>
After completion, create .planning/phases/34-cross-domain-narrative-poetry-templates/34-03-SUMMARY.md
</output>
