---
phase: 34-cross-domain-narrative-poetry-templates
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/phase34-cross-domain-templates.test.js
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - TPL-05
  - TPL-06

must_haves:
  truths:
    - "test/phase34-cross-domain-templates.test.js exists and runs with node:test"
    - "All 6 template groups have failing tests that describe the exact files and content markers required"
    - "All tests fail (RED) because the implementation files do not exist yet"
    - "Tests are static file checks only - no Pandoc or Typst execution required"
  artifacts:
    - path: "test/phase34-cross-domain-templates.test.js"
      provides: "Phase 34 regression test suite covering TPL-01..TPL-06"
      min_lines: 120
  key_links:
    - from: "test/phase34-cross-domain-templates.test.js"
      to: "data/export-templates/scriveno-stageplay.typst"
      via: "fs.readFileSync content check"
      pattern: "scriveno-stageplay"
    - from: "test/phase34-cross-domain-templates.test.js"
      to: "commands/scr/build-print.md"
      via: "positional index check for STEP 1.8"
      pattern: "STEP 1.8"
---

<objective>
Create the Phase 34 regression test suite in TDD RED state. All tests describe the exact files, content markers, and structural positions required by TPL-01 through TPL-06. Every test MUST fail before the implementation plans run.

Purpose: Lock the acceptance criteria for all 6 cross-domain templates as machine-executable contracts before writing a single template file.
Output: test/phase34-cross-domain-templates.test.js - a node:test suite that fails on every assert until Plans 34-02 and 34-03 deliver the implementation.
</objective>

<execution_context>
@$HOME/.claude/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@test/phase33-sacred-tradition-profiles.test.js
@commands/scr/build-print.md
@commands/scr/build-ebook.md
@data/CONSTRAINTS.json
</context>

<feature>
  <name>Phase 34 Regression Test Suite (TDD RED)</name>
  <files>test/phase34-cross-domain-templates.test.js</files>
  <behavior>
    TPL-01 Stage Play:
    - data/export-templates/scriveno-stageplay.typst exists
    - scriveno-stageplay.typst contains "8.5in" wide and "11in" tall page dimensions (US Letter for play format)
    - scriveno-stageplay.typst contains character name ALL CAPS centering pattern (upper() or text-transform or "CHARACTER NAME" aligned center)
    - scriveno-stageplay.typst contains italic stage direction pattern (emph or style: "italic" for parens content)
    - scriveno-stageplay.typst contains heading level 1 for acts and level 2 for scenes
    - build-print.md STEP 1.8 exists and appears after STEP 1.7 and before ### STEP 2:

    TPL-02 Picture Book:
    - data/export-templates/scriveno-picturebook.typst exists
    - scriveno-picturebook.typst contains "8.75in" (bleed width: 8.5 + 0.125 + 0.125)
    - scriveno-picturebook.typst contains "8.75in" (bleed height: same square format)
    - scriveno-picturebook.typst contains "0.125in" bleed value
    - scriveno-picturebook.typst contains "0.25in" safe zone / margin value
    - scriveno-picturebook.typst contains spread or 2-page layout logic

    TPL-03 Fixed-Layout EPUB:
    - data/export-templates/scriveno-fixed-layout-epub.css exists
    - data/export-templates/scriveno-fixed-layout.opf exists
    - scriveno-fixed-layout-epub.css contains "fixed-layout" or "rendition:layout" or "-epub-layout"
    - scriveno-fixed-layout.opf contains "rendition:layout" property (OPF metadata for fixed-layout)
    - scriveno-fixed-layout.opf contains "rendition:spread"
    - build-ebook.md contains "--fixed-layout" flag reference

    TPL-04 Smashwords:
    - data/export-templates/scriveno-smashwords.docx exists (binary, existsSync only)
    - data/export-templates/scriveno-smashwords-styles.md exists
    - scriveno-smashwords-styles.md contains "no tabs" or "tabs" prohibition
    - scriveno-smashwords-styles.md contains "first-line indent" or "FirstLineIndent" reference
    - commands/scr/build-smashwords.md exists
    - build-smashwords.md contains "--reference-doc" flag reference
    - data/CONSTRAINTS.json contains "build-smashwords" key

    TPL-05 Chapbook:
    - data/export-templates/scriveno-chapbook.typst exists
    - scriveno-chapbook.typst contains "5.5in" page width
    - scriveno-chapbook.typst contains "8.5in" page height
    - scriveno-chapbook.typst contains "multiple of 4" or "saddle" or "mod 4" page count logic comment

    TPL-06 Poetry Submission:
    - data/export-templates/scriveno-poetry-submission.docx exists (binary, existsSync only)
    - data/export-templates/scriveno-poetry-submission-styles.md exists
    - scriveno-poetry-submission-styles.md contains "page break" or "pagebreak" (1 poem per page)
    - scriveno-poetry-submission-styles.md contains "Times New Roman" or "Garamond"
    - commands/scr/build-poetry-submission.md exists
    - build-poetry-submission.md contains "title page" or "title-page" reference
    - build-poetry-submission.md contains "TOC" or "toc" or "table of contents" reference
    - data/CONSTRAINTS.json contains "build-poetry-submission" key
  </behavior>
  <implementation>
    Write test/phase34-cross-domain-templates.test.js following the exact structure of test/phase33-sacred-tradition-profiles.test.js:
    - Use const { describe, it } = require('node:test') and const assert = require('node:assert/strict')
    - Use const fs = require('fs') and const path = require('path')
    - Define ROOT as path.join(__dirname, '..')
    - Use the readFile(filePath) helper that returns null on error
    - Group tests by requirement: TPL-01, TPL-02, TPL-03, TPL-04, TPL-05, TPL-06
    - Each describe block title: 'Phase 34: TPL-0N description'
    - Each it() title ends with ' - TPL-0N' for traceability
    - existsSync checks use assert.ok(content !== null, ...) via readFile
    - Binary DOCX files: use fs.existsSync directly (can't readFileSync a binary as utf8 for content)
    - Positional checks in build-print.md: indexOf('STEP 1.7'), indexOf('STEP 1.8'), indexOf('### STEP 2:') - assert s17 < s18 < s2
    - All tests MUST fail on first run because implementation files do not exist yet
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Write Phase 34 failing test suite (RED)</name>
  <files>test/phase34-cross-domain-templates.test.js</files>
  <read_first>
    - test/phase33-sacred-tradition-profiles.test.js (structure analog - copy patterns exactly)
    - commands/scr/build-print.md (confirm STEP 1.7 and ### STEP 2: text for positional assertions)
    - commands/scr/build-ebook.md (confirm --skip-validate flag pattern for --fixed-layout assertion)
    - data/CONSTRAINTS.json (confirm "build-ebook" and "build-print" key shapes for new entries)
  </read_first>
  <action>
    Create test/phase34-cross-domain-templates.test.js with node:test + assert/strict. No new npm dependencies.

    File header constants:
    ```js
    const ROOT              = path.join(__dirname, '..');
    const TEMPLATES_DIR     = path.join(ROOT, 'data', 'export-templates');
    const COMMANDS_DIR      = path.join(ROOT, 'commands', 'scr');
    const BUILD_PRINT_PATH  = path.join(ROOT, 'commands', 'scr', 'build-print.md');
    const BUILD_EBOOK_PATH  = path.join(ROOT, 'commands', 'scr', 'build-ebook.md');
    const CONSTRAINTS_PATH  = path.join(ROOT, 'data', 'CONSTRAINTS.json');
    ```

    Include the readFile helper from Phase 33 test (try/catch, returns null).

    Write 6 describe blocks, one per TPL requirement. Each describe block contains it() assertions exactly matching the behavior spec above. All assertions target files that do not yet exist, so all tests must fail in RED state.

    --- TPL-01 block (stage play) ---
    describe('Phase 34: TPL-01 scriveno-stageplay.typst exists with Samuel French format markers', ...) {
      const STAGEPLAY = path.join(TEMPLATES_DIR, 'scriveno-stageplay.typst');
      it('scriveno-stageplay.typst exists - TPL-01', ...)  // readFile != null
      it('scriveno-stageplay.typst contains page width 8.5in - TPL-01', ...)  // includes('8.5in')
      it('scriveno-stageplay.typst contains character name centering pattern - TPL-01', ...)  // includes('upper(') || includes('align(center')
      it('scriveno-stageplay.typst contains italic stage direction pattern - TPL-01', ...)  // includes('emph') || includes('style: "italic"')
      it('scriveno-stageplay.typst contains act heading at level 1 - TPL-01', ...)  // includes('heading.where(level: 1)')
      it('scriveno-stageplay.typst contains scene heading at level 2 - TPL-01', ...)  // includes('heading.where(level: 2)')
      it('build-print.md contains STEP 1.8 - TPL-01', ...)  // BUILD_PRINT_PATH readFile includes('STEP 1.8')
      it('build-print.md STEP 1.8 appears after STEP 1.7 and before STEP 2 - TPL-01', ...)  // positional indexOf checks
    }

    --- TPL-02 block (picture book) ---
    describe('Phase 34: TPL-02 scriveno-picturebook.typst exists with bleed and safe zone markers', ...) {
      const PICTUREBOOK = path.join(TEMPLATES_DIR, 'scriveno-picturebook.typst');
      it('scriveno-picturebook.typst exists - TPL-02', ...)
      it('scriveno-picturebook.typst contains bleed width 8.75in - TPL-02', ...)  // includes('8.75in')
      it('scriveno-picturebook.typst contains bleed value 0.125in - TPL-02', ...)  // includes('0.125in')
      it('scriveno-picturebook.typst contains safe zone 0.25in - TPL-02', ...)  // includes('0.25in')
      it('scriveno-picturebook.typst contains spread/two-page layout marker - TPL-02', ...)  // includes('spread') || includes('two-page') || includes('facing')
    }

    --- TPL-03 block (fixed-layout EPUB) ---
    describe('Phase 34: TPL-03 fixed-layout EPUB CSS and OPF stub exist with Apple Books compatibility markers', ...) {
      const FL_CSS = path.join(TEMPLATES_DIR, 'scriveno-fixed-layout-epub.css');
      const FL_OPF = path.join(TEMPLATES_DIR, 'scriveno-fixed-layout.opf');
      it('scriveno-fixed-layout-epub.css exists - TPL-03', ...)
      it('scriveno-fixed-layout.opf exists - TPL-03', ...)
      it('scriveno-fixed-layout-epub.css contains epub-layout fixed marker - TPL-03', ...)  // includes('-epub-layout') || includes('rendition:layout')
      it('scriveno-fixed-layout.opf contains rendition:layout property - TPL-03', ...)  // includes('rendition:layout')
      it('scriveno-fixed-layout.opf contains rendition:spread property - TPL-03', ...)  // includes('rendition:spread')
      it('build-ebook.md contains --fixed-layout flag reference - TPL-03', ...)  // BUILD_EBOOK_PATH readFile includes('--fixed-layout')
    }

    --- TPL-04 block (Smashwords) ---
    describe('Phase 34: TPL-04 Smashwords DOCX reference doc and build command exist', ...) {
      const SW_DOCX = path.join(TEMPLATES_DIR, 'scriveno-smashwords.docx');
      const SW_STYLES = path.join(TEMPLATES_DIR, 'scriveno-smashwords-styles.md');
      const SW_CMD = path.join(COMMANDS_DIR, 'build-smashwords.md');
      it('scriveno-smashwords.docx exists - TPL-04', ...)  // fs.existsSync(SW_DOCX)
      it('scriveno-smashwords-styles.md exists - TPL-04', ...)
      it('scriveno-smashwords-styles.md prohibits tabs - TPL-04', ...)  // includes('no tabs') || includes('tabs')
      it('scriveno-smashwords-styles.md specifies first-line indent - TPL-04', ...)  // includes('first-line') || includes('FirstLineIndent') || includes('first_line')
      it('commands/scr/build-smashwords.md exists - TPL-04', ...)
      it('build-smashwords.md references --reference-doc flag - TPL-04', ...)  // includes('--reference-doc')
      it('data/CONSTRAINTS.json contains build-smashwords command entry - TPL-04', ...)  // CONSTRAINTS readFile includes('"build-smashwords"')
    }

    --- TPL-05 block (chapbook) ---
    describe('Phase 34: TPL-05 scriveno-chapbook.typst exists with 5.5x8.5 saddle-stitch markers', ...) {
      const CHAPBOOK = path.join(TEMPLATES_DIR, 'scriveno-chapbook.typst');
      it('scriveno-chapbook.typst exists - TPL-05', ...)
      it('scriveno-chapbook.typst contains page width 5.5in - TPL-05', ...)  // includes('5.5in')
      it('scriveno-chapbook.typst contains page height 8.5in - TPL-05', ...)  // includes('8.5in')
      it('scriveno-chapbook.typst contains page-count multiple-of-4 comment - TPL-05', ...)  // includes('multiple of 4') || includes('saddle') || includes('mod 4')
    }

    --- TPL-06 block (poetry submission) ---
    describe('Phase 34: TPL-06 poetry submission DOCX reference doc and build command exist', ...) {
      const PS_DOCX = path.join(TEMPLATES_DIR, 'scriveno-poetry-submission.docx');
      const PS_STYLES = path.join(TEMPLATES_DIR, 'scriveno-poetry-submission-styles.md');
      const PS_CMD = path.join(COMMANDS_DIR, 'build-poetry-submission.md');
      it('scriveno-poetry-submission.docx exists - TPL-06', ...)  // fs.existsSync
      it('scriveno-poetry-submission-styles.md exists - TPL-06', ...)
      it('scriveno-poetry-submission-styles.md specifies page break per poem - TPL-06', ...)  // includes('page break') || includes('pagebreak') || includes('page-break')
      it('scriveno-poetry-submission-styles.md specifies Times New Roman or Garamond - TPL-06', ...)  // includes('Times New Roman') || includes('Garamond')
      it('commands/scr/build-poetry-submission.md exists - TPL-06', ...)
      it('build-poetry-submission.md references title page - TPL-06', ...)  // includes('title page') || includes('title-page')
      it('build-poetry-submission.md references TOC - TPL-06', ...)  // includes('TOC') || includes('toc') || includes('table of contents')
      it('data/CONSTRAINTS.json contains build-poetry-submission command entry - TPL-06', ...)  // includes('"build-poetry-submission"')
    }

    After writing, confirm the test file runs with node:test runner and all tests fail:
    ```bash
    node --test test/phase34-cross-domain-templates.test.js 2>&1 | tail -20
    ```
    Expected: all assertions fail with "must exist" or content-not-found errors. No crashes or syntax errors.
  </action>
  <verify>
    <automated>node --test test/phase34-cross-domain-templates.test.js 2>&1 | grep -E "^(not ok|x|fail)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - test/phase34-cross-domain-templates.test.js exists at project root test/ directory
    - File uses require('node:test') and require('node:assert/strict') - no new npm packages
    - Six describe blocks present: TPL-01 through TPL-06
    - Running `node --test test/phase34-cross-domain-templates.test.js` produces failures (not a syntax error crash)
    - All it() assertions fail because the template and command files do not yet exist
    - Grep check: `grep -c "TPL-0[1-6]" test/phase34-cross-domain-templates.test.js` returns >= 30 (at least 5 per requirement)
    - Grep check: `grep "describe(" test/phase34-cross-domain-templates.test.js | wc -l` returns 6
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> filesystem | Tests read production template files; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | test file paths | accept | All paths are relative to project ROOT; no secrets or credentials involved |
| T-34-02 | Tampering | readFile helper | accept | Read-only helper; no writes in test code |
</threat_model>

<verification>
- node --test test/phase34-cross-domain-templates.test.js runs without syntax errors
- All tests fail in RED state (no template files exist yet)
- grep -c "TPL-0[1-6]" test/phase34-cross-domain-templates.test.js >= 30
</verification>

<success_criteria>
test/phase34-cross-domain-templates.test.js exists, runs, and fails on every assertion. The failure messages are descriptive (e.g. "scriveno-stageplay.typst must exist - TPL-01"). No test passes. Phase is ready for Wave 2 implementation.
</success_criteria>

<output>
After completion, create .planning/phases/34-cross-domain-narrative-poetry-templates/34-01-SUMMARY.md
</output>
