---
phase: 29-architectural-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/sacred/catholic/manifest.yaml
  - templates/sacred/orthodox/manifest.yaml
  - templates/sacred/tewahedo/manifest.yaml
  - templates/sacred/protestant/manifest.yaml
  - templates/sacred/jewish/manifest.yaml
  - templates/sacred/islamic-hafs/manifest.yaml
  - templates/sacred/islamic-warsh/manifest.yaml
  - templates/sacred/pali/manifest.yaml
  - templates/sacred/tibetan/manifest.yaml
  - templates/sacred/sanskrit/manifest.yaml
  - templates/sacred/README.md
  - templates/platforms/kdp/manifest.yaml
  - templates/platforms/ingram/manifest.yaml
  - templates/platforms/d2d/manifest.yaml
  - templates/platforms/apple/manifest.yaml
  - templates/platforms/kobo/manifest.yaml
  - templates/platforms/google/manifest.yaml
  - templates/platforms/bn/manifest.yaml
  - templates/platforms/smashwords/manifest.yaml
  - templates/platforms/README.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "templates/sacred/<tradition>/manifest.yaml exists for all 10 seeded traditions"
    - "templates/platforms/<platform>/manifest.yaml exists for all 8 seeded platforms"
    - "Each manifest declares the full schema shape downstream phases will populate"
    - "A contributor dropping templates/sacred/<new-tradition>/manifest.yaml into the repo needs no core edits"
    - "A contributor dropping templates/platforms/<new-platform>/manifest.yaml into the repo needs no core edits"
  artifacts:
    - path: "templates/sacred/catholic/manifest.yaml"
      provides: "Placeholder Catholic tradition manifest with schema keys"
      contains: "tradition: catholic"
    - path: "templates/sacred/islamic-hafs/manifest.yaml"
      provides: "Placeholder Hafs Quran tradition manifest"
      contains: "tradition: islamic-hafs"
    - path: "templates/platforms/kdp/manifest.yaml"
      provides: "Placeholder KDP platform manifest with schema keys"
      contains: "platform: kdp"
    - path: "templates/platforms/ingram/manifest.yaml"
      provides: "Placeholder IngramSpark platform manifest"
      contains: "platform: ingram"
    - path: "templates/sacred/README.md"
      provides: "Drop-in contribution guide for new traditions"
    - path: "templates/platforms/README.md"
      provides: "Drop-in contribution guide for new platforms"
  key_links:
    - from: "templates/sacred/*/manifest.yaml"
      to: "runtime validator (Plan 03)"
      via: "directory listing at read-time"
      pattern: "templates/sacred/[a-z-]+/manifest\\.yaml"
    - from: "templates/platforms/*/manifest.yaml"
      to: "runtime validator (Plan 03)"
      via: "directory listing at read-time"
      pattern: "templates/platforms/[a-z-]+/manifest\\.yaml"
---

<objective>
Create the `templates/sacred/` and `templates/platforms/` directory scaffolds with one subdirectory per seeded profile, each containing a placeholder `manifest.yaml` that declares the full schema keys downstream phases (32, 33) will populate. Ship README files at each root explaining the drop-in contribution convention.

Purpose: Satisfies ARCH-01 and ARCH-02 - the architectural extension points must exist before any phase can validate them, consume them, or populate them with real content. The schema keys in each manifest are declared (as nulls or placeholders) so the validator in Plan 03 can be written against a stable shape. Real content lands in Phases 32 and 33.

Output: 18 placeholder manifests + 2 README files. Zero code. Zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-architectural-foundation/29-CONTEXT.md
@.planning/tmp/cross-domain-platforms-research.md
@CLAUDE.md

<interfaces>
<!-- Schema that each manifest declares. These are the keys downstream phases will populate. -->
<!-- For sacred manifests (templates/sacred/<tradition>/manifest.yaml): -->
```yaml
# templates/sacred/<tradition>/manifest.yaml
tradition: <tradition-slug>  # e.g. catholic, orthodox, tewahedo, etc.
label: <Human-readable name>  # e.g. "Roman Catholic", "Ethiopian Orthodox Tewahedo"
book_order: null           # populated Phase 33 - list of canonical book names in traditional order
approval_block: null       # populated Phase 33 - YAML schema (imprimatur, patriarchate_blessing, mushaf_cert, etc.) or null if no approval
font_stack: null           # populated Phase 33 - list of font names in fallback order
rtl: null                  # populated Phase 33 - boolean, true for Hebrew/Arabic
numbering: null            # populated Phase 33 - identifier for verse numbering macro (e.g. "masoretic", "quranic_hafs")
script: null               # populated Phase 33 - script/unicode range (e.g. "latin", "greek-polytonic", "ethiopic", "arabic", "hebrew", "devanagari", "tibetan")
status: placeholder        # this plan ships placeholders; Phase 33 flips to "active"
```

<!-- For platform manifests (templates/platforms/<platform>/manifest.yaml): -->
```yaml
# templates/platforms/<platform>/manifest.yaml
platform: <platform-slug>  # e.g. kdp, ingram, d2d, apple, kobo, google, bn, smashwords
label: <Human-readable name>  # e.g. "Amazon KDP", "IngramSpark"
trim_sizes: null           # populated Phase 32 - list of supported trim sizes e.g. ["5x8", "5.5x8.5", "6x9"]
max_pages: null            # populated Phase 32 - integer or object per binding (paperback/hardcover)
epub_variant: null         # populated Phase 32 - "reflowable", "fixed-layout", or list
metadata_shape: null       # populated Phase 32 - object describing required metadata fields
formats_accepted: null     # populated Phase 32 - list e.g. ["epub", "pdf", "docx"]
status: placeholder        # this plan ships placeholders; Phase 32 flips to "active"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Seed 10 sacred tradition placeholder manifests + README</name>
  <files>
    templates/sacred/catholic/manifest.yaml,
    templates/sacred/orthodox/manifest.yaml,
    templates/sacred/tewahedo/manifest.yaml,
    templates/sacred/protestant/manifest.yaml,
    templates/sacred/jewish/manifest.yaml,
    templates/sacred/islamic-hafs/manifest.yaml,
    templates/sacred/islamic-warsh/manifest.yaml,
    templates/sacred/pali/manifest.yaml,
    templates/sacred/tibetan/manifest.yaml,
    templates/sacred/sanskrit/manifest.yaml,
    templates/sacred/README.md
  </files>
  <read_first>
    - templates/sacred/ (directory listing - note existing files COSMOLOGY.md, DOCTRINES.md, FIGURES.md, FRAMEWORK.md, LINEAGES.md, THEOLOGICAL-ARC.md MUST be preserved; they are project-scaffold templates and must coexist with the new tradition subdirectories)
    - .planning/tmp/cross-domain-platforms-research.md (§2 "Sacred Text Traditions Beyond Roman Catholic" - authoritative for tradition labels and scripts)
    - templates/config.json (existing template reference for style)
    - CLAUDE.md (confirm zero-dependency + markdown-first constraint)
  </read_first>
  <action>
Step 1 - Preserve existing sacred/ contents. The directory `templates/sacred/` currently holds 6 project-scaffold markdown files (COSMOLOGY.md, DOCTRINES.md, FIGURES.md, FRAMEWORK.md, LINEAGES.md, THEOLOGICAL-ARC.md). DO NOT delete, move, or modify any of them. You are ADDING subdirectories alongside these files.

Step 2 - Create 10 tradition subdirectories with a `manifest.yaml` each. Use mkdir as needed; write each manifest verbatim (substitute the tradition slug and label per row). Status MUST be `placeholder` in every file.

Manifest template (substitute `<slug>`, `<label>`):
```yaml
# Sacred tradition profile - placeholder manifest.
# Real content (book_order, approval_block, font_stack, rtl, numbering, script)
# lands in Phase 33 (Sacred Tradition Profiles).
# DO NOT remove this file - Phase 29's validator (Plan 03) reads the
# directory listing to populate the allowed tradition values.
tradition: <slug>
label: "<label>"
book_order: null
approval_block: null
font_stack: null
rtl: null
numbering: null
script: null
status: placeholder
```

The 10 tradition files to write (slug -> label):
- `templates/sacred/catholic/manifest.yaml` -> slug `catholic`, label `Roman Catholic`
- `templates/sacred/orthodox/manifest.yaml` -> slug `orthodox`, label `Eastern Orthodox`
- `templates/sacred/tewahedo/manifest.yaml` -> slug `tewahedo`, label `Ethiopian Orthodox Tewahedo`
- `templates/sacred/protestant/manifest.yaml` -> slug `protestant`, label `Protestant`
- `templates/sacred/jewish/manifest.yaml` -> slug `jewish`, label `Jewish (Tanakh)`
- `templates/sacred/islamic-hafs/manifest.yaml` -> slug `islamic-hafs`, label `Islamic (Hafs ʿan ʿĀsim)`
- `templates/sacred/islamic-warsh/manifest.yaml` -> slug `islamic-warsh`, label `Islamic (Warsh ʿan Nāfiʿ)`
- `templates/sacred/pali/manifest.yaml` -> slug `pali`, label `Buddhist (Pali Canon)`
- `templates/sacred/tibetan/manifest.yaml` -> slug `tibetan`, label `Buddhist (Tibetan Canon)`
- `templates/sacred/sanskrit/manifest.yaml` -> slug `sanskrit`, label `Hindu (Sanskrit / Devanagari)`

Step 3 - Write `templates/sacred/README.md` with this exact content:

```markdown
# Sacred Tradition Profiles

This directory is the drop-in extension point for sacred tradition profiles. Each subdirectory ships as a self-contained profile; Scriveno's runtime reads the directory listing at load time - no core-template edits are required to add a tradition.

## Adding a new tradition

1. Create `templates/sacred/<your-tradition-slug>/`.
2. Inside, add a `manifest.yaml` declaring: `tradition`, `label`, `book_order`, `approval_block`, `font_stack`, `rtl`, `numbering`, `script`, `status`. See any of the shipped manifests for the shape.
3. Set `status: active` once the manifest is populated. Profiles with `status: placeholder` are recognized by the validator but signal "schema present, content pending."
4. Optional: add sibling files alongside `manifest.yaml` (CSS, Typst snippets, font-stack fragments) as downstream phases define conventions.

## Currently seeded traditions (placeholders)

catholic, orthodox, tewahedo, protestant, jewish, islamic-hafs, islamic-warsh, pali, tibetan, sanskrit.

All 10 ship with `status: placeholder` in v1.7 Phase 29. Phase 33 populates their real content (book order, approval blocks, fonts, RTL flags, numbering macros).

## Co-located legacy files

The files `COSMOLOGY.md`, `DOCTRINES.md`, `FIGURES.md`, `FRAMEWORK.md`, `LINEAGES.md`, `THEOLOGICAL-ARC.md` in this directory are project-scaffold templates copied into new sacred-work-type projects (see `/scr:new-work`). They are unrelated to tradition profiles and are preserved as-is.
```
  </action>
  <verify>
    <automated>test -f templates/sacred/catholic/manifest.yaml && test -f templates/sacred/orthodox/manifest.yaml && test -f templates/sacred/tewahedo/manifest.yaml && test -f templates/sacred/protestant/manifest.yaml && test -f templates/sacred/jewish/manifest.yaml && test -f templates/sacred/islamic-hafs/manifest.yaml && test -f templates/sacred/islamic-warsh/manifest.yaml && test -f templates/sacred/pali/manifest.yaml && test -f templates/sacred/tibetan/manifest.yaml && test -f templates/sacred/sanskrit/manifest.yaml && test -f templates/sacred/README.md && test -f templates/sacred/COSMOLOGY.md && test -f templates/sacred/DOCTRINES.md && grep -l "tradition: catholic" templates/sacred/catholic/manifest.yaml && grep -l "tradition: islamic-hafs" templates/sacred/islamic-hafs/manifest.yaml && grep -c "status: placeholder" templates/sacred/*/manifest.yaml | awk -F: '{if($2!=1) exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - 10 files match path `templates/sacred/*/manifest.yaml` (one per slug above)
    - Each manifest contains the literal line `status: placeholder`
    - Each manifest contains the literal line `tradition: <its-slug>`
    - Each manifest contains the literal line `label: "<its-label>"`
    - All six pre-existing files in templates/sacred/ (COSMOLOGY.md, DOCTRINES.md, FIGURES.md, FRAMEWORK.md, LINEAGES.md, THEOLOGICAL-ARC.md) still exist
    - `templates/sacred/README.md` exists and contains the phrase "drop-in extension point"
    - `grep "drop-in extension point" templates/sacred/README.md` returns a match
  </acceptance_criteria>
  <done>10 sacred manifests + README committed, existing sacred scaffold files untouched, all manifests declare status: placeholder and the full schema shape (tradition, label, book_order, approval_block, font_stack, rtl, numbering, script).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed 8 platform placeholder manifests + README</name>
  <files>
    templates/platforms/kdp/manifest.yaml,
    templates/platforms/ingram/manifest.yaml,
    templates/platforms/d2d/manifest.yaml,
    templates/platforms/apple/manifest.yaml,
    templates/platforms/kobo/manifest.yaml,
    templates/platforms/google/manifest.yaml,
    templates/platforms/bn/manifest.yaml,
    templates/platforms/smashwords/manifest.yaml,
    templates/platforms/README.md
  </files>
  <read_first>
    - templates/ (confirm `platforms/` does not yet exist - this task creates it)
    - .planning/tmp/cross-domain-platforms-research.md (§1 "Book Publishing Platforms Beyond KDP/IngramSpark" - authoritative for platform labels and formats)
    - templates/sacred/README.md (if written by Task 1 - match style for consistency; if this task runs before Task 1, match the tradition manifest pattern from the interfaces block in the plan context)
    - CLAUDE.md
  </read_first>
  <action>
Step 1 - Create the 8 platform subdirectories with a `manifest.yaml` each.

Manifest template (substitute `<slug>`, `<label>`):
```yaml
# Publishing platform profile - placeholder manifest.
# Real content (trim_sizes, max_pages, epub_variant, metadata_shape, formats_accepted)
# lands in Phase 32 (Build Pipelines & Platform Awareness).
# DO NOT remove this file - Phase 29's validator (Plan 03) reads the
# directory listing to populate the allowed platform values.
platform: <slug>
label: "<label>"
trim_sizes: null
max_pages: null
epub_variant: null
metadata_shape: null
formats_accepted: null
status: placeholder
```

The 8 platform files to write (slug -> label):
- `templates/platforms/kdp/manifest.yaml` -> slug `kdp`, label `Amazon KDP`
- `templates/platforms/ingram/manifest.yaml` -> slug `ingram`, label `IngramSpark`
- `templates/platforms/d2d/manifest.yaml` -> slug `d2d`, label `Draft2Digital`
- `templates/platforms/apple/manifest.yaml` -> slug `apple`, label `Apple Books`
- `templates/platforms/kobo/manifest.yaml` -> slug `kobo`, label `Kobo Writing Life`
- `templates/platforms/google/manifest.yaml` -> slug `google`, label `Google Play Books`
- `templates/platforms/bn/manifest.yaml` -> slug `bn`, label `Barnes & Noble Press`
- `templates/platforms/smashwords/manifest.yaml` -> slug `smashwords`, label `Smashwords`

Step 2 - Write `templates/platforms/README.md` with this exact content:

```markdown
# Publishing Platform Profiles

This directory is the drop-in extension point for per-platform publishing configs. Each subdirectory ships as a self-contained profile; Scriveno's runtime reads the directory listing at load time - no core-template edits are required to add a platform.

## Adding a new platform

1. Create `templates/platforms/<your-platform-slug>/`.
2. Inside, add a `manifest.yaml` declaring: `platform`, `label`, `trim_sizes`, `max_pages`, `epub_variant`, `metadata_shape`, `formats_accepted`, `status`. See any of the shipped manifests for the shape.
3. Set `status: active` once the manifest is populated. Profiles with `status: placeholder` are recognized by the validator but signal "schema present, content pending."
4. Optional: add sibling files alongside `manifest.yaml` (per-platform CSS, metadata YAML, Typst snippets) as Phase 32 defines conventions.

## Currently seeded platforms (placeholders)

kdp, ingram, d2d, apple, kobo, google, bn, smashwords.

All 8 ship with `status: placeholder` in v1.7 Phase 29. Phase 32 populates their real content (trim sizes, page limits, metadata shapes, accepted formats).
```
  </action>
  <verify>
    <automated>test -f templates/platforms/kdp/manifest.yaml && test -f templates/platforms/ingram/manifest.yaml && test -f templates/platforms/d2d/manifest.yaml && test -f templates/platforms/apple/manifest.yaml && test -f templates/platforms/kobo/manifest.yaml && test -f templates/platforms/google/manifest.yaml && test -f templates/platforms/bn/manifest.yaml && test -f templates/platforms/smashwords/manifest.yaml && test -f templates/platforms/README.md && grep -l "platform: kdp" templates/platforms/kdp/manifest.yaml && grep -l "platform: smashwords" templates/platforms/smashwords/manifest.yaml && [ "$(ls templates/platforms/*/manifest.yaml | wc -l | tr -d ' ')" = "8" ] && grep "drop-in extension point" templates/platforms/README.md</automated>
  </verify>
  <acceptance_criteria>
    - 8 files match path `templates/platforms/*/manifest.yaml` (exactly - no more, no fewer)
    - Each manifest contains the literal line `status: placeholder`
    - Each manifest contains the literal line `platform: <its-slug>`
    - Each manifest contains the literal line `label: "<its-label>"`
    - `templates/platforms/README.md` exists and contains the phrase "drop-in extension point"
    - Directory listing `ls templates/platforms/` shows exactly: apple, bn, d2d, google, ingram, kdp, kobo, README.md, smashwords (alphabetical order after sort)
  </acceptance_criteria>
  <done>8 platform manifests + README committed, all declare status: placeholder and the full schema shape (platform, label, trim_sizes, max_pages, epub_variant, metadata_shape, formats_accepted).</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Directory shape correct
[ "$(ls templates/sacred/*/manifest.yaml | wc -l | tr -d ' ')" = "10" ]
[ "$(ls templates/platforms/*/manifest.yaml | wc -l | tr -d ' ')" = "8" ]

# READMEs shipped
test -f templates/sacred/README.md
test -f templates/platforms/README.md

# Legacy sacred scaffold preserved
test -f templates/sacred/COSMOLOGY.md
test -f templates/sacred/FRAMEWORK.md

# All placeholders consistently marked
grep -l "status: placeholder" templates/sacred/*/manifest.yaml | wc -l   # 10
grep -l "status: placeholder" templates/platforms/*/manifest.yaml | wc -l # 8

# No manifest is empty
find templates/sacred templates/platforms -name manifest.yaml -empty   # (empty output - no empty files)
```
</verification>

<success_criteria>
- 10 sacred/*/manifest.yaml files exist with full schema and status: placeholder
- 8 platforms/*/manifest.yaml files exist with full schema and status: placeholder
- Two README.md files document the drop-in contribution convention
- Existing sacred scaffold markdown files (COSMOLOGY.md etc.) untouched
- Zero new runtime dependencies added
- Zero changes to package.json or installer
- A contributor could drop a new `templates/sacred/zoroastrian/manifest.yaml` or `templates/platforms/lulu/manifest.yaml` and no code would need editing (Plan 03 will confirm the validator picks it up)
</success_criteria>

<output>
After completion, create `.planning/phases/29-architectural-foundation/29-01-SUMMARY.md` documenting:
- Exact list of files created (18 manifests + 2 READMEs = 20 files)
- Schema shape for sacred manifests (tradition, label, book_order, approval_block, font_stack, rtl, numbering, script, status)
- Schema shape for platform manifests (platform, label, trim_sizes, max_pages, epub_variant, metadata_shape, formats_accepted, status)
- Note that Phase 33 populates sacred content, Phase 32 populates platform content
- Any deviations from the plan
</output>
</content>
</invoke>