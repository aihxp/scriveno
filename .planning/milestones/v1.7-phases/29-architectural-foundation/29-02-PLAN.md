---
phase: 29-architectural-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/WORK.md
  - data/CONSTRAINTS.json
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "A writer can set `tradition:` in a sacred-work WORK.md and the key is template-documented"
    - "A writer can set `platform:` in a book-work WORK.md and the key is template-documented"
    - "Work types that have no tradition or platform concept (technical, screenplay, poetry, etc.) do not get these keys in their WORK.md"
    - "data/CONSTRAINTS.json declares the authoritative list of valid tradition values (10 entries)"
    - "data/CONSTRAINTS.json declares the authoritative list of valid platform values (8 entries)"
    - "data/CONSTRAINTS.json declares the work-type -> default-tradition inference map"
    - "data/CONSTRAINTS.json declares the work-type -> default-platform inference map"
  artifacts:
    - path: "templates/WORK.md"
      provides: "Template with conditional tradition/platform block the new-work command fills for applicable groups"
      contains: "{{TRADITION_OR_PLATFORM_BLOCK}}"
    - path: "data/CONSTRAINTS.json"
      provides: "Authoritative tradition/platform schema + inference maps"
      contains: "architectural_profiles"
  key_links:
    - from: "data/CONSTRAINTS.json architectural_profiles.traditions"
      to: "Plan 03 runtime validator"
      via: "JSON read + cross-check against templates/sacred/ directory listing"
      pattern: "architectural_profiles\\.traditions"
    - from: "data/CONSTRAINTS.json architectural_profiles.platforms"
      to: "Plan 03 runtime validator"
      via: "JSON read + cross-check against templates/platforms/ directory listing"
      pattern: "architectural_profiles\\.platforms"
    - from: "data/CONSTRAINTS.json architectural_profiles.defaults_by_work_type"
      to: "Plan 03 default-inference function"
      via: "JSON read at runtime"
      pattern: "defaults_by_work_type"
---

<objective>
Add `tradition:` and `platform:` project-spec keys to the `templates/WORK.md` template (rendered into projects only where the work type's group warrants them), and declare the authoritative schema + inference maps in `data/CONSTRAINTS.json`.

Purpose: Satisfies ARCH-03 - the project spec must accept and declare the two new keys, and `CONSTRAINTS.json` must be the single source of truth for valid values and per-work-type defaults. Plan 03's validator reads both sources.

Output: One updated template, one updated JSON. Zero code. Zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-architectural-foundation/29-CONTEXT.md
@templates/WORK.md
@data/CONSTRAINTS.json
@CLAUDE.md

<interfaces>
<!-- Current templates/WORK.md is plain markdown with {{PLACEHOLDER}} tokens, no YAML frontmatter. -->
<!-- Shape the WORK.md template ships: keep ALL existing {{PLACEHOLDER}} tokens in place. -->
<!-- Add ONE new block near top that renders conditionally based on work-type group. -->

<!-- Current work_type_groups in CONSTRAINTS.json (for determining which groups get what key): -->
<!-- prose, script, academic, technical, visual, poetry, interactive, speech_song, sacred -->

<!-- Group applicability for the two new keys: -->
<!-- tradition: - applies ONLY to `sacred` group -->
<!-- platform: - applies to `prose`, `visual`, `poetry`, `sacred` (groups that produce book-shaped output that's platform-published) -->
<!-- Neither applies to: script, academic, technical, interactive, speech_song (they have their own channels: festivals, journals, repos, storefronts, stages) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add conditional tradition/platform block to templates/WORK.md</name>
  <files>templates/WORK.md</files>
  <read_first>
    - templates/WORK.md (current full content - you must preserve all existing lines)
    - commands/scr/new-work.md (confirm how WORK.md placeholders are substituted and how sacred/technical groups get extra sections today - the new block follows the same conditional-rendering contract)
  </read_first>
  <action>
Update `templates/WORK.md` by inserting a new section titled "## Profile" immediately AFTER the existing `## Work type` section (which ends around line 14 with `- **Audience:** {{AUDIENCE}}`) and BEFORE `## Elevator pitch`.

The new section reads EXACTLY:

```markdown
## Profile

<!--
  The `tradition` key applies only to sacred work types.
  The `platform` key applies to work types that publish as books (prose, visual, poetry, sacred).
  For work types where neither applies (script, academic, technical, interactive, speech_song),
  `/scr:new-work` renders this section as "Not applicable for this work type." and omits the keys.
  Valid values and per-work-type defaults live in `data/CONSTRAINTS.json` under `architectural_profiles`.
-->

{{PROFILE_BLOCK}}
```

Do not change any other part of the file. All existing `{{PLACEHOLDER}}` tokens (`{{WORK_TITLE}}`, `{{PREMISE}}`, `{{WORK_TYPE}}`, `{{GROUP}}`, `{{COMMAND_UNIT}}`, `{{TARGET_LENGTH}}`, `{{GENRE}}`, `{{AUDIENCE}}`, `{{ELEVATOR_PITCH}}`, `{{LOGLINE}}`, `{{COMPARABLES}}`, `{{CENTRAL_QUESTION}}`, `{{CORE_CONFLICT}}`, `{{TONE}}`, `{{STRUCTURE_NOTES}}`, `{{DIFFERENTIATORS}}`) MUST remain byte-identical. The closing note paragraph `*This file is the north star. ...*` MUST remain unchanged.

`{{PROFILE_BLOCK}}` is a new placeholder the `/scr:new-work` flow will substitute with one of four possible blocks at project-creation time - but wiring `/scr:new-work` to actually render the block is DEFERRED to a later phase (the context doc's "Out of scope" line 25: "Changing existing `/scr:new-work` flow beyond adding the two keys"). For Phase 29, it is sufficient that the template declares the placeholder. The four rendered variants (for reference, documented as comment in CONSTRAINTS.json under `architectural_profiles._render_variants`):

- Sacred group -> `- **Tradition:** {{TRADITION}}` + `- **Platform:** {{PLATFORM}}`
- Prose/visual/poetry groups -> `- **Platform:** {{PLATFORM}}` only
- Script/academic/technical/interactive/speech_song groups -> `Not applicable for this work type.`

After the edit, run `git diff templates/WORK.md` mentally - the diff should add exactly 11 new lines (the blank line, `## Profile`, blank line, 6-line comment, blank line, `{{PROFILE_BLOCK}}`, blank line).
  </action>
  <verify>
    <automated>grep -c "## Profile" templates/WORK.md | grep -q "^1$" && grep -q "{{PROFILE_BLOCK}}" templates/WORK.md && grep -q "{{WORK_TITLE}}" templates/WORK.md && grep -q "{{PREMISE}}" templates/WORK.md && grep -q "{{WORK_TYPE}}" templates/WORK.md && grep -q "{{ELEVATOR_PITCH}}" templates/WORK.md && grep -q "{{DIFFERENTIATORS}}" templates/WORK.md && grep -q "north star" templates/WORK.md && awk '/## Work type/{wt=NR} /## Profile/{pf=NR} /## Elevator pitch/{ep=NR} END{exit !(wt>0 && pf>wt && ep>pf)}' templates/WORK.md</automated>
  </verify>
  <acceptance_criteria>
    - `templates/WORK.md` contains exactly one `## Profile` section
    - The `## Profile` section appears AFTER `## Work type` and BEFORE `## Elevator pitch` (verified by line-order awk)
    - The placeholder `{{PROFILE_BLOCK}}` is present
    - All 16 pre-existing `{{...}}` placeholders are still present
    - The closing note "This file is the north star. ..." is still present
    - The file's last line is unchanged from before the edit
  </acceptance_criteria>
  <done>templates/WORK.md has a new ## Profile section with {{PROFILE_BLOCK}} placeholder, all existing placeholders and the closing note preserved, section positioned between Work type and Elevator pitch.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Declare architectural_profiles schema + inference maps in data/CONSTRAINTS.json</name>
  <files>data/CONSTRAINTS.json</files>
  <read_first>
    - data/CONSTRAINTS.json (entire file - you must preserve every existing key byte-identically; only ADD new keys)
    - data/constraints.schema.json (if it exists - CONSTRAINTS.json's $schema reference; keep it passing)
    - package.json (confirm "version" - CONSTRAINTS.json.version is tested against package.json.version by test/constraints.test.js; you should NOT bump version in this task, Plan 04's test changes do not require it)
    - .planning/phases/29-architectural-foundation/29-CONTEXT.md (specifics block - authoritative for the inference map entries)
  </read_first>
  <action>
Add a new top-level key `architectural_profiles` to `data/CONSTRAINTS.json`, positioned immediately after the existing `sacred_config_schema` block and before `messages`. Do NOT change, remove, or reorder any existing keys. Do NOT bump the `version` field.

The new block MUST match this exact shape (substitute the arrays only if you want to fix typos - otherwise verbatim):

```json
"architectural_profiles": {
  "_description": "Phase 29 v1.7: valid values + work-type defaults for the project-spec tradition and platform keys. The runtime validator cross-checks these values against the templates/sacred/ and templates/platforms/ directory listings - the intersection is the accepted set, so new contributor-dropped manifests are picked up at load time without editing this file.",
  "_render_variants": {
    "_note": "How templates/WORK.md {{PROFILE_BLOCK}} is rendered per work-type group by /scr:new-work. Wiring of new-work substitution lands in a later phase.",
    "sacred": "- **Tradition:** {{TRADITION}}\n- **Platform:** {{PLATFORM}}",
    "prose": "- **Platform:** {{PLATFORM}}",
    "visual": "- **Platform:** {{PLATFORM}}",
    "poetry": "- **Platform:** {{PLATFORM}}",
    "script": "Not applicable for this work type.",
    "academic": "Not applicable for this work type.",
    "technical": "Not applicable for this work type.",
    "interactive": "Not applicable for this work type.",
    "speech_song": "Not applicable for this work type."
  },
  "traditions": {
    "_seeded": ["catholic", "orthodox", "tewahedo", "protestant", "jewish", "islamic-hafs", "islamic-warsh", "pali", "tibetan", "sanskrit"],
    "_note": "This array seeds the initial set. The authoritative runtime value list is the intersection of _seeded with directory names under templates/sacred/ that contain a manifest.yaml. A contributor dropping templates/sacred/<new-tradition>/manifest.yaml extends the accepted set without editing this file."
  },
  "platforms": {
    "_seeded": ["kdp", "ingram", "d2d", "apple", "kobo", "google", "bn", "smashwords"],
    "_note": "This array seeds the initial set. The authoritative runtime value list is the intersection of _seeded with directory names under templates/platforms/ that contain a manifest.yaml. A contributor dropping templates/platforms/<new-platform>/manifest.yaml extends the accepted set without editing this file."
  },
  "applies_to_groups": {
    "tradition": ["sacred"],
    "platform": ["prose", "visual", "poetry", "sacred"]
  },
  "defaults_by_work_type": {
    "_description": "Per-work-type defaults applied by the runtime when the project spec omits the key. The value MUST be one of the seeded traditions/platforms above. An unlisted work type MUST NOT receive a default (runtime returns null).",
    "tradition": {
      "scripture_biblical": "catholic",
      "scripture_quranic": "islamic-hafs",
      "scripture_torah": "jewish",
      "scripture_buddhist": "pali",
      "scripture_vedic": "sanskrit",
      "scripture_generic": null,
      "commentary": null,
      "devotional": null,
      "liturgical": null,
      "historical_chronicle": null,
      "historical_account": null,
      "mythological_collection": null,
      "religious_epic": null,
      "sermon": null,
      "homiletic_collection": null
    },
    "platform": {
      "novel": "kdp",
      "novella": "kdp",
      "short_story": "kdp",
      "flash_fiction": "kdp",
      "memoir": "kdp",
      "creative_nonfiction": "kdp",
      "biography": "kdp",
      "essay": "kdp",
      "essay_collection": "kdp",
      "poetry_collection": "kdp",
      "single_poem": "kdp",
      "song_lyric": "kdp",
      "comic": "kdp",
      "graphic_novel": "kdp",
      "childrens_book": "kdp",
      "picture_book": "kdp",
      "scripture_biblical": "kdp",
      "scripture_quranic": "kdp",
      "scripture_torah": "kdp",
      "scripture_vedic": "kdp",
      "scripture_buddhist": "kdp",
      "scripture_generic": "kdp",
      "commentary": "kdp",
      "devotional": "kdp",
      "liturgical": "kdp",
      "historical_chronicle": "kdp",
      "historical_account": "kdp",
      "mythological_collection": "kdp",
      "religious_epic": "kdp",
      "sermon": "kdp",
      "homiletic_collection": "kdp"
    }
  }
}
```

Insertion convention: the new block goes as a sibling key at the same JSON-object depth as `work_type_groups`, `work_types`, `commands`, `sacred_config_schema`, `messages`. Insert immediately after `sacred_config_schema`'s closing brace and its following comma, before `"messages":`.

After editing, the file must remain valid JSON - run `node -e "JSON.parse(require('fs').readFileSync('data/CONSTRAINTS.json'))"` mentally (and the verify step does it for real).

IMPORTANT - do NOT edit the existing `sacred_config_schema.tradition.values` array at line 1380. That field is the OLD tradition taxonomy (christian, jewish, islamic, buddhist, hindu, interfaith, historical_secular, custom) used by existing per-project sacred `.manuscript/config.json` blocks. The NEW `architectural_profiles.traditions` is the per-tradition-PROFILE axis (catholic, orthodox, tewahedo, ...). They coexist: the old key answers "which religion?", the new key answers "which lineage's book order / approval block / fonts?". Future work may collapse them - that's deferred.
  </action>
  <verify>
    <automated>node -e "const c = JSON.parse(require('fs').readFileSync('data/CONSTRAINTS.json','utf8')); if (!c.architectural_profiles) throw new Error('missing architectural_profiles'); if (c.architectural_profiles.traditions._seeded.length !== 10) throw new Error('expected 10 seeded traditions'); if (c.architectural_profiles.platforms._seeded.length !== 8) throw new Error('expected 8 seeded platforms'); if (c.architectural_profiles.applies_to_groups.tradition[0] !== 'sacred') throw new Error('tradition group wrong'); if (!c.architectural_profiles.applies_to_groups.platform.includes('prose')) throw new Error('platform groups wrong'); if (c.architectural_profiles.defaults_by_work_type.tradition.scripture_biblical !== 'catholic') throw new Error('bible default wrong'); if (c.architectural_profiles.defaults_by_work_type.tradition.scripture_quranic !== 'islamic-hafs') throw new Error('quran default wrong'); if (c.architectural_profiles.defaults_by_work_type.platform.novel !== 'kdp') throw new Error('novel platform default wrong'); if (!c.work_types) throw new Error('work_types clobbered'); if (!c.commands) throw new Error('commands clobbered'); if (!c.sacred_config_schema) throw new Error('sacred_config_schema clobbered'); if (!c.messages) throw new Error('messages clobbered'); if (c.sacred_config_schema.tradition.values[0] !== 'christian') throw new Error('old sacred tradition taxonomy altered'); console.log('OK');" && node --test test/constraints.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `data/CONSTRAINTS.json` parses as valid JSON
    - New key `architectural_profiles` exists at the top level
    - `architectural_profiles.traditions._seeded` has exactly 10 entries matching the Plan-01 sacred directory slugs
    - `architectural_profiles.platforms._seeded` has exactly 8 entries matching the Plan-01 platforms directory slugs
    - `architectural_profiles.applies_to_groups.tradition` equals `["sacred"]`
    - `architectural_profiles.applies_to_groups.platform` equals `["prose", "visual", "poetry", "sacred"]`
    - `architectural_profiles.defaults_by_work_type.tradition.scripture_biblical` equals `"catholic"`
    - `architectural_profiles.defaults_by_work_type.tradition.scripture_quranic` equals `"islamic-hafs"`
    - `architectural_profiles.defaults_by_work_type.platform.novel` equals `"kdp"`
    - Existing keys untouched: `work_type_groups`, `work_types`, `commands`, `exports`, `dependencies`, `command_adaptations`, `file_adaptations`, `sacred_config_schema`, `messages` all still present
    - The OLD `sacred_config_schema.tradition.values` array STILL starts with `"christian"` (byte-preserved)
    - `test/constraints.test.js` still passes (zero regression in existing CONSTRAINTS schema integrity tests)
  </acceptance_criteria>
  <done>data/CONSTRAINTS.json gains architectural_profiles block with traditions (10), platforms (8), applies_to_groups, and defaults_by_work_type; existing keys untouched; existing constraints test passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# JSON is still parseable and existing test passes
node --test test/constraints.test.js

# New block is queryable
node -e "const c=require('./data/CONSTRAINTS.json'); console.log(Object.keys(c.architectural_profiles))"
# expected: [ '_description', '_render_variants', 'traditions', 'platforms', 'applies_to_groups', 'defaults_by_work_type' ]

# WORK.md template intact and extended
grep -c "^## " templates/WORK.md   # expected: existing section count + 1
grep "PROFILE_BLOCK" templates/WORK.md
```
</verification>

<success_criteria>
- `templates/WORK.md` has a `## Profile` section with `{{PROFILE_BLOCK}}` placeholder, all other sections preserved
- `data/CONSTRAINTS.json` declares `architectural_profiles` with the complete traditions/platforms/defaults schema
- Existing test `test/constraints.test.js` still passes with zero regressions
- Zero code, zero dependencies, zero installer edits
- Plan 03 can read `data/CONSTRAINTS.json` directly to implement validation and default-inference against a stable contract
</success_criteria>

<output>
After completion, create `.planning/phases/29-architectural-foundation/29-02-SUMMARY.md` documenting:
- Exact lines added to `templates/WORK.md` (the ## Profile block with {{PROFILE_BLOCK}} placeholder)
- Exact keys added under `architectural_profiles` in `data/CONSTRAINTS.json`
- Confirmation that the OLD `sacred_config_schema.tradition.values` taxonomy was NOT touched
- Note that /scr:new-work wiring to actually render {{PROFILE_BLOCK}} is deferred (phase-29 scope limit)
- Confirmation that `test/constraints.test.js` still passes
</output>
</content>
</invoke>