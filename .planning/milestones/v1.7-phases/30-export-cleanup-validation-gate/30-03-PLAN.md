---
phase: 30-export-cleanup-validation-gate
plan: 03
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
files_modified:
  - commands/scr/export.md
  - commands/scr/publish.md
autonomous: true
requirements:
  - VALID-03

must_haves:
  truths:
    - "A writer invoking `/scr:export` on a manuscript with `[Fill in` or `Alternate N:` markers is blocked before STEP 2 (tool detection) runs"
    - "A writer invoking `/scr:publish` on a dirty manuscript is blocked before STEP 2 (ROUTE) runs"
    - "Both gates show the exact file:line list of blocking markers plus a pointer to `/scr:cleanup --apply`"
    - "Both gates accept `--skip-validate` with a visible warning block (not silent bypass)"
    - "Gate runs BEFORE tool detection (`command -v pandoc`) in export.md - no Pandoc probe on a dirty manuscript"
  artifacts:
    - path: "commands/scr/export.md"
      provides: "STEP 1.5 validate gate injected before STEP 2"
      contains: "STEP 1.5"
    - path: "commands/scr/publish.md"
      provides: "STEP 1.5 validate gate injected after STEP 1 context load"
      contains: "STEP 1.5"
  key_links:
    - from: "commands/scr/export.md STEP 1.5"
      to: "commands/scr/export.md STEP 2"
      via: "positional ordering - STEP 1.5 text precedes STEP 2 text"
      pattern: "indexOf.*STEP 1\\.5.*<.*indexOf.*STEP 2"
    - from: "commands/scr/publish.md STEP 1.5"
      to: "commands/scr/publish.md STEP 2"
      via: "positional ordering - STEP 1.5 text precedes STEP 2 text (ROUTE)"
      pattern: "indexOf.*STEP 1\\.5.*<.*indexOf.*STEP 2"
    - from: "export.md gate"
      to: "commands/scr/cleanup.md"
      via: "resolution pointer in blockquote error"
      pattern: "/scr:cleanup --apply"
---

<objective>
Inject the validate gate as STEP 1.5 into `commands/scr/export.md` and `commands/scr/publish.md` - positioned before tool detection in export and before routing in publish.

Purpose: VALID-03 - export and publish commands automatically block when scaffold markers are present, with actionable guidance pointing to `/scr:cleanup --apply`.
Output: Surgical edits to two existing command files; full `npm test` suite stays green.
</objective>

<execution_context>
@$HOME/.claude/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-export-cleanup-validation-gate/30-CONTEXT.md
@.planning/phases/30-export-cleanup-validation-gate/30-RESEARCH.md
@.planning/phases/30-export-cleanup-validation-gate/30-PATTERNS.md
@.planning/phases/30-export-cleanup-validation-gate/30-02-SUMMARY.md

<interfaces>
<!-- Exact injection points (from PATTERNS.md): -->

<!-- export.md injection boundary: -->
<!-- Current lines 82-86: -->
<!--   Then **stop**.                    ← line 82 (end of format-not-available gate) -->
<!--                                     ← line 83 (blank) -->
<!--   ---                               ← line 84 (STEP separator) -->
<!--                                     ← line 85 (blank) -->
<!--   ### STEP 2: CHECK PREREQUISITES   ← line 86 -->
<!-- INSERT the new STEP 1.5 block AFTER line 82 (`Then **stop**.`) and BEFORE line 84 (`---`). -->
<!-- That is: insert between "Then **stop**." and the `---` separator that introduces STEP 2. -->

<!-- publish.md injection boundary: -->
<!-- Current lines 24-36: -->
<!--   ### STEP 1: LOAD CONTEXT          ← line 24 -->
<!--   ...                               ← lines 25-33 (context loads + work type determination) -->
<!--   ---                               ← line 34 (STEP separator closing STEP 1) -->
<!--                                     ← line 35 (blank) -->
<!--   ### STEP 2: ROUTE                 ← line 36 -->
<!-- INSERT the new STEP 1.5 block AFTER line 34 (`---`) and BEFORE line 35 (blank line before STEP 2). -->

<!-- Gate block content (from PATTERNS.md code examples and RESEARCH.md Pattern 2): -->
<!--
### STEP 1.5: VALIDATE MANUSCRIPT

**Check for scaffold markers in `.manuscript/drafts/`.**

Scan all `.md` files in `.manuscript/drafts/` for:
- Lines containing `[Fill in` (covers `[Fill in:]`, `[Fill in or delete:]`)
- Lines containing `[Delete if not applicable:]`
- Lines containing `Alternate 1:` or `Alternate 2:`
- Files with more than one `# ` (top-level H1) heading

**If `--skip-validate` was passed:**

> **Warning: Validate gate skipped (`--skip-validate`). Your manuscript may contain
> unresolved scaffold markers. Run `/scr:validate` to check before submitting.**

Proceed to STEP 2.

**If markers are found** (and `--skip-validate` was not passed):

> **Export blocked: unresolved scaffold markers found.**
>
> [list each as: `path/to/file.md:LINE_NUMBER: marker text`]
>
> **Fix:** Run `/scr:cleanup --apply` to remove scaffold markers, or manually
> edit the listed files, then re-run this export command.

Then **stop** -- do not proceed to STEP 2.

If no markers found: proceed to STEP 2.
-->
<!-- publish.md uses "Publishing blocked" instead of "Export blocked" in the blockquote -->

<!-- STEP separator pattern (from PATTERNS.md Shared Patterns): -->
<!-- Between STEP blocks: blank line + `---` + blank line + `### STEP N: HEADING` -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject STEP 1.5 validate gate into export.md</name>
  <files>commands/scr/export.md</files>

  <read_first>
    - commands/scr/export.md (full - must see the exact current text at the injection boundary before editing)
    - .planning/phases/30-export-cleanup-validation-gate/30-PATTERNS.md (export.md section - contains the exact STEP 1.5 block to insert verbatim, and the precise injection point: after line containing "Then **stop**." that ends the format-availability check, before the `---` separator leading into `### STEP 2`)
    - .planning/phases/30-export-cleanup-validation-gate/30-RESEARCH.md (Pitfall 1 - gate MUST be before STEP 2, not after; Common Pitfalls section)
  </read_first>

  <behavior>
    - Position check: `indexOf('STEP 1.5')` returns a position less than `indexOf('STEP 2')` in the final file
    - Content check: gate block contains "STEP 1.5: VALIDATE MANUSCRIPT" heading
    - Marker list: gate block lists all four marker patterns: `[Fill in`, `[Delete if not applicable:]`, `Alternate 1:`, `Alternate 2:`, duplicate `# ` heading
    - Skip-validate warning: gate block contains `--skip-validate` bypass with a visible `> **Warning:**` blockquote
    - Fail block: gate block contains "Export blocked" blockquote with file:line list spec
    - Resolution pointer: fail block contains `/scr:cleanup --apply`
    - Stop instruction: gate block contains `Then **stop** -- do not proceed to STEP 2.`
    - No regression: existing STEP 1 and STEP 2 content unchanged (only insertion, no deletion)
  </behavior>

  <action>
    Read `commands/scr/export.md` in full to confirm the current content.

    Locate the exact injection point: the line reading `Then **stop**.` that ends the format-availability check in STEP 1 (this is the stop for "format not available for work type"). The very next non-blank line should be `---` followed by `### STEP 2: CHECK PREREQUISITES`.

    Insert the following block between that `Then **stop**.` line and the `---` that precedes STEP 2. The insertion is an ADDITION - do not remove or alter any existing lines.

    **Block to insert** (insert exactly, including surrounding blank lines and `---` separator):

    ```markdown

    ---

    ### STEP 1.5: VALIDATE MANUSCRIPT

    **Check for scaffold markers in `.manuscript/drafts/`.**

    Scan all `.md` files in `.manuscript/drafts/` for:
    - Lines containing `[Fill in` (covers `[Fill in:]`, `[Fill in or delete:]`)
    - Lines containing `[Delete if not applicable:]`
    - Lines containing `Alternate 1:` or `Alternate 2:`
    - Files with more than one `# ` (top-level H1) heading

    **If `--skip-validate` was passed:**

    > **Warning: Validate gate skipped (`--skip-validate`). Your manuscript may contain
    > unresolved scaffold markers. Run `/scr:validate` to check before submitting.**

    Proceed to STEP 2.

    **If markers are found** (and `--skip-validate` was not passed):

    > **Export blocked: unresolved scaffold markers found.**
    >
    > [list each as: `path/to/file.md:LINE_NUMBER: marker text`]
    >
    > **Fix:** Run `/scr:cleanup --apply` to remove scaffold markers, or manually
    > edit the listed files, then re-run this export command.

    Then **stop** -- do not proceed to STEP 2.

    If no markers found: proceed to STEP 2.
    ```

    The file after injection should have this sequence:
    1. ... (STEP 1 content) ...
    2. `Then **stop**.` (end of format-availability check)
    3. [blank line]
    4. `---`
    5. [blank line]
    6. `### STEP 1.5: VALIDATE MANUSCRIPT`
    7. ... (gate content) ...
    8. `If no markers found: proceed to STEP 2.`
    9. [blank line]
    10. `---`
    11. [blank line]
    12. `### STEP 2: CHECK PREREQUISITES`

    Write the updated file.
  </action>

  <acceptance_criteria>
    - `grep -n "STEP 1.5" commands/scr/export.md` returns at least 1 line with "STEP 1.5: VALIDATE MANUSCRIPT"
    - `grep -n "STEP 2" commands/scr/export.md | head -1` returns a line number GREATER than the line number returned by `grep -n "STEP 1.5" commands/scr/export.md`
    - `grep "skip-validate" commands/scr/export.md` returns a line inside the gate block
    - `grep "Warning.*skip-validate\|skip-validate.*Warning" commands/scr/export.md` returns a line showing the warning is visible (not silent)
    - `grep "Export blocked" commands/scr/export.md` returns the blockquote error header
    - `grep "scr:cleanup --apply" commands/scr/export.md` returns the resolution pointer line
    - `grep "do not proceed to STEP 2" commands/scr/export.md` returns the stop instruction
    - `grep "CHECK PREREQUISITES" commands/scr/export.md` returns a line (STEP 2 content unchanged)
    - `node --test test/phase30-export-cleanup-validation-gate.test.js 2>&1 | grep "VALID-03\|export"` shows the export.md-related VALID-03 tests passing
    - `npm test` passes all tests
  </acceptance_criteria>

  <verify>
    <automated>node --test test/phase30-export-cleanup-validation-gate.test.js 2>&1 | grep -E "VALID-03|export\.md"</automated>
  </verify>

  <done>
    - `commands/scr/export.md` contains STEP 1.5 block positioned before STEP 2
    - Gate contains: marker list, --skip-validate warning, "Export blocked" error block, `/scr:cleanup --apply` resolution, stop instruction
    - VALID-03 tests for export.md all GREEN
    - `npm test` remains green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject STEP 1.5 validate gate into publish.md</name>
  <files>commands/scr/publish.md</files>

  <read_first>
    - commands/scr/publish.md (full - must see the exact current text at the injection boundary before editing)
    - .planning/phases/30-export-cleanup-validation-gate/30-PATTERNS.md (publish.md section - contains the exact STEP 1.5 block to insert for publish, injection point: after line 34 `---` closing STEP 1, before line 36 `### STEP 2: ROUTE`)
    - .planning/phases/30-export-cleanup-validation-gate/30-RESEARCH.md (Pattern 3 - why STEP 1.5 not STEP 0 in publish.md; Pitfall 5 - --skip-validate MUST emit visible warning)
  </read_first>

  <behavior>
    - Position check: `indexOf('STEP 1.5')` returns a position less than `indexOf('STEP 2')` in the final file
    - Content check: gate block contains "STEP 1.5: VALIDATE MANUSCRIPT" heading
    - Publish-specific label: blockquote error says "Publishing blocked" (not "Export blocked")
    - Marker list: same four patterns as export.md gate
    - Skip-validate warning: visible `> **Warning:**` blockquote (not silent)
    - Resolution pointer: `/scr:cleanup --apply` in the fail block
    - Stop instruction: `Then **stop** -- do not proceed to STEP 2.`
    - No regression: STEP 1 (LOAD CONTEXT) and STEP 2 (ROUTE) content unchanged
  </behavior>

  <action>
    Read `commands/scr/publish.md` in full to confirm the current content.

    Locate the injection point: the `---` horizontal rule that closes `### STEP 1: LOAD CONTEXT` (after the "Determine the work type group..." sentence). The very next content should be blank line + `### STEP 2: ROUTE`.

    Insert the following block between the `---` that closes STEP 1 and the blank line + `### STEP 2: ROUTE`. The insertion is an ADDITION - do not remove or alter any existing lines.

    **Block to insert** (insert exactly, after the `---` that closes STEP 1):

    ```markdown

    ### STEP 1.5: VALIDATE MANUSCRIPT

    **Check for scaffold markers in `.manuscript/drafts/`.**

    Scan all `.md` files in `.manuscript/drafts/` for:
    - Lines containing `[Fill in` (covers `[Fill in:]`, `[Fill in or delete:]`)
    - Lines containing `[Delete if not applicable:]`
    - Lines containing `Alternate 1:` or `Alternate 2:`
    - Files with more than one `# ` (top-level H1) heading

    **If `--skip-validate` was passed:**

    > **Warning: Validate gate skipped (`--skip-validate`). Your manuscript may contain
    > unresolved scaffold markers. Run `/scr:validate` to check before submitting.**

    Proceed to STEP 2.

    **If markers are found** (and `--skip-validate` was not passed):

    > **Publishing blocked: unresolved scaffold markers found.**
    >
    > [list each as: `path/to/file.md:LINE_NUMBER: marker text`]
    >
    > **Fix:** Run `/scr:cleanup --apply` to remove scaffold markers, or manually
    > edit the listed files, then re-run this publish command.

    Then **stop** -- do not proceed to STEP 2.

    If no markers found: proceed to STEP 2.

    ---
    ```

    The file after injection should have this sequence:
    1. `### STEP 1: LOAD CONTEXT`
    2. ... (STEP 1 content - config loads, work type determination) ...
    3. `---` (the original STEP 1 closing separator)
    4. [blank line]
    5. `### STEP 1.5: VALIDATE MANUSCRIPT`
    6. ... (gate content) ...
    7. `If no markers found: proceed to STEP 2.`
    8. [blank line]
    9. `---`
    10. [blank line]
    11. `### STEP 2: ROUTE`

    Write the updated file.
  </action>

  <acceptance_criteria>
    - `grep -n "STEP 1.5" commands/scr/publish.md` returns at least 1 line with "STEP 1.5: VALIDATE MANUSCRIPT"
    - `grep -n "STEP 2" commands/scr/publish.md | head -1` returns a line number GREATER than the line from STEP 1.5 grep
    - `grep "skip-validate" commands/scr/publish.md` returns a line inside the gate block
    - `grep "Warning.*skip-validate\|skip-validate.*Warning" commands/scr/publish.md` returns the visible warning line
    - `grep "Publishing blocked" commands/scr/publish.md` returns the blockquote error header
    - `grep "scr:cleanup --apply" commands/scr/publish.md` returns the resolution pointer
    - `grep "do not proceed to STEP 2" commands/scr/publish.md` returns the stop instruction
    - `grep "ROUTE" commands/scr/publish.md` returns the STEP 2 heading (STEP 2 content unchanged)
    - `grep "LOAD CONTEXT" commands/scr/publish.md` returns the STEP 1 heading (STEP 1 content unchanged)
    - `node --test test/phase30-export-cleanup-validation-gate.test.js` - all 15 assertions GREEN
    - `npm test` passes all tests
  </acceptance_criteria>

  <verify>
    <automated>node --test test/phase30-export-cleanup-validation-gate.test.js 2>&1 | tail -5</automated>
  </verify>

  <done>
    - `commands/scr/publish.md` contains STEP 1.5 block positioned before STEP 2 (ROUTE)
    - Gate uses "Publishing blocked" label (not "Export blocked")
    - Gate contains: marker list, --skip-validate visible warning, blockquote error, `/scr:cleanup --apply` resolution, stop instruction
    - All 15 Phase 30 regression test assertions GREEN
    - `npm test` full suite green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer -> /scr:export | Writer invokes export; agent scans .manuscript/drafts/ before running any external tool |
| writer -> /scr:publish | Writer invokes publish wizard; agent scans before routing to any preset |
| --skip-validate flag | Writer can bypass gate; must receive visible warning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Tampering | export.md gate injection | mitigate | Gate is additive (insertion only). Existing STEP 1 and STEP 2 content unchanged. Verified by `grep "CHECK PREREQUISITES"` and `grep "LOAD CONTEXT"` still returning hits after edit. |
| T-30-08 | Tampering | publish.md gate injection | mitigate | Same additive-only approach. STEP 1 and STEP 2 verified present after edit by grep checks. |
| T-30-09 | Elevation of Privilege | --skip-validate bypass | mitigate | Bypass always emits a visible `> **Warning:**` blockquote in the session. Writer cannot silently skip the gate - the warning is unconditional when the flag is used. No silent passthrough permitted (per CONTEXT.md locked decision). |
| T-30-10 | Repudiation | Gate blocks export | accept | Gate output is in-session only (no persistent log). Writer sees the block reason and file:line list in the session. This is intentional per the deferred/out-of-scope decision in CONTEXT.md (no persistent VALIDATE-REPORT.md). |
</threat_model>

<verification>
1. `node --test test/phase30-export-cleanup-validation-gate.test.js` - all 15 assertions GREEN (complete phase coverage)
2. `npm test` - full suite green (1132+ prior tests + 15 new assertions)
3. Ordering check: `grep -n "STEP 1.5\|STEP 2" commands/scr/export.md` - STEP 1.5 line number is less than STEP 2 line number
4. Ordering check: `grep -n "STEP 1.5\|STEP 2" commands/scr/publish.md` - STEP 1.5 line number is less than STEP 2 line number
5. `grep "scr:cleanup --apply" commands/scr/export.md commands/scr/publish.md` - resolution pointer present in both files
</verification>

<success_criteria>
Gate injected into both export.md and publish.md. All 15 Phase 30 regression tests passing. Full npm test suite green. VALID-03 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/30-export-cleanup-validation-gate/30-03-SUMMARY.md`
</output>
