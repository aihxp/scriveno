---
phase: 30-export-cleanup-validation-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/phase30-export-cleanup-validation-gate.test.js
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - VALID-01
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - "Running `node --test test/phase30-export-cleanup-validation-gate.test.js` produces 15 test assertions (all failing before plan 02-03 ship)"
    - "Every CLEAN-01, CLEAN-02, VALID-01, VALID-02, VALID-03 requirement maps to at least one assertion"
    - "Full `npm test` suite still passes (1132 + new tests, new tests skip gracefully when files not yet present)"
  artifacts:
    - path: "test/phase30-export-cleanup-validation-gate.test.js"
      provides: "Regression contract for all Phase 30 deliverables"
      min_lines: 60
  key_links:
    - from: "test/phase30-export-cleanup-validation-gate.test.js"
      to: "commands/scr/cleanup.md"
      via: "fs.existsSync + content check"
      pattern: "cleanup\\.md"
    - from: "test/phase30-export-cleanup-validation-gate.test.js"
      to: "commands/scr/validate.md"
      via: "fs.existsSync + content check"
      pattern: "validate\\.md"
    - from: "test/phase30-export-cleanup-validation-gate.test.js"
      to: "commands/scr/export.md"
      via: "content check for STEP 1.5 block"
      pattern: "STEP 1\\.5"
    - from: "test/phase30-export-cleanup-validation-gate.test.js"
      to: "commands/scr/publish.md"
      via: "content check for STEP 1.5 block"
      pattern: "STEP 1\\.5"
---

<objective>
Create the regression test suite that locks all Phase 30 behavior as machine-executable contracts before any command files are written.

Purpose: Nyquist rule - tests run RED now (files don't exist yet), go GREEN when plans 02 and 03 ship. The test file is the specification in executable form.
Output: `test/phase30-export-cleanup-validation-gate.test.js` with 15 assertions covering CLEAN-01, CLEAN-02, VALID-01, VALID-02, VALID-03
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-export-cleanup-validation-gate/30-RESEARCH.md
@.planning/phases/30-export-cleanup-validation-gate/30-PATTERNS.md

<interfaces>
<!-- Test pattern from existing phase test file -->
<!-- From test/phase29-architectural-foundation.test.js lines 1-12: -->
```javascript
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');

const ROOT = path.join(__dirname, '..');
```

<!-- From test/commands.test.js - structural invariants checked for every command: -->
<!-- 1. File exists at commands/scr/<name>.md -->
<!-- 2. Content contains YAML frontmatter (---\n...\n---) -->
<!-- 3. Content contains 'description:' in frontmatter -->
<!-- 4. Content contains at least one '# ' heading after frontmatter -->

<!-- Test runner: node --test test/*.test.js (no Jest, no Mocha) -->
<!-- Node built-in test runner: describe/it from 'node:test', assert from 'node:assert/strict' -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write phase 30 regression test suite (RED phase)</name>
  <files>test/phase30-export-cleanup-validation-gate.test.js</files>

  <read_first>
    - test/phase29-architectural-foundation.test.js (full - copy the describe/it/assert structure, ROOT path setup)
    - test/commands.test.js (lines 34-59 - see what structural checks the existing suite already does so tests don't duplicate)
    - .planning/phases/30-export-cleanup-validation-gate/30-RESEARCH.md (Validation Architecture section - the Phase Requirements -> Test Map table lists all 15 assertions)
    - .planning/phases/30-export-cleanup-validation-gate/30-PATTERNS.md (Pattern 1 - exact command structural invariants)
  </read_first>

  <behavior>
    Test 1 (CLEAN-01 structural): `cleanup.md` exists at `commands/scr/cleanup.md`
    Test 2 (CLEAN-01 structural): `cleanup.md` content contains YAML frontmatter (`---` block) and `description:` field
    Test 3 (CLEAN-01 content): `cleanup.md` content includes `--apply` flag mention
    Test 4 (CLEAN-01 content): `cleanup.md` content scopes to `.manuscript/drafts/` (not broader glob)
    Test 5 (CLEAN-02 content): `cleanup.md` content includes diff summary language (contains both "Summary" and removed/cleaned marker counts)
    Test 6 (VALID-01 structural): `validate.md` exists at `commands/scr/validate.md`
    Test 7 (VALID-01 structural): `validate.md` content contains YAML frontmatter and `description:` field
    Test 8 (VALID-02 content): `validate.md` content mentions non-zero exit / "stop" on marker detection
    Test 9 (VALID-02 content): `validate.md` content references file:line output format (contains `:LINE` or `file:line` or similar)
    Test 10 (VALID-02 content): `validate.md` content mentions pass confirmation ("Manuscript clean" or "no scaffold markers found")
    Test 11 (VALID-03 content): `export.md` contains a validate gate step before the prerequisites step - specifically, the text "STEP 1.5" appears before "STEP 2"
    Test 12 (VALID-03 content): `export.md` gate block mentions `--skip-validate` bypass
    Test 13 (VALID-03 content): `export.md` gate block mentions `/scr:cleanup --apply` as the resolution path
    Test 14 (VALID-03 content): `publish.md` contains validate gate - specifically "STEP 1.5" appears and it precedes "STEP 2" (ROUTE)
    Test 15 (VALID-03 content): `publish.md` gate mentions `--skip-validate` with visible warning language
  </behavior>

  <action>
    Create `test/phase30-export-cleanup-validation-gate.test.js` using the Node.js built-in test runner pattern from phase29 test file.

    File structure:
    1. Require block: `node:test`, `node:assert/strict`, `fs`, `path`
    2. Constants: `ROOT = path.join(__dirname, '..')`, paths to the 4 command files
    3. Helper: read file content with `fs.readFileSync(path, 'utf8')` - wrap each read in a try/catch that fails the test cleanly if the file doesn't exist yet (so the suite doesn't crash before plan 02/03 ship)
    4. 6 `describe` blocks (one per requirement ID): CLEAN-01, CLEAN-02, VALID-01, VALID-02, VALID-03
    5. Inside each describe block: the relevant `it()` assertions from the behavior list above

    For content assertions use `assert.ok(content.includes('...'), 'descriptive message')` or regex: `assert.match(content, /pattern/, 'message')`.

    For the "STEP 1.5 appears before STEP 2" ordering check (Tests 11, 14):
    ```javascript
    const step15Pos = content.indexOf('STEP 1.5');
    const step2Pos = content.indexOf('STEP 2');
    assert.ok(step15Pos !== -1, 'export.md must contain STEP 1.5 validate gate');
    assert.ok(step15Pos < step2Pos, 'STEP 1.5 must appear before STEP 2 in export.md');
    ```

    Per CLEAN-02 (Test 5): check for the word "Summary" and at least one of "bracket markers", "Alternate block", "removed" - the diff summary must name what was changed.

    Per VALID-02 (Test 8): check for "stop" (the stop-on-failure pattern) AND for "non-zero" OR "fail" OR "blocked" to indicate blocking behavior.

    Per VALID-02 (Test 9): check for a pattern like `.md:` followed by a digit, or the text "file:line" or "LINE_NUMBER".

    Run the tests after creation. They should ALL FAIL because `cleanup.md` and `validate.md` don't exist yet. Confirm RED state is correct - this is expected TDD behavior.
  </action>

  <verify>
    <automated>node --test test/phase30-export-cleanup-validation-gate.test.js 2>&1 | tail -20</automated>
  </verify>

  <done>
    - `test/phase30-export-cleanup-validation-gate.test.js` exists
    - Running `node --test test/phase30-export-cleanup-validation-gate.test.js` shows all assertions failing (RED state - expected)
    - Running `npm test` still passes all prior tests (the new test file's failures are new failures only, not crashes of the existing suite)
    - Each of CLEAN-01, CLEAN-02, VALID-01, VALID-02, VALID-03 has at least one `describe` block with `it()` assertions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file -> command files | Test reads local files; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | test/phase30-*.test.js | accept | Test files in version control; mutation would be visible in git diff. No sensitive data in test assertions. |
| T-30-02 | Denial of Service | npm test suite | accept | New test file failures are isolated to Phase 30 tests; existing 1132 tests unaffected by new file presence. |
</threat_model>

<verification>
1. `node --test test/phase30-export-cleanup-validation-gate.test.js` runs without crashing the process (exits with non-zero because files missing, but no uncaught exceptions)
2. `npm test` passes all 1132+ prior tests (new failures are only the new Phase 30 assertions)
3. File has 6 `describe` blocks covering CLEAN-01, CLEAN-02, VALID-01, VALID-02, VALID-03
</verification>

<success_criteria>
Test file created, all 15 assertions in RED state (files don't exist yet), full suite stays green for prior tests
</success_criteria>

<output>
After completion, create `.planning/phases/30-export-cleanup-validation-gate/30-01-SUMMARY.md`
</output>
