---
phase: 32-build-pipelines-platform-awareness
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - commands/scr/build-ebook.md
  - commands/scr/build-print.md
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - BUILD-05

must_haves:
  truths:
    - "A writer can run /scr:build-ebook and get an EPUB at .manuscript/output/ebook.epub"
    - "A writer can run /scr:build-print --platform kdp and get a PDF at .manuscript/output/print-kdp.pdf"
    - "Both commands check for scaffold markers (STEP 1.5) and scaffold front matter (STEP 1.6) before building"
    - "Both commands detect missing Pandoc/Typst/Ghostscript before attempting any build"
    - "The EPUB output embeds lang tags, alt text instructions, and semantic nav for EAA compliance"
    - "The --platform, --trim, --strict, --skip-validate flags are all accepted"
  artifacts:
    - path: "commands/scr/build-ebook.md"
      provides: "EPUB build pipeline command"
      exports: []
    - path: "commands/scr/build-print.md"
      provides: "Print-ready PDF build pipeline command"
      exports: []
  key_links:
    - from: "commands/scr/build-ebook.md"
      to: "commands/scr/export.md"
      via: "STEP 1.5 and STEP 1.6 copied verbatim (only 'Build blocked' differs from 'Export blocked')"
      pattern: "STEP 1.5"
    - from: "commands/scr/build-print.md"
      to: "templates/platforms/kdp/manifest.yaml"
      via: "trim_sizes lookup and wpp page-count estimation in STEP 2.5"
      pattern: "manifest.yaml"
    - from: "commands/scr/build-ebook.md"
      to: ".manuscript/output/ebook.epub"
      via: "pandoc invocation in STEP 3"
      pattern: "ebook.epub"
---

<objective>
Create `commands/scr/build-ebook.md` and `commands/scr/build-print.md` - the two new build pipeline command files.

Purpose: Give writers dedicated, platform-aware build commands that go beyond /scr:export by adding trim-size selection, page-count guardrails, and targeted accessibility scaffolding. Both commands inherit the Phase 30 validate gate (STEP 1.5) and Phase 31 scaffold-exclusion gate (STEP 1.6) from export.md.

Output: Two markdown command files in `commands/scr/` following the exact structural conventions of export.md.
</objective>

<execution_context>
@$HOME/.claude/workflows/execute-plan.md
@$HOME/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-build-pipelines-platform-awareness/32-CONTEXT.md
@.planning/phases/32-build-pipelines-platform-awareness/32-PATTERNS.md

<interfaces>
<!-- Exact patterns the executor copies from export.md. Read export.md lines 1-222 before writing. -->

From commands/scr/export.md - STEP 1.5 (copy verbatim, change only "Export blocked" -> "Build blocked"):
Lines 86-116 contain the full STEP 1.5 block. The --skip-validate escape hatch and marker list must be preserved exactly.

From commands/scr/export.md - STEP 1.6 (copy verbatim):
Lines 118-164 contain the full STEP 1.6 block (scaffold exclusion + GENERATE auto-refresh).

From commands/scr/export.md - Pandoc EPUB invocation template (lines 397-415):
```bash
pandoc .manuscript/output/assembled-manuscript.md \
  -o .manuscript/output/manuscript.epub \
  --metadata-file=.manuscript/output/metadata.yaml \
  --epub-cover-image=.manuscript/output/cover.jpg \
  --css=data/export-templates/scriveno-epub.css \
  --toc \
  --toc-depth=2 \
  --split-level=1
```
For build-ebook.md: output filename is `ebook.epub` (not `manuscript.epub`).

From commands/scr/export.md - Pandoc print PDF invocation template (lines 370-390):
```bash
pandoc .manuscript/output/assembled-manuscript.md \
  -o .manuscript/output/manuscript-print.pdf \
  --pdf-engine=typst \
  --template=data/export-templates/scriveno-book.typst \
  --metadata-file=.manuscript/output/metadata.yaml \
  --toc \
  --toc-depth=2 \
  -V paperwidth={trim_width}in \
  -V paperheight={trim_height}in \
  -V margin-inside=0.75in \
  -V margin-outside=0.5in \
  -V margin-top=0.75in \
  -V margin-bottom=0.75in
```
For build-print.md: output filename is `print-{platform}.pdf`; trim dimensions come from manifest lookup.

From data/CONSTRAINTS.json exports section (lines 1222-1238) - constraint keys for STEP 1:
- build-ebook.md: constraint key is `build_ebook`, available for ["prose", "visual", "poetry", "interactive", "sacred"]
- build-print.md: constraint key is `build_print`, available for ["prose", "visual", "poetry", "sacred"]

From lib/architectural-profiles.js:
- `validatePlatform(slug)` - use in STEP 2.5 to validate --platform flag
- `listPlatforms()` - use in error message when platform is invalid
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create commands/scr/build-ebook.md</name>
  <files>commands/scr/build-ebook.md</files>
  <read_first>
    - commands/scr/export.md (full file - copy STEP 1.5 and STEP 1.6 verbatim from here)
    - commands/scr/publish.md (confirm STEP 1.5 and STEP 1.6 are identical between export.md and publish.md)
    - .planning/phases/32-build-pipelines-platform-awareness/32-CONTEXT.md (exact decision values)
    - .planning/phases/32-build-pipelines-platform-awareness/32-PATTERNS.md (full command structure)
  </read_first>
  <behavior>
    - build-ebook.md exists at commands/scr/build-ebook.md
    - YAML frontmatter has description: and argument-hint: "[--platform <platform>] [--skip-validate]"
    - STEP 1 checks CONSTRAINTS.json for build_ebook availability by work type group
    - STEP 1.5 is copied verbatim from export.md lines 86-116, with only "Export blocked" changed to "Build blocked"
    - STEP 1.6 is copied verbatim from export.md lines 118-164
    - STEP 1.5 appears before STEP 1.6 in the file
    - STEP 1.6 appears before ### STEP 2: in the file
    - STEP 2 checks for Pandoc only (`command -v pandoc >/dev/null 2>&1`) with multi-platform install hints (brew/apt/choco + download URL)
    - STEP 3 assembles manuscript (read OUTLINE.md, scan front matter with scaffold exclusion)
    - STEP 4 invokes Pandoc to produce .manuscript/output/ebook.epub
    - Pandoc invocation includes: `--epub-metadata` or `--metadata-file`, `--epub-cover-image`, `--css=data/export-templates/scriveno-epub.css`, `--toc`, `--toc-depth=2`, `--split-level=1`
    - STEP 4 includes EAA accessibility guidance: instruct agent to ensure all images have alt text, document lang tag is set from project language, epub:type semantic nav is included via the --toc flag
    - STEP 5 reports: `[x] EPUB built -> .manuscript/output/ebook.epub (file size)`
  </behavior>
  <action>
Create `commands/scr/build-ebook.md` with this exact structure:

**Frontmatter:**
```yaml
---
description: Build a publication-ready EPUB from the manuscript for a target platform.
argument-hint: "[--platform <platform>] [--skip-validate]"
---
```

**Heading:** `# /scr:build-ebook -- EPUB Build Pipeline`

**Brief description paragraph:** "Assemble the manuscript and produce an accessible EPUB for the selected publishing platform. Includes validate gate, scaffold-exclusion, accessibility (EAA-compliant lang tags, alt text, semantic nav), and platform-aware Pandoc invocation."

**Usage section:**
```
/scr:build-ebook [--platform <platform>] [--skip-validate]
```
Platform values: `kdp | ingram | apple | bn | d2d | kobo | google | smashwords` (default: kdp)

**Instruction section header:** "You are a **manuscript build specialist** for EPUB output."

**### STEP 1: LOAD CONTEXT**
- Load `.manuscript/config.json` for work_type, title, author, language
- Load CONSTRAINTS.json; check `build_ebook` constraint key
- Available for: `["prose", "visual", "poetry", "interactive", "sacred"]`
- If not available: show error + stop

**### STEP 1.5: VALIDATE MANUSCRIPT** - copy verbatim from export.md lines 86-116, changing only the blocked message from `Export blocked` to `Build blocked` and "re-run this export command" to "re-run this build command".

**### STEP 1.6: FRONT-MATTER GATE** - copy verbatim from export.md lines 118-164.

**### STEP 2: CHECK PREREQUISITES**
- Check Pandoc only:
  ```bash
  command -v pandoc >/dev/null 2>&1
  ```
  If not found, show:
  > **Pandoc is required for EPUB build but is not installed.**
  >
  > **Install Pandoc:**
  > - macOS: `brew install pandoc`
  > - Linux: `sudo apt install pandoc`
  > - Windows: `choco install pandoc`
  > - Or download from https://pandoc.org/installing.html
  >
  > After installing, run this build command again.
  
  Then **stop**.

**### STEP 3: ASSEMBLE MANUSCRIPT**
- Read OUTLINE.md, assemble body chapters in order
- Scan .manuscript/front-matter/, apply scaffold exclusion list from STEP 1.6a (omit files where scaffold: true)
- Write assembled file to .manuscript/output/assembled-manuscript.md
- Write metadata.yaml to .manuscript/output/metadata.yaml with: title, author, language (from config.json), date (current year)

**### STEP 4: BUILD EPUB**

**4a - Accessibility pre-check:**
Before invoking Pandoc, verify:
- All images referenced in the assembled manuscript have alt text in their Markdown syntax (`![alt text](path)`). For any image missing alt text, add a placeholder: `![Illustration: [describe the image]](path)`.
- The project language (`lang`) is set - if absent from config.json, default to `en`.

**4b - Pandoc invocation:**
```bash
pandoc .manuscript/output/assembled-manuscript.md \
  -o .manuscript/output/ebook.epub \
  --metadata-file=.manuscript/output/metadata.yaml \
  --epub-cover-image=.manuscript/output/cover.jpg \
  --css=data/export-templates/scriveno-epub.css \
  --toc \
  --toc-depth=2 \
  --split-level=1 \
  -V lang={language}
```
If .manuscript/output/cover.jpg does not exist, omit the `--epub-cover-image` flag.

**4c - Semantic nav note:**
The `--toc` flag causes Pandoc to emit an `epub:type="toc"` nav document (EPUB3 semantic navigation). This satisfies EU EAA June 2025 requirement for machine-readable navigation.

**### STEP 5: REPORT**
Show:
```
[x] EPUB built -> .manuscript/output/ebook.epub ({file_size})
```
Get file size with: `ls -lh .manuscript/output/ebook.epub | awk '{print $5}'`
  </action>
  <verify>
    <automated>
      node --test test/phase32-build-pipelines-platform-awareness.test.js 2>&1 | grep -E "BUILD-01|BUILD-03|BUILD-04|BUILD-05" | head -20
    </automated>
    After this task: BUILD-01, BUILD-03, BUILD-04, BUILD-05 tests that reference build-ebook.md should pass.
  </verify>
  <acceptance_criteria>
    - `commands/scr/build-ebook.md` exists
    - `grep "^---" commands/scr/build-ebook.md` shows YAML frontmatter delimiters
    - `grep "description:" commands/scr/build-ebook.md` contains "Build a publication-ready EPUB"
    - `grep "argument-hint:" commands/scr/build-ebook.md` contains "--platform"
    - `grep "STEP 1.5" commands/scr/build-ebook.md` returns a match
    - `grep "STEP 1.6" commands/scr/build-ebook.md` returns a match
    - `grep "STEP 1.6" commands/scr/build-ebook.md` appears AFTER `grep "STEP 1.5"` (use indexOf check in tests)
    - `grep "### STEP 2:" commands/scr/build-ebook.md` appears AFTER STEP 1.6
    - `grep "Build blocked" commands/scr/build-ebook.md` returns a match (not "Export blocked")
    - `grep "command -v pandoc" commands/scr/build-ebook.md` returns a match
    - `grep "brew install pandoc" commands/scr/build-ebook.md` returns a match
    - `grep "ebook.epub" commands/scr/build-ebook.md` returns at least 2 matches (output path + report line)
    - `grep "alt text" commands/scr/build-ebook.md` returns a match (EAA requirement)
    - `grep "lang" commands/scr/build-ebook.md` returns a match (EAA lang tag)
    - `grep "epub:type\|semantic nav\|epub:type" commands/scr/build-ebook.md` or `grep "nav document" commands/scr/build-ebook.md` returns a match
    - `grep "\-\-skip-validate" commands/scr/build-ebook.md` returns a match
  </acceptance_criteria>
  <done>build-ebook.md exists, has all required gates (STEP 1.5, 1.6), prerequisite check (Pandoc), EAA accessibility guidance, Pandoc invocation producing ebook.epub, and success report line. All BUILD-01/03/04/05 tests referencing build-ebook.md pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create commands/scr/build-print.md</name>
  <files>commands/scr/build-print.md</files>
  <read_first>
    - commands/scr/build-ebook.md (just created - use as structural template for gates)
    - commands/scr/export.md (Pandoc print PDF invocation pattern, lines 370-390)
    - .planning/phases/32-build-pipelines-platform-awareness/32-CONTEXT.md (platform flags, trim defaults, page-count warning format)
    - .planning/phases/32-build-pipelines-platform-awareness/32-PATTERNS.md (build-print.md structure section)
    - lib/architectural-profiles.js (validatePlatform and listPlatforms API to reference)
  </read_first>
  <behavior>
    - build-print.md exists at commands/scr/build-print.md
    - YAML frontmatter has description: and argument-hint: "[--platform <platform>] [--trim <size>] [--strict] [--skip-validate]"
    - STEP 1 checks CONSTRAINTS.json for build_print availability by work type group (available: prose, visual, poetry, sacred)
    - STEP 1.5 validate gate is identical to build-ebook.md (copied from export.md, "Build blocked" message)
    - STEP 1.6 front-matter gate is identical to build-ebook.md (copied from export.md)
    - STEP 2 checks Pandoc AND Typst; for --platform ingram also checks Ghostscript (`command -v gs`)
    - STEP 2.5 validates --platform flag using validatePlatform() logic; lists valid platforms on failure
    - STEP 2.5 loads manifest for selected platform from templates/platforms/{platform}/manifest.yaml
    - STEP 2.5 validates --trim against manifest.trim_sizes; rejects unsupported trim with clear error + valid list
    - STEP 2.5 estimates page count from manuscript word count and manifest wpp value; emits warning if estimate exceeds max_pages
    - Warning format: `WARNING Estimated {N} pages at {trim} ({PLATFORM} limit: {MAX}pp). Consider {ALT_PLATFORM} ({ALT_MAX}pp). Building anyway...`
    - --strict flag converts warning to hard block
    - STEP 3 assembles manuscript (same as build-ebook.md)
    - STEP 4 invokes Pandoc with --pdf-engine=typst, trim dimensions from manifest, output to .manuscript/output/print-{platform}.pdf
    - STEP 5 reports: `[x] PDF built -> .manuscript/output/print-{platform}.pdf ({file_size})`
  </behavior>
  <action>
Create `commands/scr/build-print.md` with this exact structure:

**Frontmatter:**
```yaml
---
description: Build a print-ready PDF from the manuscript for a target publishing platform.
argument-hint: "[--platform <platform>] [--trim <size>] [--strict] [--skip-validate]"
---
```

**Heading:** `# /scr:build-print -- Print PDF Build Pipeline`

**Brief description paragraph:** "Assemble the manuscript and produce a print-ready PDF for the selected publishing platform. Includes validate gate, scaffold-exclusion, trim size validation, page-count guardrail (warning by default, hard block with --strict), and platform-aware Pandoc + Typst invocation."

**Usage section:**
```
/scr:build-print [--platform <platform>] [--trim <size>] [--strict] [--skip-validate]
```
Platform values: `kdp | ingram | apple | bn | d2d | kobo | google | smashwords` (default: kdp)
Trim values (KDP/Ingram): `5x8 | 5.25x8 | 5.5x8.5 | 6x9 | 7x10` (default: 6x9)

Note: Apple, B&N, D2D, Kobo, Google, Smashwords are EPUB-only platforms. Running build-print with these platforms will produce an error directing the writer to use /scr:build-ebook instead.

**Instruction section header:** "You are a **manuscript build specialist** for print-ready PDF output."

**### STEP 1: LOAD CONTEXT**
- Load `.manuscript/config.json` and CONSTRAINTS.json
- Check `build_print` constraint key; available for: `["prose", "visual", "poetry", "sacred"]`
- If not available: show error + stop

**### STEP 1.5: VALIDATE MANUSCRIPT** - copy verbatim from export.md, changing "Export blocked" -> "Build blocked" and "re-run this export command" -> "re-run this build command".

**### STEP 1.6: FRONT-MATTER GATE** - copy verbatim from export.md lines 118-164.

**### STEP 2: CHECK PREREQUISITES**

Check Pandoc:
```bash
command -v pandoc >/dev/null 2>&1
```
If not found, show install guidance (same multi-platform format as build-ebook.md). Then **stop**.

Check Typst:
```bash
command -v typst >/dev/null 2>&1
```
If not found:
> **Typst is required for print PDF build but is not installed.**
>
> **Install Typst:**
> - macOS: `brew install typst`
> - Linux: Download from https://github.com/typst/typst/releases
> - Windows: `winget install typst`
> - Or visit https://typst.app for installation options
>
> After installing, run this build command again.

Then **stop**.

If `--platform ingram` was passed, also check Ghostscript:
```bash
command -v gs >/dev/null 2>&1
```
If not found:
> **Ghostscript is required for IngramSpark PDF/X-1a output but is not installed.**
>
> **Install Ghostscript:**
> - macOS: `brew install ghostscript`
> - Linux: `sudo apt install ghostscript`
> - Windows: `choco install ghostscript`
> - Or download from https://www.ghostscript.com/releases/gsdnld.html
>
> After installing, run this build command again.

Then **stop**.

**### STEP 2.5: VALIDATE PLATFORM AND TRIM SIZE**

Resolve the platform slug:
- If `--platform` was passed, use that value.
- If not passed, default to `kdp`.

Validate the platform slug using `lib/architectural-profiles.js` `validatePlatform(slug)` logic (check that slug is in the list returned by `listPlatforms()`).

If platform is invalid:
> **Platform "{slug}" is not recognised.**
>
> Valid platforms: kdp, ingram, apple, bn, d2d, kobo, google, smashwords
>
> Example: `/scr:build-print --platform kdp`

Then **stop**.

If the platform is EPUB-only (apple, bn, d2d, kobo, google, smashwords):
> **{PLATFORM} is an EPUB-only platform and does not accept print PDFs.**
>
> To build an EPUB for this platform, run: `/scr:build-ebook --platform {slug}`

Then **stop**.

Load `templates/platforms/{platform}/manifest.yaml`.

Resolve the trim size:
- If `--trim` was passed, use that value.
- If not passed, use manifest's `default_trim` value (6x9 for KDP and Ingram).

Check the trim size against the manifest's `trim_sizes` map.
If the trim size is NOT in the map:
> **Trim size "{size}" is not supported for {PLATFORM}.**
>
> Supported trim sizes for {PLATFORM}: {list all keys from manifest.trim_sizes}
>
> Default trim: {manifest.default_trim}

Then **stop**.

**Page-count estimation and guardrail** (only when platform has `max_pages` in manifest):

Get manuscript word count by reading assembled markdown and counting words (split by whitespace). If word count cannot be determined, skip guardrail silently.

Estimate page count: `estimated_pages = Math.round(word_count / manifest.trim_sizes[trim].wpp)`

Compare against `manifest.max_pages`:
- For paperback: compare against `manifest.max_pages.paperback`
- For hardcover: only KDP hardcover applies - compare against `manifest.max_pages.hardcover` only if `--hardcover` flag is passed (otherwise use paperback limit)

If estimated_pages exceeds the limit:
- If `--strict` was NOT passed (default warning mode):
  > WARNING Estimated {estimated_pages} pages at {trim} ({PLATFORM} paperback limit: {MAX}pp). Consider IngramSpark (1200pp). Building anyway...
- If `--strict` was passed (hard block):
  > **Build blocked (--strict): Estimated {estimated_pages} pages at {trim} exceeds {PLATFORM} paperback limit ({MAX}pp).**
  >
  > **Options:**
  > - Switch platform: `/scr:build-print --platform ingram`
  > - Reduce manuscript length
  > - Remove `--strict` to build anyway with a warning
  
  Then **stop**.

**### STEP 3: ASSEMBLE MANUSCRIPT**
Same as build-ebook.md STEP 3 (read OUTLINE.md, scaffold exclusion, write assembled-manuscript.md and metadata.yaml).

**### STEP 4: BUILD PDF**

Look up trim dimensions from manifest:
- `width_in` from `manifest.trim_sizes[trim].width_in`
- `height_in` from `manifest.trim_sizes[trim].height_in`

```bash
pandoc .manuscript/output/assembled-manuscript.md \
  -o .manuscript/output/print-{platform}.pdf \
  --pdf-engine=typst \
  --template=data/export-templates/scriveno-book.typst \
  --metadata-file=.manuscript/output/metadata.yaml \
  --toc \
  --toc-depth=2 \
  -V paperwidth={width_in}in \
  -V paperheight={height_in}in \
  -V margin-inside=0.75in \
  -V margin-outside=0.5in \
  -V margin-top=0.75in \
  -V margin-bottom=0.75in
```

For IngramSpark (`--platform ingram`): after Pandoc generates the PDF, note to the writer that a PDF/X-1a conversion via Ghostscript may be required for final IngramSpark submission; provide the command pattern but do not auto-run it (the conversion is destructive and writer should verify the intermediate PDF first).

**### STEP 5: REPORT**
Show:
```
[x] PDF built -> .manuscript/output/print-{platform}.pdf ({file_size})
```
Get file size with: `ls -lh .manuscript/output/print-{platform}.pdf | awk '{print $5}'`
  </action>
  <verify>
    <automated>
      node --test test/phase32-build-pipelines-platform-awareness.test.js 2>&1 | grep -E "BUILD-02|BUILD-03|BUILD-04|PLATFORM-01|PLATFORM-02" | head -20
    </automated>
    After this task: BUILD-02, BUILD-03, BUILD-04, PLATFORM-01, PLATFORM-02 tests that reference build-print.md should pass.
  </verify>
  <acceptance_criteria>
    - `commands/scr/build-print.md` exists
    - `grep "description:" commands/scr/build-print.md` contains "print-ready PDF"
    - `grep "argument-hint:" commands/scr/build-print.md` contains "--platform" AND "--trim" AND "--strict"
    - `grep "STEP 1.5" commands/scr/build-print.md` returns a match
    - `grep "STEP 1.6" commands/scr/build-print.md` returns a match
    - `grep "Build blocked" commands/scr/build-print.md` returns a match
    - `grep "command -v pandoc" commands/scr/build-print.md` returns a match
    - `grep "command -v typst" commands/scr/build-print.md` returns a match
    - `grep "brew install typst" commands/scr/build-print.md` returns a match
    - `grep "command -v gs" commands/scr/build-print.md` returns a match
    - `grep "wpp\|words-per-page" commands/scr/build-print.md` returns a match
    - `grep "Estimated" commands/scr/build-print.md` returns a match (page-count warning format)
    - `grep "Building anyway" commands/scr/build-print.md` returns a match
    - `grep "\-\-strict" commands/scr/build-print.md` returns a match
    - `grep "print-{platform}.pdf\|print-kdp.pdf\|print-ingram.pdf" commands/scr/build-print.md` returns a match
    - `grep "\-\-pdf-engine=typst" commands/scr/build-print.md` returns a match
    - `grep "not supported\|unsupported\|Trim size" commands/scr/build-print.md` returns a match (trim rejection)
  </acceptance_criteria>
  <done>build-print.md exists, has all required gates, Pandoc+Typst prerequisites, platform validation, trim-size validation, page-count guardrail (warning + --strict hard block), PDF invocation, and success report. BUILD-02/03/04 and PLATFORM-01/02 tests all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command file -> shell | build-ebook.md and build-print.md invoke shell commands (pandoc, typst, gs); all inputs are local file paths |
| --platform / --trim flags -> manifest | User-supplied platform and trim values are validated against manifest before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Tampering | --platform flag value | mitigate | validate --platform against validatePlatform() list before reading manifest; invalid slug -> stop with clear error |
| T-32-02-02 | Tampering | --trim flag value | mitigate | validate --trim against manifest.trim_sizes map before use; unlisted size -> stop with valid list shown |
| T-32-02-03 | Information Disclosure | .manuscript/output/ | accept | Output directory is in project folder, writer-owned local files; no external exposure |
| T-32-02-04 | Denial of Service | pandoc/typst subprocess | accept | External tool invocations are local; no network surface; build can be interrupted with Ctrl-C |
| T-32-02-05 | Elevation of Privilege | command -v gs injection | mitigate | platform check is string equality against known slug list, not shell interpolation |
</threat_model>

<verification>
After both tasks complete, run the full Phase 32 test suite:

```bash
node --test test/phase32-build-pipelines-platform-awareness.test.js
```

Expected: BUILD-01 through BUILD-05 tests pass. PLATFORM-01/PLATFORM-02 tests for build-print.md content pass. PLATFORM-03 tests (manifest status: active) still fail - those are addressed in Wave 3 (32-03-PLAN.md).

Also verify the file structure:
```bash
ls commands/scr/build-ebook.md commands/scr/build-print.md
```
</verification>

<success_criteria>
- commands/scr/build-ebook.md exists with STEP 1.5, STEP 1.6, STEP 2 (Pandoc check), STEP 3 (assemble), STEP 4 (Pandoc EPUB invocation with EAA accessibility), STEP 5 (report)
- commands/scr/build-print.md exists with STEP 1.5, STEP 1.6, STEP 2 (Pandoc + Typst + conditional Ghostscript), STEP 2.5 (platform/trim validation + page-count guardrail), STEP 3 (assemble), STEP 4 (Pandoc PDF invocation), STEP 5 (report)
- BUILD-01 through BUILD-05 all pass in the Phase 32 test suite
- Both files committed to git with message: `feat(32-02): add build-ebook.md and build-print.md command files`
</success_criteria>

<output>
After completion, create `.planning/phases/32-build-pipelines-platform-awareness/32-02-SUMMARY.md`
</output>
