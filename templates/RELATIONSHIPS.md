---
creative_pillar: cast
always_load_for: [relationship-map]
authority: derived
---

# Relationship map

This file is auto-regenerated by Scriveno from the relationship sections of the adapted cast surface. It is the one place to open and see every pairing at a glance, including pairs with no relationship. Do not hand-edit -- it is rebuilt from disk whenever a cast entry's relationships change (on `/scr:new-character`, `/scr:character-touch`, `/scr:relationship-map --edit`, `/scr:save`, and `/scr:scan --fix`).

If this file is missing, stale, or disagrees with the adapted cast surface, run `/scr:save` (or `/scr:scan --fix`) to rebuild it. To change a relationship, edit the adapted cast entries (via `/scr:new-character`, `/scr:character-touch`, or `/scr:relationship-map --edit`); this file follows.

**Updated:** {{LAST_UPDATED}}
**Updated by:** {{LAST_COMMAND}}
**Project:** {{TITLE}} ({{WORK_TYPE}})

## Cast

{{CAST_COUNT}} cast entries. Every pair below is accounted for; a pair with no established relationship is marked `none` rather than left blank.

## Pairwise matrix

{{RELATIONSHIP_MATRIX}}

Legend: `->` directional, `--` mutual, `none` no established relationship, `(deceased)` / `(absent)` status carried from the adapted cast entries.

## Interaction notes

For each load-bearing pair, the dynamics carried from the adapted cast entries (trust posture, conflict pattern, speech shift, hidden agenda or fear):

{{INTERACTION_NOTES}}

## Undefined pairs

Pairs that exist in the cast but have no relationship described yet, and no `none` confirmed. Each is a prompt, not an error -- define it (or confirm `none`) via `/scr:relationship-map --edit` or `/scr:character-touch`. The system surfaces the gap; it never invents a relationship to fill it.

{{UNDEFINED_PAIRS}}

## Trust check

The map above is derived from the relationship sections of the adapted cast surface. The pairings are claims, not facts -- run `/scr:scan` to confirm they still match the adapted cast entries.

**Last `/scr:scan` run:** {{LAST_SCAN}}
**Last drift verdict:** {{LAST_SCAN_VERDICT}}

---
*Derived map. See `docs/relationships-protocol.md` for how it is computed. To change a relationship, edit the adapted cast entries; this file follows.*
