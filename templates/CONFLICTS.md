---
creative_pillar: structure
always_load_for: [plot-graph]
authority: derived
---

# Conflict map

This file is auto-regenerated by Scriveno from the core conflict in `WORK.md` and the character entries in `CHARACTERS.md`. It is the one place to open and see the work's conflicts at every level: the central conflict, and every character pairing, including pairs with no conflict. Do not hand-edit -- it is rebuilt from disk whenever the conflict picture changes (on `/scr:new-character`, `/scr:character-touch`, `/scr:save`, and `/scr:scan --fix`).

If this file is missing or stale, run `/scr:save` (or `/scr:scan --fix`) to rebuild it. To change a conflict, edit `WORK.md` (central conflict) or the character entries (pairwise); this file follows.

**Updated:** {{LAST_UPDATED}}
**Updated by:** {{LAST_COMMAND}}
**Project:** {{TITLE}} ({{WORK_TYPE}})

## Central conflict

{{CENTRAL_CONFLICT}}

- **Primary type:** {{CONFLICT_TYPE}} (character vs self / character / nature / supernatural / technology / society)
- **Crisis (lowest point):** {{CRISIS_REF}}
- **Climax (the turn):** {{CLIMAX_REF}}

Crisis and climax are distinct: the crisis is the disaster where the protagonist has no options and story energy peaks against them; the climax is the moment of success that follows. The arc positions live in `OUTLINE.md`.

## Pairwise conflict matrix

Every character pair is accounted for. A pair in conflict shows its nature and stakes; a pair with none is marked `no conflict` rather than left blank.

{{CONFLICT_MATRIX}}

Legend: `no conflict` no established conflict, `(latent)` implied by clashing wants but not yet on the page, `(open)` active on the page, `(resolved)` settled.

## Scene conflict

Per-unit scene conflict, rolled up from the unit plans (each scene's goal, the obstacle in its way, and the outcome). Units with no scene conflict captured yet are listed as pending.

{{SCENE_CONFLICT}}

## Undefined pairs

Pairs whose conflict status no one has established yet. Each is a prompt to decide (including deciding `no conflict`), not an error:

{{UNDEFINED_CONFLICT_PAIRS}}

## Trust check

The map above is derived from `WORK.md` and the character entries. The conflicts are claims, not facts -- run `/scr:scan` to confirm they still match the source.

**Last `/scr:scan` run:** {{LAST_SCAN}}
**Last drift verdict:** {{LAST_SCAN_VERDICT}}

---
*Derived map. See `docs/conflict-protocol.md` for how it is computed. To change a conflict, edit `WORK.md` or the character entries; this file follows.*
